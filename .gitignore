# Dependencies
node_modules/
.pnp
.pnp.js

# Build
dist/
build/
*.local

# IDE — tracked: `.vscode/settings.json`, `.vscode/extensions.json` (team); rest local
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.local

# Registro maestro credenciales — solo local / cifrado (ver npm run credentials:registry)
docs/team/credentials-registry/local/

# Mercado Libre — token store local cifrado (OAuth; nunca commitear)
.ml-tokens.enc

# BMC Dashboard — service account key (never commit)
**/service-account*.json
docs/bmc-dashboard-modernization/service-account.json
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Expert dev traceability — snapshots locales (regenerar con npm run expert:checkpoint)
.cursor/dev-checkpoints/

# Claude/Cursor local worktrees are nested git checkouts; never commit gitlinks.
.claude/worktrees/

# Runtime exports (MATRIZ CSV pull, logs locales)
.runtime/

# Cache
.eslintcache
.parcel-cache
.codebuddy
.vercel

# Nested copies / other projects accidentally created inside this repo (never commit)
Calculadora-BMC/
OmniCRM-Sync/

# Mercado Libre — exportaciones de corpus (pueden contener datos de terceros; regenerar con npm run ml:corpus-export)
docs/team/panelsim/reports/ml-corpus/exports/

# Follow-up tracker (local JSON; opcional FOLLOWUP_STORE_PATH)
.followup/

# Email snapshot ingest dedupe (npm run email:ingest-snapshot)
.email-ingest/

# channels:automated --write
.channels/

# Catálogo local de fichas PDF (no versionar)
PDF Productos /
PDF Productos/

# Bundles locales de export (no versionar)
bmc-logistica-transport-bundle.zip
.env*.local

# Audit relevamiento — dumps y logs crudos (solo entregables matriz/*.md se commitean)
.relevamiento/dumps/
.relevamiento/logs/
.relevamiento/inventarios/
.relevamiento/scripts/

# Modo Transportista — evidencias locales (dev upload-b64)
data/transportista-evidence/
data/training-kb.json
data/training-sessions/

# UX — sesiones de vídeo locales (pesadas / posible PII; ver docs/team/ux-feedback/sessions/README.md)
docs/team/ux-feedback/sessions/**/*.mp4
docs/team/ux-feedback/sessions/**/*.mov
docs/team/ux-feedback/sessions/**/*.m4v
docs/team/ux-feedback/sessions/**/audio.wav
docs/team/ux-feedback/sessions/**/extracted-*/**
docs/team/ux-feedback/sessions/**/extracted/**

# AUTOTRACE (Python helpers)
tools/release-traceability/scripts/__pycache__/

# Accessible Base — local Sheets snapshots (auto-generated, not versioned except manifest)
.accessible-base/*.json
!.accessible-base/manifest.json
