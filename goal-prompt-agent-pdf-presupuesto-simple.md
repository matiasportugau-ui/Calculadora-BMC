# Role
Backend + PDF pipeline engineer for Calculadora BMC / Panelin. Wire the automated AI agent (`generar_pdf` tool) to emit customer PDFs using the same **Presupuesto Simple** (`layout: 'simple'`) template family that the manual calculator uses — not the legacy inline HTML path.

# Context
[CONFIRMED: `docs/team/PROJECT-STATE.md` §2026-06-14] The recommended customer-facing PDF default in the React calculator is `'simple'` ("Presupuesto Simple") — navy BMC brand, real logo, lightweight 1–2 page output. It is selected via `localStorage` key `bmc.pdfLayout` and rendered through `renderPdfLayout()` in `src/pdf-templates/index.js`.

[CONFIRMED: `server/lib/agentTools.js` L1295–1352] The AE-Agent tool `generar_pdf` POSTs to `/calc/cotizar/pdf` via `postCotizarPdf()` in `server/lib/calcLoopbackClient.js` with `source: "ae_agent"`. The tool schema has **no `layout` parameter**.

[CONFIRMED: `server/routes/calc.js` L558–577] `POST /calc/cotizar/pdf` builds HTML with legacy `generatePrintHTML()` from `src/utils/helpers.js` — **not** `buildQuotationModel()` + `renderPdfLayout()`. Agent PDFs therefore diverge visually from manual "PDF Cliente" exports.

[INFERRED: user instruction truncated at "presupuesto simple with..." | basis: repo default layout + agent/calc mismatch] The user wants agent-exported PDFs to match **Presupuesto Simple** (`simple`), aligning agent channel output with the calculator UI default. Any extra "with …" clause (variant color, specific fields, versioning footer) is **unconfirmed** — treat as optional follow-up unless clarified in the saved prompt edit.

[CONFIRMED: `docs/team/panelsim/AE-AGENT-CALC-CONTRACT.md`] Agent quote tools must keep loopback HTTP, `source: "ae_agent"` provenance, and registry archiving via `registerQuotation()`.

# Goal
Make PDFs generated by the automated AI agent (`generar_pdf` → `/calc/cotizar/pdf`) use the **Presupuesto Simple** template (`layout: 'simple'`) so they match the manual calculator's default client PDF.

- Trace the full agent PDF pipeline from `generar_pdf` through HTML generation, GCS/Drive upload, and `/calc/pdf/:id` serving.
- Replace or branch the legacy `generatePrintHTML()` path on `/calc/cotizar/pdf` to use `buildQuotationModel()` + `renderPdfLayout('simple', q)` (or equivalent server-side wiring).
- Default `layout` to `'simple'` for `source: "ae_agent"`; optionally accept `layout` in the request body for future overrides without breaking the calculator path.
- Preserve `source: "ae_agent"`, quote registry, identity quote upsert, and existing API response shape (`pdf_id`, `pdf_url`, `gcs_url`, `drive_url`, `resumen`).
- Add/update offline tests covering agent PDF layout selection.
- Run `npm run gate:local` (and `npm run smoke:bmc-pdf` if API HTML path changes materially).

# Scope
IN:
- `server/routes/calc.js` — `POST /cotizar/pdf` HTML generation
- `server/lib/agentTools.js` — optional `layout` param on `generar_pdf` (default `'simple'`)
- Shared PDF template wiring: `src/pdf-templates/index.js`, `buildQuotationModel()`, `renderPdfLayout()`
- Tests: `tests/agentTools.test.js`, `tests/calc-routes.validation.js`, new/updated layout assertion tests
- Minimal doc note in `docs/team/panelsim/AE-AGENT-CALC-CONTRACT.md` if behavior changes

OUT:
- Changing manual calculator UI default layout (already `'simple'`)
- Migrating all legacy `generatePrintHTML()` call sites (wolfboard, superAgent) unless required for parity
- Switching default to `simple-carbon` or other variants unless user explicitly edits this prompt
- Deploy to Vercel/Cloud Run (code-only; deploy is separate step)
- CRM/Sheets schema changes
- Prompt-only changes in `chatPrompts.js` without fixing the server HTML path

# Inputs
- [CONFIRMED] `server/routes/calc.js` — `/cotizar/pdf` handler (~L487–664)
- [CONFIRMED] `server/lib/agentTools.js` — `generar_pdf` tool definition + handler
- [CONFIRMED] `server/lib/calcLoopbackClient.js` — `postCotizarPdf()`
- [CONFIRMED] `src/pdf-templates/index.js` — `LAYOUT_OPTIONS`, `buildQuotationModel()`, `renderPdfLayout()`
- [CONFIRMED] `src/pdf-templates/simple.js` — Presupuesto Simple template
- [CONFIRMED] `src/utils/helpers.js` — legacy `generatePrintHTML()` (current agent path)
- [CONFIRMED] `src/components/PanelinCalculadoraV3_backup.jsx` — reference for how UI builds `QuotationModel` and calls `renderPdfLayout(pdfLayout, q)` (~L3747)
- [CONFIRMED] `docs/team/panelsim/AE-AGENT-CALC-CONTRACT.md`
- [CONFIRMED] `tests/agentTools.test.js`, `tests/calc-routes.validation.js`
- [ASSUMPTION: user wants layout id `'simple'` not a color variant | verify before executing — if they meant `simple-carbon`, change default accordingly]

# Tools & MCPs
- Read/grep codebase under `/Users/matias/calculadora-bmc`
- Shell: `npm run gate:local`, `npm test`, `node tests/agentTools.test.js`, optional `npm run smoke:bmc-pdf` with API on `:3001`
- Optional: `npm run start:api` for manual PDF spot-check via `POST /calc/cotizar/pdf` with `source: "ae_agent"`
- Tools NOT needed: Sheets MCP, deploy MCP, browser automation (unless visual diff requested)

# Constraints & Guardrails
- DO NOT break the AE-Agent loopback contract — keep `postCotizarPdf` → `127.0.0.1:${config.port}/calc/cotizar/pdf` [CONFIRMED: AE-AGENT-CALC-CONTRACT.md]
- DO NOT remove or alter `source: "ae_agent"` provenance or `registerQuotation()` archiving
- DO NOT hardcode sheet IDs, tokens, or production URLs — use `config.*` / `process.env.*`
- DO NOT commit `.env` or credentials
- DO NOT use `npm audit fix --force`
- DO NOT change master price sheets or fiscal data (read-only)
- DO preserve API response fields consumed by chat (`pdf_id`, `pdf_url`, `gcs_url`, `drive_url`, `resumen`, `instrucciones`)
- DO keep backward compatibility for non-agent callers of `/calc/cotizar/pdf` — document if legacy HTML remains for `source: "calculator"` or migrate both paths consistently
- DO run `npm run lint` on any edited `src/` files
- DO append a line under "Cambios recientes" in `docs/team/PROJECT-STATE.md` if behavior ships

# Anti-patterns
- DO NOT assume `localStorage` / `bmc.pdfLayout` exists on the server — agent path has no browser storage; layout must be explicit in server code or request body
- DO NOT only update `chatPrompts.js` telling the agent to "use simple layout" without fixing `/calc/cotizar/pdf` HTML generation — the bug is server-side today
- DO NOT default agent PDFs to `simple-carbon` based on stale docs (`presupOrchestrator.js`, May 2026 production notes) — [CONFIRMED: PROJECT-STATE 2026-06-14] UI default is `'simple'`
- DO NOT duplicate QuotationModel mapping logic — reuse `buildQuotationModel()` and mirror field mapping already done in `calc.js` L519–576
- DO NOT break ESM imports — server already imports from `src/utils/helpers.js`; dynamic import for templates matches `renderPdfLayout` pattern
- DO NOT return 500 for recoverable template errors without logging; follow existing calc route error semantics

# Deliverables
- Code change in `server/routes/calc.js` (and helpers if extracted) so agent PDFs render via `renderPdfLayout('simple', …)`
- Optional: `layout` property on `generar_pdf` tool schema + pass-through in `agentTools.js` body (default `'simple'`)
- Updated tests proving `/calc/cotizar/pdf` with `source: "ae_agent"` uses simple template (assert HTML contains Presupuesto Simple markers — e.g. BMC logo path, navy brand, template-specific structure — not legacy `buildPdfHead`-only output)
- Entry in `docs/team/PROJECT-STATE.md` "Cambios recientes" if merged
- Short implementation note in `docs/team/panelsim/AE-AGENT-CALC-CONTRACT.md` §generar_pdf if layout param added

# Success Criteria
- `POST /calc/cotizar/pdf` with `{ source: "ae_agent", escenario: "solo_techo", techo: { familia: "ISOPANEL", espesor: "100", zonas: [{ largo: 10, ancho: 8 }] } }` returns `ok: true` and stored HTML matches **simple** template (visual or structural assertion in test)
- Agent PDF HTML is **not** identical to legacy `generatePrintHTML()` output for the same quote (proves template switch)
- Manual calculator PDF path unchanged or intentionally unified — no regression in `npm run gate:local:full` if frontend touched
- `npm run gate:local` passes (lint + test + test:api)
- `tests/agentTools.test.js` generar_pdf cases still pass; add layout assertion if feasible
- Optional manual check: generated HTML opens correctly at `/calc/pdf/:id` and prints as 1–2 page client quote

# Operational Anchors
- Source hierarchy: planilla validada (operativa) > repos vigentes (lógica) > docs de fórmulas (documental) > dashboards viejos (auxiliar). Never treat a copy as master.
- State labeling: every claim the executor produces should be marked `hecho confirmado`, `inferencia`, or `duda abierta`.
- Triangulation: planilla → repo → documentation → consolidate. For this task, triangulate **UI PDF path** (`PanelinCalculadoraV3_backup.jsx` + `pdf-templates/`) vs **agent path** (`calc.js` `/cotizar/pdf`) vs **PROJECT-STATE** default layout notes.
- Read-only by default: parámetros, logs, automation tabs, master prices, fiscal data.
- If docs conflict on default layout (`simple` vs `simple-carbon`): trust latest PROJECT-STATE (2026-06-14 → `'simple'`) over May 2026 production notes.

# Open Items
- [ASSUMPTION: user wants layout id `'simple'` ("Presupuesto Simple") | verify before executing — truncated instruction ended at "presupuesto simple with..."]
- [ASSUMPTION: trailing "with …" may mean a specific variant (e.g. with logo, with IVA footer, with client block) — Presupuesto Simple already includes standard client/BOM/totals; confirm if extra fields needed]
- [ASSUMPTION: non-agent `/calc/cotizar/pdf` callers (GPT Actions, external integrations) should also get `'simple'` or keep legacy HTML — default recommendation: unify all server PDF generation to template system with `'simple'` default]
- [INFERRED: no layout env var exists today | basis: grep server for pdfLayout/layout on cotizar/pdf] — if product wants env-driven default (`BMC_AGENT_PDF_LAYOUT`), add only if explicitly requested
