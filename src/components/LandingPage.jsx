export default function LandingPage() {
  // TEMPORARY: Bypass authentication entirely for immediate calculator access
  // TODO: Re-enable Google OAuth login once configured (see authGoogle.js)
  return null;
}
