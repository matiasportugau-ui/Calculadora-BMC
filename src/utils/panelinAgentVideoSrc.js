/** Loop MP4 for Panelin agent avatar (header + chat drawer). */
export const PANELIN_AGENT_VIDEO_SRC = `${typeof import.meta !== "undefined" ? import.meta.env?.BASE_URL ?? "/" : "/"}video/panelin-lista-loop.mp4`;
