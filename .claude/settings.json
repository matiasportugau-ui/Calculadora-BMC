{
  "permissions": {
    "allow": [
      "Bash(npm run *)",
      "Bash(npx *)",
      "Bash(node *)",
      "Bash(git *)",
      "Bash(ls *)",
      "Bash(gcloud logging:*)"
    ]
  },
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npm run env:ensure && node -e \"console.log(''); console.log('=== Calculadora BMC ==='); console.log(''); console.log('  npm run dev:full    API :3001 + Vite :5173'); console.log('  npm run dev         Vite only'); console.log('  npm run gate:local  lint + test (before commit)'); console.log('');\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
