# MATRIZ DE CONFIGURACIÓN BMC / PANELIN — Parte 1

**Fecha relevamiento:** 2026-04-23
**Entorno:** Claude Code en contenedor remoto (solo lectura)
**Alcance real:** repo `matiasportugau-ui/calculadora-bmc` (MCP + local). GCP, Drive/Sheets y otros repos NO accesibles desde este entorno — ver `gaps.md`.

## Leyenda

- **Estado:** 🟢 verde (master claro) · 🟡 amarillo (probable con aux compitiendo) · 🔴 rojo (sin master claro o conflicto grave)
- **Última modificación:** fecha de commit/archivo cuando es visible desde este entorno. `?` = no verificable.
- Etiquetas: `hecho confirmado` (validado en archivos trackeados de este repo) · `inferencia` (deducido de docs/código, no corroborado fuera del repo) · `duda abierta` (requiere acceso que no tengo).

## Tabla maestra

| # | Dominio | Master (ID + tab/path) | Auxiliar vivo | Auxiliar histórico | Repo asociado | Endpoint / deploy | Última mod. visible | Permiso recomendado | Conflictos | Estado |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | **Cotizaciones** | Sheet `1N-4kyT_uSPSVnu5tMIc6VzFIaga8FHDDEDGcclafRWg` (BMC crm_automatizado) · tab **CRM_Operativo** | Sheet `1Ie0KCpgWhrGaAKGAS1giLo7xpqblOUOIHEg1QbOQuu0` (2.0 – Administrador de Cotizaciones) como fuente para tab `Admin_Cotizaciones` | tab `Master_Cotizaciones` (schema alt, controlado por `BMC_SHEET_SCHEMA`) | `matiasportugau-ui/calculadora-bmc` — `server/routes/calc.js` + `server/routes/legacyQuote.js` + `server/routes/agentChat.js` (buildQuote) | Cloud Run `panelin-calc` (us-central1) · endpoints `POST /calc/cotizar`, `POST /calc/cotizar/pdf`, `POST /calc/calculate_quote`, `POST /api/agent/chat` (buildQuote action) | Commit main `1118dfd` (2026-04-22) | Editor para service account en workbook principal; Lector en `1Ie0KCpg...` | Dos schemas simultáneos (CRM_Operativo vs Master_Cotizaciones); dos rutas de cotización (`/calc/*` y `/quote/*` legacy) | 🟡 |
| 2 | **CRM** | Sheet `1N-4kyT_uSPSVnu5tMIc6VzFIaga8FHDDEDGcclafRWg` · tab **CRM_Operativo** | Extensión Chrome **OmniCRM Sync** (ML → Sheets), referenciada en ~10 docs | Master_Cotizaciones; AUDIT_LOG (opcional) | `matiasportugau-ui/calculadora-bmc` (rutas cockpit en `server/routes/bmcDashboard.js` líneas 2384–2752) + extensión Chrome **no en este repo** | Cloud Run `panelin-calc` · 13 endpoints cockpit `/crm/*` (cockpit-token, quote-link, approval, mark-sent, send-approved, ml-queue, wa-queue, sync-ml) | Commit main `1118dfd` | Editor; token `API_AUTH_TOKEN` (= `API_KEY`) requerido; allowlist `COCKPIT_TOKEN_ALLOWED_ORIGINS` | Repo extensión OmniCRM Sync fuera de `calculadora-bmc` y fuera del scope MCP → no puedo confirmar estado | 🟡 |
| 3 | **Precios** | Sheet `1oDMkBgWxX7cu7TpSvuO30tCTUWl68IBDhC4cQTP79Xo` (MATRIZ de COSTOS y VENTAS 2026) — default hardcoded en `server/config.js:52` | CSV local `src/data/constants.js` + overrides runtime vía `PricingEditor.jsx` + `src/utils/pricingOverrides.js` | Archivos `docs/google-sheets-module/MATRIZ-PRECIOS-CALCULADORA.md`, `MAPPER-PRECISO-PLANILLAS-CODIGO.md` (mapping SKU→path) | `matiasportugau-ui/calculadora-bmc` — `server/routes/bmcDashboard.js` + `server/routes/calc.js` (matriz_precios JSON) + `src/data/matrizPreciosMapping.js` | Cloud Run `panelin-calc` · `GET /api/actualizar-precios-calculadora` (descarga CSV), `POST /api/matriz/push-pricing-overrides` (escritura autenticada) | Commit main `1118dfd` | Lector para GET público; Editor con `API_AUTH_TOKEN` para POST push | Dos caminos paralelos: (a) CSV MATRIZ + overrides vs (b) constants.js hardcoded → necesitan sincronizar manualmente | 🟡 |
| 4 | **Ventas / envíos** | Sheet `1KFNKWLQmBHj_v8BZJDzLklUtUPbNssbYEsWcmc0KPQA` (2.0 – Ventas) · múltiples tabs (merge en `/api/ventas`) | Sheet `1egtKJAGaATLmmsJkaa2LlCv3Ah4lmNoGMNm4l0rXJQw` (Stock E-Commerce) — `/api/stock-*`; tabs `Ventas_Consolidado`, `Coordinaciones` | tab mensual calendario · legacy tab 'Ventas realizadas y entregadas' en workbook principal | `matiasportugau-ui/calculadora-bmc` — `server/routes/bmcDashboard.js` (rutas `/ventas`, `/ventas/tabs`, `/stock-*`, `/coordinacion-logistica`, `/marcar-entregado`) + **Postgres Transportista** (`server/routes/transportista.js` + `server/lib/transportista*`) | Cloud Run `panelin-calc` · ~10 rutas; **Postgres** separado (`DATABASE_URL`) para transportista | Commit main `1118dfd` | Editor en ambos Sheets; DB Postgres separada (viajes/evidencias) | Tres capas concurrentes: 2.0-Ventas (Sheets) + Stock E-Commerce (Sheets) + transportista (Postgres) sin sync docs explícito | 🟡 |
| 5 | **Pagos / fiscal** (RO estricto) | Sheet `1AzHhalsZKGis_oJ6J06zQeOb6uMQCsliR82VrSKUUsI` (Pagos Pendientes 2026) · tab real **`Pendientes_`** (no "Pagos_Pendientes" como documentó doc legacy) + Sheet `1bvnbYq7MTJRpa6xEHE5m-5JcGNI9oCFke3lsJj99tdk` (Calendario vencimientos) | tab `Metas_Ventas` (opcional, degrada limpio); AUDIT_LOG | Planilla histórica "Pagos_Pendientes" referenciada en algunos docs viejos | `matiasportugau-ui/calculadora-bmc` — `server/routes/bmcDashboard.js` (`/pagos*`, `/kpi-financiero`, `/calendario-vencimientos`, `/audit`, `/metas-ventas`, `/kpi-report`) | Cloud Run `panelin-calc` | Commit main `1118dfd` | Editor; todas las mutaciones van a AUDIT_LOG | Columna **MONTO** vs columnas D/E (PRECIO_VENTA / COSTO_COMPRA) — fuente distinta según endpoint, documentado como nota 2026-03-27 pero aún susceptible de confusión | 🟡 |
| 6 | **Calculadora (código)** | Repo `matiasportugau-ui/calculadora-bmc` · branch **`main`** · entry point `src/components/PanelinCalculadoraV3_backup.jsx` (re-exportado desde `PanelinCalculadoraV3.jsx`) | Repos hermanos referenciados: `GPT-Panelin-Calc`, `Calculadora-BMC-GPT`, `GPT-PANELIN-V3.2`, `aistudioPAnelin` (no accesibles desde este entorno) | Carpeta `Calculadora-BMC/` anidada y `OmniCRM-Sync/` en `.gitignore` como "nested copies / other projects accidentally created inside this repo" | `matiasportugau-ui/calculadora-bmc` | Vercel prod `calculadora-bmc.vercel.app` + Cloud Run `panelin-calc-web` (alt) | Commit main `1118dfd` (2026-04-22). Rama activa actual: `claude/bmc-production-audit-3Pjos` (este relevamiento) | Editor para el dueño; resto Lector | 84 ramas abiertas (20 PRs open, 15 de ellos draft); múltiples estrategias de deploy coexisten (Vercel + Cloud Run frontend + Cloud Run full-stack) — ver dominio 8 | 🔴 |
| 7 | **Dashboards / KPIs** | Backend `server/routes/bmcDashboard.js` (2781 líneas) — endpoint `/api/kpi-report` agrega todo | Apps Scripts: `Code.gs`, `CalendarioRecordatorio.gs`, `PagosPendientes.gs`, `StockAlertas.gs`, `VentasConsolidar.gs` (en `docs/bmc-dashboard-modernization/`) | Copias duplicadas en `docs 2/bmc-dashboard-modernization/Code.gs` y `docs.zip` | `matiasportugau-ui/calculadora-bmc` + workbook Sheets; los `.gs` se despliegan manualmente a Apps Script (no automatizado desde repo) | Cloud Run `panelin-calc` para API; SPA dashboard expuesta por `Dockerfile.bmc-dashboard` (`VITE_BASE=/calculadora/`) | Commit main `1118dfd` | Editor en Sheets; Apps Script bound to workbook | `.gs` en repo son solo referencia: no hay forma de confirmar desde acá qué versión está realmente ejecutándose en Google Workspace | 🟡 |
| 8 | **Automatizaciones / infra** | Cloud Run services en `chatbot-bmc-live` (project) / `us-central1`: **`panelin-calc`** (API) + **`panelin-calc-web`** (frontend nginx) | Cloud Run `cloudbuild.yaml` construye `Dockerfile.bmc-dashboard` (full-stack) → **posible servicio alt también llamado `panelin-calc`** | Launchagents macOS locales (`scripts/install-*-schedule.sh`, `drive-cleanup-launchd.plist`); scripts dev en `scripts/shell-aliases-calculadora-bmc.zsh` | `matiasportugau-ui/calculadora-bmc` + Artifact Registry `us-central1-docker.pkg.dev/chatbot-bmc-live/cloud-run-repo/*` | Workflows `.github/workflows/deploy-calc-api.yml` (API) y `deploy-frontend.yml` (nginx); `ci.yml` ejecuta smoke prod; `knowledge-antenna-scheduled.yml` | Workflows commit `1118dfd` | WIF (`GCP_WIF_PROVIDER`); SA deploy (`GCP_DEPLOY_SA_EMAIL`) y runtime (`GCP_RUNTIME_SA_EMAIL`) | **Tres modelos de deploy coexisten** (ver `conflictos.md` #1); GCR (`gcr.io/$PROJECT_ID`) vs Artifact Registry para la misma imagen | 🔴 |

## Servicios confirmados y referenciados (desde workflows + env.example + docs)

- **GCP project:** `chatbot-bmc-live` — `inferencia desde workflows` (no gcloud disponible para confirmar)
- **Región Cloud Run:** `us-central1` — `inferencia desde workflows`
- **Artifact Registry:** `us-central1-docker.pkg.dev/chatbot-bmc-live/cloud-run-repo/` — `inferencia desde workflows`
- **Cloud Run services declarados:**
  - `panelin-calc` (API) — imagen `cloud-run-repo/panelin-calc:<sha>` — `inferencia`
  - `panelin-calc-web` (frontend) — imagen `cloud-run-repo/panelin-calc-web:<sha>` — `inferencia`
- **URL Cloud Run canónica en código (`.env.example` + `server/config.js`):** `https://panelin-calc-642127786762.us-central1.run.app` — `hecho confirmado (string en repo)` / `duda abierta (sobre el estado en vivo)`
- **Vercel:** `https://calculadora-bmc.vercel.app` (producción) — `hecho confirmado (string en repo)` / `duda abierta (sobre el estado en vivo)`
- **Service account Sheets:** `bmc-dashboard-sheets@chatbot-bmc-live.iam.gserviceaccount.com` — `hecho confirmado (declarado en docs/google-sheets-module/planilla-inventory.md)` / `duda abierta (sobre permisos actuales reales en cada workbook)`

## Observaciones de alto nivel

1. **El dominio 6 (Calculadora / código) está en rojo** por deuda de ramas/PRs abiertos (84 ramas, 20 PRs open, 15 draft) — no por falta de master. El master es `main`, pero hay mucha carga de integración pendiente.
2. **El dominio 8 (Infra) está en rojo** por conflicto de modelos de deploy: ver `conflictos.md`.
3. Los demás dominios tienen **master claro** pero con auxiliares compitiendo → clasificación 🟡 consistente.
4. **No se detectaron credenciales hardcodeadas** en archivos git-tracked (no `sk-*`, no `AIza*`, no `BEGIN PRIVATE KEY`, no `Bearer <token>`). `.env` existe pero está en `.gitignore`. Ver `credenciales-sospechas.txt` y `credenciales-detalle.txt` para ubicaciones de referencias textuales.
