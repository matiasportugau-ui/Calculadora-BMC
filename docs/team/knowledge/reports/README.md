# Knowledge Reports

This folder stores generated research-antenna outputs:

- `KNOWLEDGE-REPORT-YYYY-MM-DD.md`

Generate a report with:

- `npm run knowledge:scan`
- or full pipeline: `npm run knowledge:run`
