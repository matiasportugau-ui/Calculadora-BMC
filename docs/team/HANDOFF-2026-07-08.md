# HANDOFF — 2026-07-08 (consola gestionable de Asistentes IA + resiliencia del seam + seguridad ML)

Continúa `HANDOFF-2026-07-07.md`. Esta sesión convirtió el panel de control de AI en una **consola
gestionable y honesta**, agregó **resiliencia** al seam (nunca quedarse sin AI), cerró **agujeros de
seguridad** en las rutas ML, y dejó **#610 documentado** con las acciones owner-gated.

## Shipped a producción esta sesión (todos mergeados + deployados + prod-verificados)

| PR | Qué | Verificación en prod |
|----|-----|----------------------|
| **#638** | ML "Generar con IA" era **auth, no IA**: `mlFetch` adjunta el JWT de operador | anon→401, token→200 |
| **#639** | **Panel honesto**: `agentCore` retiene `lastError` por proveedor; `assistantHealth` marca `degraded` + proveedor real; panel muestra el motivo | `claude 400 "credit balance"` visible, `ml→gemini` |
| **#641** | **Enable/disable en runtime** sin redeploy (reusa `waConfig`/`wa_settings`, sin migración): `POST /api/assistants/:key/toggle` | toggle `wa` on→503 se va, off→503 vuelve |
| **#642** | Switches on/off en el panel + hint owner-gated | UI wired |
| **#648** | Seguridad: auth en rutas ML que **cambian estado** (answer/items/description) | anon→401 |
| **#651** | **Cooldown para errores duros**: 400/401/403 sidelinea 15 min a la primera falla + `POST /api/assistants/providers/reset-cooldowns` + botón "Reset cooldowns" | cooldown 14.5min + reset limpia |
| **#652** | **Fallback terminal OpenRouter** (open-models, tier gratis) — cadena `…→openai→openrouter` | dormant sin key, boot 200 |
| **#656** | Seguridad: auth en rutas ML de **lectura** (perfil/listings/preguntas/órdenes con PII) | deploy en curso al cerrar |

## Estado actual de prod
- **Asistentes activos:** `canales / panelin / ml` — funcionales pero **degraded** (sirviendo vía Gemini).
  `wa / email / wolfboard` **apagados** (toggle, a pedido).
- **Causa de la degradación = #610** (abajo). El sistema responde bien vía Gemini.
- Panel: `/hub/admin/assistants` — muestra proveedor real + motivo + toggles + Reset cooldowns.

## ⚠️ Pendiente — todo owner-gated (no lo puede hacer el agente)

### 1. #610 — restaurar Claude + Grok (lo que devuelve todo a LIVE)
Causa raíz confirmada (no es código): **Anthropic sin créditos** (400 "credit balance too low") +
**`GROK_API_KEY` inválido** (400 "Incorrect API key"). El seam cae a Gemini.
- **Acción A (financiera):** cargar créditos en console.anthropic.com → Plans & Billing.
- **Acción B (credencial):** key nueva en console.x.ai → GCP Secret Manager (`chatbot-bmc-live`,
  secret `GROK_API_KEY`, **+ New Version**) → redeploy `deploy-calc-api.yml`.
- **Después:** en `/hub/admin/assistants` clickear **"Reset cooldowns"** → el seam reintenta Claude
  al instante (sin esperar 15 min). Verificar `activeProvider=claude` + `cache_read_tokens>0`.
- Prompt listo para la extensión de Claude: `goal-prompt-fix-seam-credentials.md`.

### 2. ⭐ ATAJO recomendado (gratis, ~2 min) — piso de AI garantizado SIN tocar billing
El fallback OpenRouter (#652) ya está deployado, **dormant esperando la key**:
1. Key gratis en **openrouter.ai/keys**.
2. Crear secret `OPENROUTER_API_KEY` en GCP Secret Manager (`chatbot-bmc-live`).
3. Agregar `OPENROUTER_API_KEY=OPENROUTER_API_KEY:latest` al `--set-secrets` de `deploy-calc-api.yml`
   → redeploy.
Con esto el seam nunca se queda sin AI aunque fallen los 4 comerciales. (NO se agregó la línea al
workflow porque montar un secret inexistente rompe el deploy.)

### 3. #358 — pricing PGLC250
Falta el costo real de BROMYROS para PGLC250 (hoy hay un interino 31.08 en la Matriz). Cuando esté →
re-bake `constants.js` (`bake-matriz-to-constants.mjs`) + lint de monotonicidad.

## MCP servers (del screenshot del usuario)
- **task-master-ai** → ✅ **arreglado**: caché npx corrupto (`@ai-sdk/anthropic` drift `.mjs`/`.js`).
  Borré `~/.npm/_npx/…`; reinstala limpio. **Acción:** reiniciar Claude Code para reconectar.
- **notebooks / visualization** → MCP servers de un toolbox GCP (`genai-toolbox` binario **no
  instalado**; no están en `enabledPlugins` ni en ningún `.mcp.json` — entradas huérfanas). No se
  pueden deshabilitar limpio desde config (origen no localizable). **Quitar vía la UI de MCP**
  (Ctrl+O → connectors) si molestan; son ruido inofensivo.
- **Notion [needs auth]** → token OAuth vencido. **Reautenticar** en el panel de connectors (click).

## Seguridad — follow-ups cerrados esta sesión
Rutas ML inline (`server/index.js`) ahora **todas** con `requireMlAuth`
(`requireServiceOrUser({authOnly:true})`): reads + writes. `/auth/ml/*` (OAuth/status) sigue público;
`mlSearch` mantiene su guard propio. Sin agujeros anónimos conocidos en `/ml/*`.

## Verificación pendiente (UI, necesita sesión logueada)
- PR A (#638) botón "Generar con IA" y PR D (#642) switches del panel — verificados a nivel endpoint,
  falta el ojo humano en el navegador logueado. PR #656 deploy en curso al cerrar (verificar
  anon→401 en un `GET /ml/*` cuando termine).

## Git
- Worktree `.claude/worktrees/ai-connections-report`, rama `main`. Esta rama `docs/handoff-2026-07-08`
  es solo para este doc. Untracked: `goal-prompt-*.md` (2, no commitear).

## Prompt para retomar
> Estado: consola de asistentes IA honesta + gestionable + resiliente, todo en prod. Bloqueante =
> #610 (owner). Primero: cargar la key gratis de OpenRouter (#652, ver handoff §2) para el piso de AI.
> Después #610 (créditos Anthropic + Grok key) → "Reset cooldowns" en `/hub/admin/assistants`.
> Verificar PR #656 en prod (`GET /ml/*` anon→401). Cerrar #610 cuando `activeProvider=claude`.
