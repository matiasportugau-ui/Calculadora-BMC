# Handoff — 2026-07-06 Market Intel keyword monitor + sesión review/close

**Current branch**: `fix/docker-bmc-pdf`

**Uncommitted changes**:
- `src/data/calculatorDataVersion.js` (auto-bump local, no commitear sin intención)
- `?? .worktrees/` (gitignored worktrees locales)

**Blockers**:
- **Keyword SERP en prod (opcional):** `keywordSerpPlaywright.js` usa Playwright bundled Chromium; el Docker image solo tiene system Chromium para PDF. Refrescos programados (scheduler P1 04:00 UTC) pueden fallar hasta wirear `CHROMIUM_EXECUTABLE_PATH` o `npx playwright install chromium` en build.
- **UAT humano keyword monitor:** verificar Hub → Market Intelligence → Inteligencia → tabla 25 keywords + botón **Refrescar P1**.
- **smoke:prod 8/9:** `POST /api/crm/suggest-response` 401 sin `API_AUTH_TOKEN` local — pre-existente, no bloquea keyword monitor.

**Next prompt to resume exactly**:
"Continue from here: Wire keyword monitor SERP to `CHROMIUM_EXECUTABLE_PATH` (same pattern as `server/routes/pdf.js`), ship via PR, then verify Refrescar P1 works in prod Cloud Run. Read `docs/team/HANDOFF-2026-07-06-1755.md`."

**Key files touched** (shipped on `main` esta sesión / arc):

| Área | Paths |
|------|-------|
| Keyword monitor backend | `server/lib/marketIntel/keywordMonitor.js`, `keywordSerpPlaywright.js`, `data/keywordSeeds.json`, `data/keywordMonitorState.json`, `scheduler.js` |
| API | `server/routes/marketing.js` — `GET/POST /api/marketing/keywords`, `POST /keywords/refresh` |
| Migration (opcional) | `server/migrations/market-intel/009_create_keyword_monitor.sql` |
| UI | `src/components/marketing-hub/KeywordMonitor.jsx`, `IntelPanel.jsx`, `MarketingHubModule.jsx` |
| Tests | `tests/market-intel/keywordMonitor.test.js` |
| Config | `.env.example` (`KEYWORD_MONITOR_*` vars), `package.json` (`playwright:install`) |
| Skills creados (fuera repo) | `~/.grok/skills/site-spider-analyze/`, `~/.grok/skills/market-keyword-research/` |
| Docker WIP (rama actual) | `server/Dockerfile` — `COPY public/bmc-pdf` (PR pendiente merge) |

**Ship status (keyword monitor — DONE on `main`)**:

| PR | Estado | Commit |
|----|--------|--------|
| #585 | Merged | `a4a29672` feat(market-intel): live keyword monitor |
| #589 | Merged | `7093a995` fix: lazy-load Playwright (Cloud Run boot) |
| #599 | Merged | `9c78aa1e` feat(hub): operator overview + PDF logo fix |

**Prod verification (2026-07-06)**:
- `GET /health` → `ok: true` (panelin-calc Cloud Run)
- `GET /api/marketing/keywords` → `401 missing_credentials` (ruta montada, auth OK)
- `origin/main` al día con keyword monitor + operator overview

**Current state reference**:
- See `docs/team/PROJECT-STATE.md` (entrada 2026-07-04 keyword monitor + 2026-07-06 operator overview)
- Raw keyword research: `/tmp/kw-bmc-expanded.json` (266 términos, Uruguay panel sandwich)