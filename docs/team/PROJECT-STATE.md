# Project State — BMC/Panelin

**Última actualización:** 2026-07-24

Fuente única de estado para que todos los agentes estén actualizados. Ver [PROJECT-TEAM-FULL-COVERAGE.md](./PROJECT-TEAM-FULL-COVERAGE.md) para el protocolo de sincronización.

**Tablero de tareas:** el backlog/kanban de **desarrollo** vive en GitHub (Issues + Project «BMC Dev»); este archivo sigue siendo el **relato narrativo** del estado. Reglas en [`AGILE.md`](./AGILE.md), espejo versionado en [`BACKLOG.md`](./BACKLOG.md).

**Guía legacy vs repo:** Si aparece documentación antigua tipo `BMC_SYSTEM_GUIDE.md` (backup Next/Mongo), contrastar con el inventario [BMC-SYSTEM-GUIDE-BACKUP-vs-CURRENT.md](../bmc-dashboard-modernization/BMC-SYSTEM-GUIDE-BACKUP-vs-CURRENT.md) — no usar ese backup como contrato de API del stack actual.

**Evolución:** Roles, skills, áreas y variables no son estáticos; se ajustan tras modificaciones o crecimiento del dominio. Ver PROJECT-TEAM-FULL-COVERAGE §0.

---

## Cambios recientes

**2026-07-26 (feat — agent platform IMP residual pack):** Hub **Costo & latencia** via `GET /api/agent/obs-summary` (memory ring cost Σ + p50/p95/ttft; multi-day still Cloud Logging). Tool tiers `tools-manifest?tier=` (IMP-14). `prompts_sha` boot + obs (IMP-13). Hybrid RAG fuse `RAG_HYBRID` default OFF (IMP-10). SDD platform v1.4 PAOS + glory memo; `test:agent` green.

**2026-07-24 (prod — PAOS LIVE on panelin-calc):** PR [#777](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/777) on main. Cloud Run env `PAOS_ENABLED=1` `PAOS_PROMOTE=1` `PAOS_CANARY_PCT=0` `PAOS_LEDGER_RETENTION_DAYS=90`. **Root cause of health 404:** traffic pinned 100% to old rev `panelin-calc-00877-4b9` while PAOS images built as 00881–00886 (GHA deploy succeeded; post-deploy `smoke:prod` failed on `/api/crm/suggest-response` AI quota — did not block image, but traffic never moved). **Fix:** `gcloud run services update-traffic panelin-calc --to-latest` → **100% LATEST `panelin-calc-00886-g5f`**. **Verified:** `GET https://panelin-calc-q74zutv7dq-uc.a.run.app/api/paos/health` → **200** `{"ok":true,"paos":{"enabled":true,"promote":true,...}}`. Residual: GHA `smoke:prod` still red on AI keys (unrelated to PAOS); optional IMP-06..08.

**2026-07-24 (docs — development-glory re-score 98):** SDD platform **v1.3** reflects IMP-02 `logAgentTurn`, IMP-08 Whisper matrix, IMP-09 voiceMetrics; SCORECARD **98 pass**; P0=0. Residual: RAG enable ops, hub $, p95 baseline.

**2026-07-24 (docs — Meta Ads SDD v0.4 As-Built + bootstrap):** Retag SDD/SCORECARD composite **94 pass** after PR1–PR3 ship; `scripts/meta-ads-bootstrap-auto.sh` (Graph smoke → Doppler `bmc-backend/prd` → GSM → Cloud Run); prod Live still human-gated on Meta BM system-user token. Evidence E-01–E-19; ADR-009 fail-open, ADR-010 range KPIs #767.

**2026-07-24 (feat — IMP-02/04/08/09 product residual):** Shared `logAgentTurn` on SSE + callAgentOnce (IMP-02). RAG precheck fail-closed; prod enable blocked without DB/embed (IMP-04). Whisper/Firefox capability tests + OPS matrix (IMP-08). Durable `agent_voice_events` dual-write via voiceMetrics + voiceErrorLog (IMP-09).
**2026-07-24 (feat — PAOS G2 functional + Cloud Run env; image deploy pending):** SUPERSEDED by **PAOS LIVE** entry above. Supervised loop implemented; env set; was 404 until traffic → LATEST.


**2026-07-24 (feat — PAOS G2 remaining: promote→KB + money eval + PG dual-write):** `paosEvaluate` rejects price deltas without calcProvenance; `paosPromote` writes Training KB on approve (canary=pending, active=permanent); candidates dual-write `learning_candidates` when DATABASE_URL set; migration 003; tests `paosPromote`. Flags still default OFF.


**2026-07-24 (fix — Meta Ads range/KPI after #764):** Re-land `applyDemoRange` + snapshot null-spend when `range !== 30d` on post-PR3 async builder. PR3 fail-open to Snapshot no longer serves monthly `$11k` under a 7d window. Supersedes dirty #761. Cleared leftover conflict markers in this file from #747 merge.

**2026-07-23 (docs — development-glory SDD platform v1.2 / audit 97):** Architect-grade as-built polish of [`docs/sdd/panelin-ai-agent-platform/`](../sdd/panelin-ai-agent-platform/) — reconciled SuperAgent cost, goldens **22**, SSE `done` telemetry, ASSISTANTS snapshot; formal SCORECARD **composite 97 pass:true**, P0=0. Contract test `tests/sddPlatformSchema.contract.test.js`. Product residual: IMP-02/04/08/09 (non-blocking).

**2026-07-23 (feat — Finanzas Proyección cashflow):** Tab `/hub/finanzas/proyeccion` — Zustand + Recharts + dnd-kit timeline, KPIs/runway/scenarios, mock API `GET /api/panelin/cashflow-init` + `PATCH /api/panelin/vencimientos`. Tests `tests/cashflow-project.test.js`.

**2026-07-23 (feat — Wave1 IMP-12 + IMP-01 residual):** SSE `done` emits `provider_used` / `model` / `latency_ms` / optional `ttft_ms`; Dev panel last-turn; contract tests `agentChatDonePayload` + `agentToolsCount` (pin 55). Single `done` on success path (removed duplicate bare done).

**2026-07-23 (feat — IMP-11 channel goldens + IMP-01 MCP tool-count hygiene):** Agent goldens **19→22** (`20-wa-write-sin-confirm`, `21-ml-limite-consulta-larga`, `22-email-draft-no-enviar`); runner assert `tool_not_confirmed`. MCP agent doc points at **55** tools / tools-manifest SoT. `pre-release` still `GOLDEN_REQUIRED=1`.

**2026-07-23 (feat — IMP-07 SuperAgent cost + calc parity):** `logSuperAgentCost` → shared `logAgentCost` (`event: superagent_ai_call`); `ae_agent_quote` log with `source: superagent_inprocess`; export `runSuperAgentCalc` + offline `tests/superAgentCalc.test.js` (totals match `calcTechoCompleto`/`calcParedCompleto`); architecture-fitness guards import. Decision: keep parallel `/quote-lead` route. ADR-007 Accepted. In `test:agent`.

**2026-07-23 (fix — smoke:prod suggest-response 401 local):** `scripts/smoke-prod-api.mjs` soft-skips suggest when `API_AUTH_TOKEN` unset **outside CI** (was hard-fail 401 `missing_credentials`). CI still requires the secret. Sends Bearer + x-api-key. `npm run smoke:prod:auth` = Doppler full check (verified IA ok gemini).

**2026-07-23 (agent — bmc-live-presentation):** New subagent [`.claude/agents/bmc-live-presentation.md`](../../.claude/agents/bmc-live-presentation.md) + slash [`/bmc-live-presentation`](../../.claude/commands/bmc-live-presentation.md) — reads fresh PROJECT-STATE, asks cut-mode Qs (Hero / Feature reel / Full atlas), captures live usage on calculadora-bmc.vercel.app, delivers MP4 + chapters under `.runtime/presentations/`. Evals: `.claude/agents/evals/bmc-live-presentation.json`, `run-bmc-live-presentation.sh`.

**2026-07-23 (docs — Panelin AI platform SDD + audit PASS 96):** SoT bundle [`docs/sdd/panelin-ai-agent-platform/`](../sdd/panelin-ai-agent-platform/) v1.1 As-Built — `SDD.md` §1–12, evidence (tools **55**/ **55**, cost-query, assistants **`canales;ml;panelin`**), IMP guide, recreation checklist closed for docs. Formal audit **composite 96** (`audit/SCORECARD.json`); evolution-loop closed P1 docs (cost OPS §10, RAG OPS §11, SuperAgent event map). Child [`panelin-chat-agent/TARGET.md`](../sdd/panelin-chat-agent/TARGET.md) defers tool counts here. OPS: [`runbooks/PANELIN-IA-OPS.md`](./runbooks/PANELIN-IA-OPS.md) refreshed.

**2026-07-22 (feat — Panelin Multi-Context Agent / PMCA):** SharedWorkspace + ContextGroup UI (Claude-style tabs, a11y tablist) in Panelin chat; `operatorContext.workspace` → prompt. New tools: `email_listar_hilos`, `email_leer_hilo`, `email_clasificar_mensaje`, `email_enviar` (HITL via Omni reply). Intent patterns for `email_enviar` + `wa_lead_to_admin`. SDD `docs/sdd/panelin-email-admin/` (TARGET + SDD + evidence). Tests: sharedWorkspace, tool count 55, OC workspace block. No Gmail DOM; send never autonomous.

**2026-07-22 (fix — local API outbox + pg hardiness):** `transportistaOutboxWorker` stops on missing `outbox_notifications` (42P01, one warn); held client gets `on("error")`; gap pools (ads/marketing/marketIntel) add `pool.on("error")`; `TRANSPORTISTA_OUTBOX_DISABLED` config. Stops 15s spam + unhandled pg Client crash in local `dev:full`.


**2026-07-22 (feat — Panelin agent B-06 circuit breaker):** Extracted `server/lib/providerCircuitBreaker.js` (N-failures cooldown, hard 400/401/403 long open, never drop providers — healthy-first reorder). `agentCore` re-exports + uses `orderChainByHealth`. Tests `providerCircuitBreaker.test.js` + existing `agentCoreFailover.test.js`. SDD-TARGET B-06 Done.


**2026-07-22 (feat — Panelin agent B-05 toolStats persist):** Durable telemetry in `public.agent_tool_calls` (auto-ensure schema); `recordToolCall` fire-and-forget insert when `DATABASE_URL` set; `GET /api/agent/tool-stats` uses `getToolStatsAsync` (`source: db|memory`). Migration `server/migrations/agent/001_agent_tool_calls.sql`. Tests `toolStatsPersist.test.js`. SDD-TARGET B-05 Done.


**2026-07-22 (feat — Panelin agent B-04 OpenAPI tools):** `GET /api/agent/tools/openapi` exports OpenAPI 3.1 generated from live `AGENT_TOOLS` (JSON/YAML); `server/lib/agentToolsOpenApi.js`; tests `agentToolsOpenApi.test.js`. SDD-TARGET B-04 Done.


**2026-07-22 (fix — Panelin Co-Work Wave 4 + skills):** Session gaps post–Wave 1–3: remap/drop `ACTION_JSON:aplicar_estado_calc`; `normalizeTipoAguas` + defaults; surface-intent prompts (Admin vs Gmail vs CRM); `listar_cotizaciones_recientes` `desde`/`hasta`; filter routine `infoNotes`; tools `email_panelsim_resumen` + `email_borrador_saliente` (draft-only). Skill [`.cursor/skills/panelin-cowork/SKILL.md`](../../.cursor/skills/panelin-cowork/SKILL.md), rule `panelin-cowork.mdc`, SKILL-INDEX + `bmc-panelin-chat` / `panelin-gym` updates.

**2026-07-22 (feat — Panelin Co-Work PR-H Document-PiP):** Shared [`src/utils/openDocumentPip.js`](../../src/utils/openDocumentPip.js); Live detach uses it; desk **Fijar arriba** portals UI into Document PiP; calculator chat **Fijar** → `openPanelinCoworkPinned` (PiP+iframe desk or popup fallback). Tests `tests/openDocumentPip.test.js`. SDD-PANELIN-COWORK PR-H Done (AC §20 #13).

**2026-07-22 (feat — Panelin Co-Work desk window PR-G):** Route `/panelin/cowork` minimal shell (`PanelinCoWorkPage`) with chat + CoWork toolbar; **Abrir en ventana** opens named popup `panelin-cowork` (default ~420×720, size remembered); `BroadcastChannel('bmc-panelin-cowork-v1')` syncs `calcState` parent→desk and `chatAction` desk→parent. Operator-facing (no longer dev-only). Tests `tests/openPanelinCoworkDesk.test.js`. SDD-PANELIN-COWORK PR-G Done.

**2026-07-22 (fix — CI apt hardening for easymidi on ubuntu-latest):** PR [#726](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/726) merged — `scripts/ci-install-alsa-dev.sh` strips flaky Google Chrome apt sources before `apt-get update` so Lint/Validate no longer fail on mirror hash mismatches when only `libasound2-dev` is needed. Wired in `ci.yml`, `cockpit-e2e-writes.yml`, `product-docs.yml`.

**2026-07-22 (ops — github-pendings clearance):** Merged active PRs [#713](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/713) PDF export fidelity, [#712](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/712) EstadoConsultasLive, [#720](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/720) SDD harness; Dependabot [#723](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/723) setup-node v7. Closed risky majors (#506 ESLint 10, #508 React 19, #551 dev-deps group, #678 react-resizable-panels v4) and 29 stale CONFLICTING PRs. Master prompt: `goal-prompt-github-pendings-clearance.md`.

**2026-07-22 (fix — Panelin Co-Work session analysis Wave 1–3):** Post-mortem sesión operador WA Web + Co-Work: informe [`reports/PANELIN-SESSION-ANALYSIS-2026-07-20.md`](./reports/PANELIN-SESSION-ANALYSIS-2026-07-20.md). **Wave 1:** prompts honestidad WA Web (JPEG only) + bloque aprendizaje Training KB; babeta desarrollo 16 cm + largo pieza ~3 m; `info` SSE → `infoNotes` (no historial); strip noise al request; Error 400 historial>60 accionable; `operatorContext.defaults` (lista venta, 1 agua, falta-info). **Wave 2:** tool agent `wa_lead_to_admin` → `POST /api/wolfboard/row-create` con `notas` (defaults + campos faltantes). **Wave 3:** SSE `provider_reset` + clear bubble/infoNotes on failover; `KEEP_RECENT` 6. SDD Co-Work non-goal WA Web automation. Tests: `chatHistoryNoise.test.js`, `coworkOperatorContext.test.js`, `wa_lead_to_admin` en `agentTools.test.js`.

- **2026-07-19** — full SDD implementation run closed: `docs/sdd/calculadora-bmc/` SCORECARD **94** pass; GAP P0=0; SoT wired AGENTS/Claude/SKILL-INDEX; A3 pre-deploy contracts hard-fail + `tests/preDeployContractsRatchet.test.js` in `test:api`; `gate:local:full` **GREEN**.

- **2026-07-19** — SDD as development reference: linked in `AGENTS.md` / `Claude.md` / `docs/team/harness/SKILL-INDEX.md`; A3 ratchet — `pre-deploy-check.sh` **fails** on contract validation when API health is up (soft-skip only if unreachable).

- **2026-07-19** — `gate:local:full` **GREEN**: `toolStats` AGENT_TOOLS 42→48; `FINANZAS_MODULE_PASSWORD` declared in `.github/required-cloud-run-secrets.txt`. SDD v0.2 SCORECARD **94**.

- **2026-07-19** — SDD kit full cycle: as-built `docs/sdd/calculadora-bmc/SDD.md` v0.2, audit SCORECARD **94** (pass≥90), GAP-PLAN P0=0, ARCHITECT-IMPROVEMENTS.md; evolution closed assistant table + day-0 bootstrap.

**2026-07-19 (docs — sdd-quality-auditor Calculadora Fletes):** Audit of [`SDD-CALCULADORA-FLETES.md`](./SDD-CALCULADORA-FLETES.md) → composite **72/100** (usable, fail ≥90). Artifacts: [`docs/sdd/calculadora-fletes/audit/`](../sdd/calculadora-fletes/audit/) (`AUDIT.md`, `SCORECARD.json`, `IDEAL-TARGET.md`, `GAP-PLAN.md`). P0: schema renumber + missing Deployment + FX BROU vs `dolarapi_uy` evidence drift. Doc-only; no code changes.

**2026-07-19 (ops — holistic health ABSOLUTE / sdd-kit):** Evidence snapshot across 12 areas — global readiness **82%**. Sensors: prod `/health`+`/capabilities` OK; `POST /calc/cotizar` OK (ISODEC_EPS 100mm → USD 3,046.77); `doppler … smoke:prod` **9/9** (IA gemini); `harness:score` **98.2**; `gate:local` **FAIL** (`toolStats` 42≠48); `test:api` **FAIL** (UNDECLARED `FINANZAS_MODULE_PASSWORD`); local :3001/:5173 down. Assistants `canales;ml;panelin` degraded→gemini (#610 OPEN). SDD as-built exists (`docs/sdd/calculadora-bmc/`, COMPAT PASS) — next `sdd-quality-auditor`, not reverse-engineer. Report: [`reports/PROJECT-HEALTH-SNAPSHOT-2026-07-19.md`](./reports/PROJECT-HEALTH-SNAPSHOT-2026-07-19.md). No deploy, no full team.

**2026-07-19 (arch — HCS as-built audit):** Evidence audit of Harness Control System (analysis-only). `claude -p` blocked on credits → executed in Cursor. Re-verify `harness:score:report` **98.2/100** (`generated_at` 2026-07-19T08:50:51Z), DoD 12/12; `test:fitness` + `test:catalog-goldens` green; `test:agent-golden` skipped (API :3001 down). agentGolden **19** (map inventory text still 15). Path drift: map S-A-05 `scripts/live-fix-verify.sh` → `~/.claude/skills/live-fix/scripts/`. Report: [`reports/HCS-AUDIT-2026-07-19.md`](./reports/HCS-AUDIT-2026-07-19.md). No deploys, no human-gate changes, no commit unless requested.

**2026-07-19 (arch — SDD as-built full system via sdd-reverse-engineer):** Bundle [`docs/sdd/calculadora-bmc/`](../sdd/calculadora-bmc/) — TARGET, SDD (12§), evidence, KB integrations, RECREATION-CHECKLIST 33/33, COMPAT PASS. Scope: SPA Vercel + API Cloud Run `panelin-calc` + calc/hub/AI/channels as observed (`/health` ok, Sheets+ML tokens). Next: `sdd-quality-auditor` on that SDD.

**2026-07-19 (verify — Grok build-max re-run):** Adapted goal → `goal-prompt-panelin-chat-agent-build-max-GROK.md`. Re-verified prod SPA (Hands-free/Whisper in calc chunk; no embedded Realtime Safari banner), API `/health` ok, `wakeWord` 19/19, goldens 16–19 on disk. Handoff: `docs/team/HANDOFF-PANELIN-CHAT-BUILD-MAX-2026-07-19-GROK.md`. P2 still deferred.

**2026-07-21 (feat — Panelin Workspace Fase 2 steps 5–13):** BMC-as-platform: Bearer via `POST /api/auth/dev-browser-login`; workspace routes require auth; `POST /api/workspace/change-requests/:id/approve|reject` (superadmin) → in-process `addTrainingEntry` + `bmc_kb_id`; minimal `POST /api/workspace/telemetry`. Companion UI (`~/Projects/panelin-workspace`): `bmcAuth`, HeaderBar login, HttpBmcClient SSE, ClassicChat → PATCH `sess-1`, ReviewPage API, CI Node 24, `npm run smoke:bmc`. **Does not modify** calc/agent/hub. Google OAuth + Vercel project = human leftover (ADR-006).

**2026-07-21 (docs — Panelin Workspace SDD quality sync ADR-008):** Aligned `~/Projects/panelin-workspace/docs/sdd/panelin-workspace/` to DEVELOPMENT-SPEC v2.1 (no Next BFF as Postgres SoT). Re-audit composite **96/100** pass; GAP-PLAN P0 drift closed. See `audit/SCORECARD.json`.

**2026-07-21 (docs — Elon deck Panelin Workspace):** Pitch deck for the agentic workspace product: [`presentations/Panelin-Workspace-Agentic-Elon.pptx`](./presentations/Panelin-Workspace-Agentic-Elon.pptx). Rebuild: `node scripts/build-panelin-workspace-elon-deck.mjs` (assets under `presentations/assets/`).

**2026-07-21 (feat — Panelin Workspace BMC-as-platform steps 1–4):** Additive schema `panelin_workspace` + `npm run workspace:migrate` (`workspace-package/migrations/`). New Express router `server/routes/workspace.js` (`GET /api/workspace/health|state`, CRUD projects/sessions/files). CORS default includes `http://localhost:3000`. **Does not modify** calc/agent/hub routes. Companion UI hydrate in `~/Projects/panelin-workspace`. Spec: `panelin-workspace/docs/sdd/panelin-workspace/DEVELOPMENT-SPEC.md` v2.1 ADR-008.

**2026-07-19 (prod — Panelin Hands-free build-max):** PR [#717](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/717) merged (`37045e0b`). Vercel **Deploy production** success. Prod SPA: no embedded “Safari…OpenAI Realtime” banner; Hands-free/Whisper markers present in calculator chunk. API `/health` ok. Smoke suggest-response 401 is pre-existing IA credentials (not this PR).

**2026-07-18 (ops — Mission Control Package A: hygiene + PR wave):** Sense/reconcile board; shipped orphan competitive docs (#708); stopped 39 zombie Claude jobs; closed 32+ stale draft PRs; rebased conflict PRs as #709–#715 (HCS blocks force-push — superseding branches). Human gates escalated: [`BLOCKED-ESCALATION-2026-07-18.md`](../../../.claude/mission-control/BLOCKED-ESCALATION-2026-07-18.md). Cleanup log: [`housekeeping/cleanup-2026-07-18.md`](./housekeeping/cleanup-2026-07-18.md). Metalog `feat/seed-completion` pushed.
**2026-07-18 (feat — Panelin chat agent build-to-max P0/P1):** Hands-free Safari gate (`isHandsFreeSupported`), wake-word `onend` exponential backoff (B-02), Firefox Whisper push-to-talk via `/api/agent/transcribe` (B-03), channel goldens 16–19 (B-07). SDD kit at [`docs/sdd/panelin-chat-agent/`](../sdd/panelin-chat-agent/); target backlog statuses in `SDD-TARGET.md` §11. P2 (OpenAPI/toolStats/circuit breaker) deferred. P0 complete only after Vercel prod UAT (no embedded Realtime Safari banner).

**2026-07-18 (strategy — competitive analysis + sales plays):** Full competitive analysis + 90d sales strategy executed from `goal-prompt-full-competitive-sales-strategy.md` (Claude Code blocked on credits → Cursor). Docs: [`COMPETITIVE-ANALYSIS-FULL-2026-07-18.md`](./COMPETITIVE-ANALYSIS-FULL-2026-07-18.md), [`COMPETITIVE-SALES-STRATEGY-2026-07-18.md`](./COMPETITIVE-SALES-STRATEGY-2026-07-18.md), [`battlecards/BATTLECARDS-TIER1.md`](./battlecards/BATTLECARDS-TIER1.md). North Star proposed = WA quotes calificadas; Meta stays ATC-only until Purchase fires. Live checks: MontFrío, BECAM, TDA, Panel Sandwich Group, BMC Shopify OK; Bromyros/Eco/Casa del Panel fetch gaps. No price/ads mutations.

**2026-07-18 (docs — competitive goal-prompt archive):** PR [#708](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/708) — goal prompts under `docs/team/goal-prompts/` (687 solver, 684 rebase, competitive sales).

**2026-07-14 (fix — CRM send-approved injectable deps + status mapping tests):** `createBmcDashboardRouter(config, deps)` ahora acepta overrides inyectables (`fetch`, `getCrmSheetsWrite`, `sendWhatsAppText`, `sendEmailReply`, `mirrorMlSendApprovedToOmni`, `getEmailIngestPool`, `getIngestByRow`) para testear y endurecer `POST /api/crm/cockpit/send-approved`: 502 cuando falla el envío WA/ML, 503 cuando no puede stamp Sheets. Tests offline `tests/crmCockpitSendApprovedStatus.test.js` (3/3).

**2026-07-18 (feat — Finanzas closeout: cash-flow tests, taxonomy rules seed, sin_clasificar fix):** `tests/banco-cash-flow.test.js` en `test:api`; `createBancoRouter` acepta `deps.pool` para tests; `sin_clasificar` y `POST /rules/apply` alineados a `categoria IS NULL` (SDD). Script [`scripts/seed-banco-rules-from-taxonomy.mjs`](../../scripts/seed-banco-rules-from-taxonomy.mjs) porta patrones metalog → `banco_rules`. Docs: [`SDD-finanzas-hub.md`](./SDD-finanzas-hub.md) Appendix B + [`fiscal/BANCO-LEDGER.md`](./fiscal/BANCO-LEDGER.md) actualizado a `/hub/finanzas`.

**2026-07-18 (fix — ML identity JWT refresh single-flight):** Follow-up a Codex P1 en #704. `refreshIdentityJwt` y `BmcAuthProvider.refreshAccess` coalescen callers concurrentes en un solo `POST /api/auth/refresh`; `applyAuth` actualiza el JWT getter de forma síncrona para retries de `mlFetch` sin esperar React effects. Evita `token_reuse_detected` al abrir ML Manager tras TTL 15m (Overview dispara varias queries en paralelo). Tests en `operatorApiClient.test.js`.

**2026-07-18 (feat — MLOMS P0: playbook queue + deploy fix):** `GET /api/ml/playbooks` (read-only desde marketIntel ml_pulse + product_matrix) + panel **Playbooks · intel → ML** en `OverviewTab`. Fix boot: `mlOptimize.js` import `{ config }` (bloqueaba deploy Cloud Run post-#701). Test `mlPlaybooks.test.js`.

**2026-07-18 (feat — MLOMS P0: Listing Quality Agent + audit UI):** `POST /api/ml/optimize/listing` + [`server/lib/mlListingQuality.js`](../../server/lib/mlListingQuality.js) (agentCore, JSON rubric title/images/attrs/description). ML Manager `ListingsTab` → botón **Auditar IA**, panel scores/issues, **Aplicar sugerencias al formulario** (human gate: guardar manual con confirmación). Test `mlListingQualityParse.test.js`. SDD: [`ml-optimization/SDD-ML-OPTIMIZATION-SYSTEM.md`](./ml-optimization/SDD-ML-OPTIMIZATION-SYSTEM.md) P0.

**2026-07-18 (arch — MLOMS SDD: MercadoLibre optimization & management system):** Design-only SDD [`ml-optimization/SDD-ML-OPTIMIZATION-SYSTEM.md`](./ml-optimization/SDD-ML-OPTIMIZATION-SYSTEM.md) from MATPROMT [`matprompt/MATPROMT-RUN-2026-07-18-ml-sdd.md`](./matprompt/MATPROMT-RUN-2026-07-18-ml-sdd.md). Covers 8 capability domains (ads, campaigns, listings, competition, strategy, discovery, quality, expansion); C4 + 3 sequences; 5 ADRs; P0–P3 roadmap. Grounded on live ML Manager (4 tabs), `/ml/*` routes, marketIntel, agentCore — **no Product Ads backend yet** (`useMlConnector` omits ads routes). Human-in-the-loop mandatory for all ML writes. Handoff: Integrations, Networks, Design, Security, Calc.

**2026-07-18 (arch — GTIS SDD: training & improvement for Grok terminal):** Design-only SDD [`SDD-GROK-TERMINAL-TRAINING-IMPROVEMENT.md`](./SDD-GROK-TERMINAL-TRAINING-IMPROVEMENT.md) + registry [`GOAL-PROMPT-REGISTRY.md`](./GOAL-PROMPT-REGISTRY.md). Defines dual-lane (audit measure → human gate → ratchet), Grok-first executor fallback, evidence contracts, Pilot 0 = freight Dropbox vs `fleteEngine` (`goal-prompt-audit-dropbox-freight-vs-engine.md` **ready, not executed** — blocked on Dropbox MCP auth). No tariff/code/audit run in this pass. Extends HCS flywheel + the-evolutor; does not replace Panelin Training KB.

**2026-07-18 (prod — Live Assist / Co-Work online):** PR [#697](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/697) squash-merged → `main` `67f10557`. **Deployed:** Cloud Run `panelin-calc-00811-x8k` (image tag `…panelin-calc:67f10557…`, 100% traffic) + Vercel production SPA. **Secrets:** `GEMINI_API_KEY` mounted from GSM (vision path; Claude credits still empty → chain falls to Gemini). **Smoke prod:** `npm run smoke:prod` OK (health, MATRIZ CSV, suggest-response via gemini). **Agent multimodal:** `POST /api/agent/chat` with JPEG attachment → SSE `cowork_ack` `framesAccepted:1` → Gemini text reply + `done` (no hang). Text-only also OK (`PONG` via gemini). **SPA UI verified:** Panelin chat shows `Activar Co-Work (visión)`, `📷 Capturar pestaña`, `Live assist`. **UAT remaining (human):** real `getDisplayMedia` over Google Sheets tab + Live ON attach-on-send in operator browser (permission-gated; agent cannot complete screen-share picker). **Deferred (SDD §20 #11–13):** desk window / Document-PiP (PR-G/H). SDD: [`SDD-PANELIN-COWORK.md`](./SDD-PANELIN-COWORK.md).

**2026-07-17 (arch — HCS Expert Harness Engineering go-live):** Dual-layer **Harness Control System** for coding agents + product AI. SDD [`SDD-HARNESS-ENGINEERING.md`](./SDD-HARNESS-ENGINEERING.md); map/score/PEV under [`harness/`](./harness/). Scripts: `harness:score`, `pre-release`, `test:fitness`, `test:catalog-goldens`, `eval:agent`. Hooks PreToolUse/PostToolUse; `costTelemetry` wired in agentCore/aiCompletion; agentGolden expanded to 15 cases; catalog goldens rebased to live `constants.js` SoT. Scorecard **98.2/100**, DoD D1–D12 green. Intentional residual: human gates (OAuth, finanzas unlock, `user_confirmed`) remain. Goal prompt: `goal-prompt-harness-engineering-100.md`. Handoff: [`HANDOFF-HCS-2026-07-17.md`](./HANDOFF-HCS-2026-07-17.md).

**2026-07-17 (feat — Live Assist 100% contract):** Pure core `src/hooks/liveAssistCore.js` (interval 4s, `LiveFrameBuffer`, attach-on-send only, source `live_assist`, shared buffer D2). `useScreenCoWork` interval never POSTs; Panelin `handleSend` + `buildAgentChatRequestBody` attach buffer + `operatorContext.liveAssist`. Admin shares buffer via `sharedLiveFrameBuffer`. Tests `tests/liveAssist.test.js` (47) + `coworkFrames`. ADR-003/D3 verified.

**2026-07-17 (arch — SDD Co-Work v1.1: ventana independiente / desk):** [`SDD-PANELIN-COWORK.md`](./SDD-PANELIN-COWORK.md) v1.1 agrega **D7 + §10.4**: Panelin como ventana independiente (popup multi-monitor sobre Sheets, Document-PiP “Fijar arriba”, ruta mínima `/panelin/cowork`, BroadcastChannel sync). Reutiliza `openDetachedChatWindow` y helpers PiP de Panelin Live / TraKtiMe. PRs planificados PR-G/H. ADR-007.

**2026-07-17 (feat — Panelin Co-Work: pantalla + Sheets en tiempo real):** SDD completo [`SDD-PANELIN-COWORK.md`](./SDD-PANELIN-COWORK.md). Hybrid MVP: capturas de pestaña (one-shot + Live assist buffer local ~4s) + tools `sheets_*` allowlisted (Admin/CRM) + multimodal en `POST /api/agent/chat`. Principio: visión = contexto, Sheets API = verdad, humano confirma escrituras. UI: `CoWorkToolbar` en Panelin flotante y `/hub/admin-ingreso`. Backend: `server/lib/coworkFrames.js`, `coworkSheets.js`, tools en `agentTools.js`, prompt Co-Work en `chatPrompts.js`. Tests `tests/coworkFrames.test.js`. Branch `feat/panelin-cowork-screen-sheets`.

**2026-07-17 (feat — Cotizar flete en wizard):** SDD [`SDD-CALCULADORA-FLETES.md`](./SDD-CALCULADORA-FLETES.md) + implementación: `TARIFAS_LOGISTICAS` en [`src/data/constants.js`](../../src/data/constants.js), empaque [`src/utils/logistica/cargoPacking.js`](../../src/utils/logistica/cargoPacking.js), engine [`src/utils/fleteEngine.js`](../../src/utils/fleteEngine.js) + FX [`src/utils/brouFx.js`](../../src/utils/brouFx.js), UI paso Flete **Cotizar flete** ([`FleteCotizarPanel.jsx`](../../src/components/FleteCotizarPanel.jsx)). Tests `tests/fleteEngine.test.js` en `test:core`. PDF sigue solo «Flete — USD X».

**2026-07-17 (feat — Finanzas module password gate):** Segunda capa de acceso al hub **Finanzas** además de OAuth + grant `banco`. Backend: [`server/lib/finanzasUnlock.js`](../../server/lib/finanzasUnlock.js), rutas `GET/POST /api/banco/unlock-status|unlock|lock`, columna `identity.sessions.finanzas_unlocked_until` (migración `20260717000001`), middleware `finLocked` en rutas de datos `/api/banco/*`. Frontend: [`FinanzasUnlockGate.jsx`](../../src/components/hub/finanzas/FinanzasUnlockGate.jsx) en `App.jsx`; re-lock si API devuelve `finanzas_locked` (TTL 12h; bypass local dev + superadmin). Secreto: `FINANZAS_MODULE_PASSWORD` en Doppler — nunca en git. SDD v1.0: [`SDD-finanzas-hub.md`](./SDD-finanzas-hub.md). Tests: `tests/finanzas-unlock.test.js`. **PROD (confirmado via PR #694 → `main`):** Doppler `bmc-backend/prd` + GSM secret montado en Cloud Run (`FINANZAS_UNLOCK_TTL_HOURS=12`); migración aplicada; API + Vercel frontend deployed. UAT manual restante: login con grant `banco` en [`/hub/finanzas`](https://calculadora-bmc.vercel.app/hub/finanzas) → unlock → Banco/Cash Flow.

**2026-07-16 (ops — WA improvement batch → Cloud Run prod KB):** `panelin:train:import` contra `https://panelin-calc-q74zutv7dq-uc.a.run.app` — **88 ok / 0 fail** (`import-improvement.json`, process-first, 0 U$S históricos). Verificado: `GET /api/agent/training-kb/match?q=Quiero cotizar un techo de 11x7` → match seed; list ~88 entradas WA. Persistencia GCS Training KB en Cloud Run. Report: `~/whatsapp-export/review/REPORT-2026-07-16-improvement.md`.

**2026-07-14 (feat — Hub Wolfboard: EstadoConsultasLive):** `/hub` muestra `EstadoConsultasLive` debajo de `OperatorOverview` — panel de consultas/canales en vivo (componente desde `feat/hub-operator-control-plane` @ a8984875; wiring mínimo en `BmcWolfboardHub.jsx`; sin caja DEBUG ni bloques de copy de investigación).

**2026-07-14 (docs — inbound conversational OS research pack):** Publicados en repo (sin código) el resumen de investigación inbound Jul-2026, `INBOUND_CONVERSATIONAL_OS_LOOP_STATE`, handoff 2026-07-06, artefacto `PANELIN-VOICE-XAI-AGENT.json` y runbook operativo de casillas email — extraídos de `feat/hub-operator-control-plane` para PR docs aislado.
**2026-07-14 (fix — client PDF export fidelity for preferred simple):** html2pdf client fallback now normalizes template HTML (strip `@page`, inject print overrides for `.page` padding/shadows/margins) so raster output matches the designed render. `simple.js` CSS uses `@page{margin:0}` + unconditional `.page` padding `7mm 8mm`. Panel qty/length and no-logo already in place. Tests updated in `tests/pdf-pipeline.test.mjs`.

**2026-07-13 (feat — instrumentación de conversión en la calculadora pública: beacon + Meta Pixel):** Diagnóstico urgente: Meta Ads reporta **$87,830 de gasto en 30 días con 0 conversiones en las 4 campañas**, pese a CTR/CPC sanos (~112k clicks) — la app no emitía absolutamente ninguna señal de tracking (ni pixel, ni beacon propio), y la acción de mayor volumen ("Enviar por WhatsApp" en `handleCopyWA`) era 100% client-side, invisible incluso internamente. Fix en dos partes: **(1)** nuevo endpoint público sin auth `POST /api/public/lead-event` ([`server/routes/publicLeadEvent.js`](../../server/routes/publicLeadEvent.js)) que escribe en `identity.user_activity_log` con `actor_user_id=null` reusando `logActivity()` — restringido a `PUBLIC_EMITTABLE` (`quote.send.whatsapp`, `quote.complete`; ya existían en `ACTION_TAXONOMY`/Historial pero nunca se emitían). **(2)** Meta Pixel base en `index.html` (`%VITE_META_PIXEL_ID%`, no-op seguro si no está seteado) + captura de `?utm_*` en `src/main.jsx` → `sessionStorage`. Ambos disparan juntos desde [`src/utils/leadTracking.js`](../../src/utils/leadTracking.js) en `handleCopyWA` y `handleConfirmQuote` (`PanelinCalculadoraV3_backup.jsx`), fire-and-forget, nunca bloquean el flujo real. Tests offline nuevos `tests/public-lead-event.test.js` (5/5, agregado a `test:api`). **Pendiente (no incluido en este PR, requiere acción humana):** crear el Pixel real en Meta Events Manager y setear `VITE_META_PIXEL_ID` en Vercel; triage de las 4 campañas (pausar "Stock de prefiles" $42k/0 conv.) vía Marketing API — propuesto, no ejecutado sin aprobación explícita por ser gasto real.

**2026-07-11 (feat — `/hub/canales`: las 3 pestañas stub pasan a funcionales, reusando código existente):** Se reemplazan los placeholders de **ML Manager**, **WA Inbox** y **Contactos Unificados** por funcionalidad real, sin sistemas nuevos. **(1) ML Manager** (el mock "modelos ML / accuracy" era engañoso — acá **ML = MercadoLibre**): ahora renderiza el dashboard de vendedor real reusando [`MlManagerModule`](../../src/components/hub/ml/MlManagerModule.jsx) (Resumen/Publicaciones/Preguntas/Pedidos vía `useMlConnector` + rutas `/ml/*`), con nuevo prop `embedded` que quita el chrome/skin-lock de la ruta standalone `/hub/ml-manager` (intacta). **(2) WA Inbox**: es el inbox Omni acotado a WhatsApp — reusa [`OmniInboxPanel`](../../src/components/hub/canales/panels/OmniInboxPanel.jsx) con nuevo prop `lockedChannel="wa"` (usa el grant `canales` y el token ya pasado, sin gate de auth anidado; el cockpit WA completo sigue en `/hub/wa`). **(3) Contactos Unificados**: tabla buscable sobre nuevo endpoint `GET /api/omni/contacts` (espejo del de duplicados: `canales:read`, degrada 42703/42P01, team-isolated para no-admin — scope por conversaciones visibles del equipo) + hook `useOmniContacts`, reusando formatters de `omniFormat.js`. Sin migraciones (schema `001_core.sql` ya alcanza). Verificado: `lint` (solo warnings preexistentes), `test`+`test:api` (falla preexistente ajena: agentCore bogus-key), `BMC_DISK_PRECHECK_SKIP=1 build`. Datos en vivo requieren entorno con Postgres omni + OAuth ML; los paneles degradan a estados vacíos limpios. Branch `claude/calculadora-bmc-channels-5hugx6`.

**2026-07-11 (fix — Visor 3D respeta techo plano/0° · #667, follow-up de #663):** El visor 3D mostraba un techo a 15° cuando `pendiente = 0` (default vendedor `TECHO_INITIAL_VENDEDOR.pendiente = 0` o cotización de techo plano), porque `RoofPanelRealisticScene` hace `Number(pendiente) || 15` (0 es falsy → 15°). Como esa escena es archivo compartido/no-touch y su `|| 15` derrota cualquier fallback nullish aguas arriba, el fix vive en el wrapper `Roof3DSection.jsx`: `scenePendiente = Number(pendiente) > 0 ? pendiente : 0.5` — sentinel chico que sobrevive el `|| 15` y cae en el piso `~2.86°` de la escena (`Math.max(0.05, …)`), lo más plano representable. Sólo cambia el caso `0`; cualquier pendiente real pasa igual. Detectado por el bot Codex en #663.

**2026-07-10 (fix — VERCEL_ENV expuesto al bundle: gate design preview funciona como documentado):** `vite.config.js` ahora define `import.meta.env.VERCEL_ENV` (Vite sólo expone `VITE_*`), que era código muerto en `designPreviewMode.js` desde Fase 1: los previews de Vercel NO auto-activaban el design preview y el hard-block de `?designPreview=1` en producción NUNCA aplicaba (el param activaba el preview incluso en prod, contra el contrato del archivo). Ahora: branch preview = gate auto-on sin query param; producción = param bloqueado. Local/CI sin cambios (`VERCEL_ENV` vacío → path de query param intacto, verificado re-corriendo `test:playwright:roof3d-chunk` 11/11 + probe simulando `VERCEL_ENV=preview|production`).

**2026-07-10 (feat — Visor 3D · Paneles para cubierta, gated design preview):** Nueva sección colapsable (colapsada por defecto) inmediatamente arriba del visor 2D (`QuoteVisualVisor`) en la calculadora, sólo visible con `isDesignPreviewEnabled()` (`?designPreview=1` / Vercel Preview / `VITE_BMC_DESIGN_PREVIEW=1`). Wrapper nuevo `src/components/roof3d/Roof3DSection.jsx` que reusa `RoofPanelRealisticScene` vía `React.lazy` (mismo chunk lazy existente; cero bytes 3D nuevos con gate apagado), lectura pura del estado del techo (one-way), probe WebGL con fallback amable. `ENABLE_ROOF_3D_VISOR` (flujo portal legacy) queda `false` e intacto. Verificación de aislamiento de chunk: `scripts/playwright-roof3d-lazy-chunk.mjs` (`npm run test:playwright:roof3d-chunk`; build + `vite preview` con service worker bloqueado por el precache PWA; caso A sin gate = 0 requests roof3d, caso B expandir = fetch del chunk + montaje, caso C dims 6×5 = `<canvas>`).

**2026-07-10 (security — middleware CSRF de verificación de procedencia, CWE-352):** Cierra el alert CodeQL `js/missing-token-validation` (#14) que apuntaba a `cookieParser()` global en `server/index.js`. Contexto: los JWT de acceso ya viajan solo por `Authorization: Bearer` (inmunes a CSRF) y la cookie de refresh es httpOnly + SameSite=Strict confinada a `/auth/refresh` y `/auth/logout` — la arquitectura ya era segura, pero faltaba la capa explícita. Nuevo [`server/middleware/csrfProtection.js`](../../server/middleware/csrfProtection.js) montado tras `cookieParser()`: para métodos inseguros CON cookies exige señal de mismo origen (`Sec-Fetch-Site`, `Origin`/`Referer` ∈ `config.corsOrigins` o chrome-extension, o header custom que fuerza preflight CORS) **y double-submit cookie**: se emite `bmc_csrf` (random, legible) que el cliente puede reflejar en `X-CSRF-Token` — si cookie y header están presentes se exige igualdad (mismatch → 403). Requests sin cookies (Bearer puro, webhooks HMAC, curl) pasan intactos, igual que clientes no-browser. Tests [`tests/csrf-protection.test.js`](../../tests/csrf-protection.test.js) (18 asserts) en `test:api`; suites de auth existentes verdes. Branch `claude/goal-prompt-metalog-bank-fm8e12` (PR #662).

**2026-07-10 (nuevo módulo — Banco: libro de movimientos bancarios METALOG):** Implementa la columna C del modelo de conciliación DGI ↔ facturación ↔ banco ([`DGI-CLAUDE-INGESTA.md`](./fiscal/DGI-CLAUDE-INGESTA.md)). Import de extractos **BROU** (XLS «Saldos y Movimientos» de e-BROU — validado contra un export real: 464 movimientos, 0 errores — y CSV legado con reparación de decimales partidos), dedup idempotente por hash (re-import y rangos solapados no duplican; movimientos idénticos legítimos se conservan), auto-detección/alta de cuenta por número + moneda del preámbulo, clasificación `categoria` + `entidad` (bmc/expreso_este/personal/mixta, taxonomía de la planilla Proveedores) con reglas automáticas por substring (`banco_rules`, sin seeds — no inventar datos de negocio), filtros + sumas y resumen mensual (`/api/banco/summary`). Stack: `banco-package/migrations/` + `npm run banco:migrate` (patrón traktime), [`server/lib/bancoStatementParser.js`](../../server/lib/bancoStatementParser.js), [`server/routes/banco.js`](../../server/routes/banco.js) (503 sin DB, nunca 500), slug `banco` en `ALL_MODULES`, UI `/hub/banco` ([`BancoLedgerModule.jsx`](../../src/components/hub/banco/BancoLedgerModule.jsx), grant `banco:read`). Tests `tests/banco-parser.test.js` (46) + `tests/banco-routes.test.js` (7) en `test:api`. PDFs de e-BROU fuera de alcance (usar export XLS del mismo rango). Doc canónica: [`fiscal/BANCO-LEDGER.md`](./fiscal/BANCO-LEDGER.md). **Pendiente operador:** correr `npm run banco:migrate` contra la DB de prod antes del primer import. Branch `claude/goal-prompt-metalog-bank-fm8e12`.

**2026-07-10 (docs/skill — Shopify Complete Connector):** Nueva skill de proyecto [`.cursor/skills/shopify-complete-connector/`](../../.cursor/skills/shopify-complete-connector/SKILL.md) para desarrollar desde el repo el conector **completo** de la tienda Shopify (diseño/theme, funcionalidad Admin/Storefront, catálogo/PIM, webhooks, extensiones, Functions, opciones Partner). Incluye `reference.md`, `design-and-storefront.md`, `examples.md`, agente Cursor y rule `.cursor/rules/shopify-complete-connector.mdc`. No reemplaza `shopify-integration-v4` (preguntas/cotizaciones → Sheet); v4 ahora enlaza al conector completo. Código de runtime sin cambios.

**2026-07-08 (security — OpenRouter fallback requiere opt-in explícito):** Follow-up a PR #652. El nuevo provider `openrouter` ya no entra en `getProviderChain()` sólo por existir `OPENROUTER_API_KEY`; requiere además `OPENROUTER_FALLBACK_ENABLED=1`, porque ese fallback envía prompts y contexto de clientes a un boundary externo nuevo (OpenRouter/modelos abiertos). `.env.example` documenta el doble gate, `deploy-calc-api.yml` propaga el repo var opcional y `tests/aiProviderConfigOpenRouter.test.js` quedó agregado a `test:agent` para cubrir key-presente/flag-apagado vs flag-encendido.

**2026-07-08 (fix — cierre de escritura Drive anónima en cotizaciones):** Follow-up crítico a PR #650: el cambio a OAuth de usuario hacía efectivo el archivo compartido `DRIVE_QUOTE_FOLDER_ID`, pero `POST /api/quotes/drive-archive` seguía público y podía crear/actualizar bundles PDF+JSON en Drive con el refresh token compartido. La ruta ahora exige `requireServiceOrUser({ authOnly: true })` (token de servicio o JWT activo), la calculadora adjunta el JWT BMC cuando el usuario está autenticado, y `/calc/cotizar/pdf` mantiene el cálculo/render público pero solo intenta Drive con usuario/servicio autenticado. Guard nuevo [`tests/quoteDriveArchiveAuth.test.js`](../../tests/quoteDriveArchiveAuth.test.js) y `calcLoopbackClient.test.js` confirma Bearer interno en loopback sin reenviarlo al fallback público.

**2026-07-08 (fix — loopbacks internos ML conservan auth tras endurecer mutadores):** Follow-up crítico a PR #648 (`POST /ml/questions/:id/answer`, `PATCH /ml/items/:id`, `POST /ml/items/:id/description` ahora requieren `requireServiceOrUser({authOnly:true})`). El cierre del write-hole anónimo era correcto, pero dos caminos legítimos server-side (`send-approved` desde CRM_Operativo y Omni `sendMlReply`) seguían llamando el endpoint por HTTP sin credenciales, por lo que las respuestas aprobadas por operador a MercadoLibre habrían fallado con 401/502. Nuevo helper `buildMlWriteHeaders()` adjunta `x-api-key` con `API_AUTH_TOKEN` a esos loopbacks internos; test `mlInternalAuthHeaders.test.js` agregado a `test:core`.

**2026-07-08 (feat — Consola gestionable de Asistentes IA: ML fix + panel honesto + toggles en runtime · PRs #638/#639/#641/#642):** Cuatro PRs que convierten `/hub/admin/assistants` de un panel read-only optimista en una consola gestionable, y arreglan el botón "Generar con IA" de ML. **(A #638)** El botón "Generar con IA" en `/hub/ml-manager` daba **"IA no disponible"**: era **auth, no IA** — `mlFetch` mandaba solo la cookie y `POST /api/crm/suggest-response` (endurecido en #567) exige `Authorization: Bearer` (la cookie `bmc_sess` es refresh-only). `mlFetch` ahora adjunta el token de operador (`ensureOperatorToken`) como Bearer + `x-api-key`; las rutas abiertas `/ml/*` ignoran el header extra. **(B #639)** El badge "LIVE vía claude" era optimista (presencia de key, no respuesta real): el seam sirve vía **gemini** porque Claude está sin créditos (#610). `agentCore.recordProviderFailure` ahora retiene `lastError {status,type,detail,model,at}` por proveedor; `assistantHealth` marca `degraded` con el motivo cuando el primario tiene error duro (400/401/403) o cooldown y resuelve el `activeProvider` real; el panel muestra "proveedores con fallas reales" con la razón exacta. Verificado en prod: `claude lastError 400 "credit balance too low"`, `ml → degraded/gemini`. **(C #641)** Override de enable/disable en **runtime sin redeploy**, reusando el store `waConfig` (`wa_settings`, namespace nuevo `assistants: z.record(z.boolean())`) — Postgres, cross-instancia vía LISTEN/NOTIFY, audit, zod, **sin migración**. `isAssistantEnabled` consulta `getSetting('assistants')[key]` (sync, cached) que gana sobre `ASSISTANTS_ACTIVE`; `seam` queda always-on. Endpoint admin `POST /api/assistants/:key/toggle` (guard `requireServiceOrUser({role:'admin'})`). Verificado en prod: toggle `wa` on→`suggestions/run` deja de dar 503; off→vuelve a 503, sin redeploy. **(D #642)** Switches Encender/Apagar por asistente en el panel + hint owner-gated para fallas de credencial/billing. **Follow-up de seguridad #648:** los mutadores ML (`answer`, `item`, `description`) quedaron autenticados con `requireServiceOrUser({authOnly:true})`; ver entrada superior por el ajuste posterior de loopbacks internos. Sigue pendiente #610 (créditos Anthropic + rotar GROK_API_KEY) como bloqueante del payoff del caching y de que el seam vuelva a servir vía Claude.

**2026-07-08 (release — OMNI FULL BUILD mergeado a main + deploy verificado · epic #623):** Los 5 PRs del programa (spec #622, gap2 ML webhook #631, gap3 IG/FB #630, gap4 secuencias #633, gap5 kanban #632) fueron mergeados en serie (con resolución de conflictos vs main: entradas de PROJECT-STATE + unión de `test:core`), CI de main verde en `2792cf96`, deploy Cloud Run OK y `smoke:prod` 9/9 (suggest-response IA vía gemini). Vercel SPA 200. Issues #625–#628 cerrados; epic #623 abierto solo por #624 (Gap 1 email, human-gated H1–H4) + cm-0 Meta OAuth para activar flags IG/FB. Handoff: [`HANDOFF-OMNI-FULL-BUILD-MERGE-2026-07-08.md`](./HANDOFF-OMNI-FULL-BUILD-MERGE-2026-07-08.md).

**2026-07-08 (feat — Gap 5 OMNI Pipeline Kanban en Hub Canales):** `/hub/canales` suma pestaña **Pipeline** sobre `omni_deals`: 6 columnas espejadas desde `DEAL_STAGES`, cards con contacto/canal/monto/edad y drag&drop nativo con rollback optimista. Backend agrega `PATCH /api/omni/deals/:id/stage` con `canales:write`, validación `canTransition()` (409 con `from/to`) y sync CRM vía `syncDealToCrm`; `GET /api/omni/deals` reutiliza `listDeals`. Verificado con `node tests/omniDealsStage.test.js`, `node tests/omniDealsStageEndpoint.test.js`, `npm run lint` (solo warnings preexistentes) y `BMC_DISK_PRECHECK_SKIP=1 npm run build`.

**2026-07-08 (feat — Gap 4 secuencias temporales HITL default OFF):** Implementado `sequenceWorker` para reglas `conversation.no_reply` y `followup.due` sobre `omni_automation_rules`, gobernado por `OMNI_SEQUENCES_ENABLED` (default OFF) y sin auto-send: la acción `ai_draft_followup` solo encola sugerencias `suggest` pendientes de aprobación en cockpit. Idempotencia por `seq:<rule_id>:<conversation_id>:<bucket>` vía `omni_automation_runs`; evita duplicados si ya hay sugerencia/job pendiente para la misma regla. En WA fuera de ventana 24h marca metadata `requires_template:true` para que el approve UI degrade a tarea/plantilla humana. Migración `017_sequence_triggers.sql`; tests offline `tests/omniSequenceWorker.test.js`.

**2026-07-08 (feat — Gap 3 IG DM + FB Messenger behind flags):** Implementado soporte dormant para Instagram DM y Facebook Messenger en Omni: flags `OMNI_IG_ENABLED` / `OMNI_FB_ENABLED` default OFF, tokens `IG_PAGE_TOKEN` / `FB_PAGE_TOKEN`, webhooks `/webhooks/instagram` y `/webhooks/messenger` con fast-ack mientras estén apagados y firma Meta al activar, adapters `igWebhook`/`messengerWebhook` a `OmniInboundEvent` (`channel: "ig"|"fb"`), outbound Graph `/me/messages` con ventana 24h (`RESPONSE`) y fallback `HUMAN_AGENT` tag fuera de ventana. Agregada migración omni `017_ig_fb_channels.sql` para permitir canales cortos. Pendiente gate humano **cm-0**: conectar app Meta, permisos `instagram_manage_messages` / `pages_messaging`, Page tokens y app review antes de prender flags en prod.

**2026-07-08 (feat — Gap 2 Omni: webhook ML → inbox):** Webhook MercadoLibre ahora comparte un servicio chico de buffering + pipeline, transforma notificaciones `questions`/`messages` (recurso ya fetchado) a `OmniInboundEvent` vía `mlWebhook.js` y persiste en Omni detrás de `OMNI_ML_SHADOW_WRITE` sin cambiar defaults. Las preguntas siguen entrando al flujo ML→CRM/auto-answer existente con guard idempotente por recurso para reentregas. Tests offline `mlWebhookOmni.test.js` agregado a `test:core`.

**2026-07-08 (docs — OMNI FULL BUILD: spec + arquitectura + benchmark del programa de paridad ≥8.6):** Nuevo [`docs/team/features/OMNI-FULL-BUILD-SPEC.md`](./features/OMNI-FULL-BUILD-SPEC.md). Consolida la investigación de mercado 2026-07-08 (40+ herramientas de lead management AI omnicanal; top: Zenvia 8.2, Botmaker 7.9, Kommo 7.2) y la auditoría de código propia: **BMC Omni/Panelin hoy scorea ~7.3** con moats que ningún vendor tiene (ML auto-answer fullAuto, cotización calculadora→PDF→WA, failover multi-LLM, RAG propio, data ownership). Decisión: **BUILD, no buy** — cerrar 5 gaps para proyectar ~8.6: (1) email saliente [XS, human-gated H1–H4 del runbook de casillas], (2) webhook ML → inbox omni [S, resuelve TODO de `routes/webhooks.js`], (3) IG DM + FB Messenger [M, behind flags hasta gate cm-0], (4) secuencias de follow-up con triggers temporales sobre `omni_automation_rules` [M, HITL siempre en v1], (5) kanban UI sobre `omni_deals`/`stageMachine` [S–M]. El spec incluye criterios de aceptación por gap, human gates aislados y plan de PRs <500 LOC. Ejecución trackeada como epic + 5 issues en GitHub «BMC Dev».

**2026-07-08 (feat — Consola gestionable de Asistentes IA: ML fix + panel honesto + toggles en runtime · PRs #638/#639/#641/#642):** Cuatro PRs que convierten `/hub/admin/assistants` de un panel read-only optimista en una consola gestionable, y arreglan el botón "Generar con IA" de ML. **(A #638)** El botón "Generar con IA" en `/hub/ml-manager` daba **"IA no disponible"**: era **auth, no IA** — `mlFetch` mandaba solo la cookie y `POST /api/crm/suggest-response` (endurecido en #567) exige `Authorization: Bearer` (la cookie `bmc_sess` es refresh-only). `mlFetch` ahora adjunta el token de operador (`ensureOperatorToken`) como Bearer + `x-api-key`; las rutas abiertas `/ml/*` ignoran el header extra. **(B #639)** El badge "LIVE vía claude" era optimista (presencia de key, no respuesta real): el seam sirve vía **gemini** porque Claude está sin créditos (#610). `agentCore.recordProviderFailure` ahora retiene `lastError {status,type,detail,model,at}` por proveedor; `assistantHealth` marca `degraded` con el motivo cuando el primario tiene error duro (400/401/403) o cooldown y resuelve el `activeProvider` real; el panel muestra "proveedores con fallas reales" con la razón exacta. Verificado en prod: `claude lastError 400 "credit balance too low"`, `ml → degraded/gemini`. **(C #641)** Override de enable/disable en **runtime sin redeploy**, reusando el store `waConfig` (`wa_settings`, namespace nuevo `assistants: z.record(z.boolean())`) — Postgres, cross-instancia vía LISTEN/NOTIFY, audit, zod, **sin migración**. `isAssistantEnabled` consulta `getSetting('assistants')[key]` (sync, cached) que gana sobre `ASSISTANTS_ACTIVE`; `seam` queda always-on. Endpoint admin `POST /api/assistants/:key/toggle` (guard `requireServiceOrUser({role:'admin'})`). Verificado en prod: toggle `wa` on→`suggestions/run` deja de dar 503; off→vuelve a 503, sin redeploy. **(D #642)** Switches Encender/Apagar por asistente en el panel + hint owner-gated para fallas de credencial/billing. **Follow-up de seguridad (separado):** `POST /ml/questions/:id/answer` (publica respuesta real al cliente en ML) está **sin autenticar** — handler inline en `server/index.js` sin middleware. Sigue pendiente #610 (créditos Anthropic + rotar GROK_API_KEY) como bloqueante del payoff del caching y de que el seam vuelva a servir vía Claude.

**2026-07-08 (feat — PDF real para cotizaciones del agente + export on-demand + renderer compartido):** El tool `generar_pdf` del agente AE subía **HTML** a GCS/Drive (el "PDF" era una página imprimible, nunca un archivo PDF). Ahora `/calc/cotizar/pdf` renderiza un PDF real con el mismo Chromium del flujo UI y lo sube a GCS (`quotes/pdf/`) + Drive (bundle PDF+JSON vía `saveQuotationBundleToDrive`, misma estructura de carpetas que el archive de la calculadora). **Contrato retrocompatible:** `pdf_url`/`gcs_url` siguen siendo HTML para siempre (el sheet-quote-pipeline hace fetch de `pdf_url` como texto); el PDF real viaja en los campos nuevos `pdf_file_url` + `drive_url` (ahora webViewLink del PDF) + `pdf_rendered`. Renderer extraído a [`server/lib/quotePdf.js`](../../server/lib/quotePdf.js) (semáforo max-2 Chromiums concurrentes; `server/routes/pdf.js` ahora lo consume con contrato idéntico). `GET /api/me/quotes/:id/export.pdf` ahora re-renderiza on-demand desde `payload.request` (persistido nuevo) en vez de 404. Kill switch: `COTIZAR_PDF_RENDER=0`. **Diseño refinado:** el HTML del agente ahora se renderiza con el template preferido `simple` (R3-C, vía `buildQuotationModel` + `renderPdfLayout` importados server-side; `src/pdf-templates/` agregado al Dockerfile del API — sin ese COPY prod crashea al boot), con fallback automático al layout técnico `generatePrintHTML` si el template falla; `body.template` permite elegir otra variante. **Bugfix incluido:** `calc.js` pasaba el webViewLink (URL) como `driveFileId` y el allowlist de `quoteStore` lo rechazaba → el upsert a `identity.quotes` fallaba silenciosamente desde siempre; ahora se pasa el file id real. **Hallazgo prod (pre-existente):** el renderer de prod está roto desde el deploy del 2026-07-04 14:48 UTC (rev `panelin-calc-00716`, rebuild de imagen bumpeó Chromium de Alpine → `Protocol error (Target.setDiscoverTargets): Target closed` con `--headless=new --single-process`); la UI caía en silencio al fallback raster html2pdf. `quotePdf.js` incluye retry de launch con arg-set moderno (solo ante fallo de launch, no de render) que debería auto-repararlo al deployar; memoria del servicio subida a 1Gi en `deploy-calc-api.yml` (2 Chromiums concurrentes + Node no entran en 512Mi). Tests nuevos [`tests/quotePdf.test.js`](../../tests/quotePdf.test.js) (17/17, agregado a `test:core`) + 2 casos nuevos en `agentTools.test.js` (298/298). Verificado local con Chrome real: `/api/pdf/generate` 200 `%PDF`, `/calc/cotizar/pdf` `pdf_rendered:true`, `smoke:bmc-pdf` OK, degradado macOS sin env var = contrato legacy intacto. Pendiente post-deploy: confirmar en prod `pdf_rendered:true` y `gsutil stat` del objeto `application/pdf`; probar dry-run del sheet-quote-pipeline (debe seguir recibiendo HTML de `pdf_url`).

**2026-07-07 (fix — PDF template: remove logo + show panel qty/length):** Logo removed from preferred "Presupuesto Simple" (refined R3-C) template as it was not rendering reliably. Enhanced `simple.js` + `buildQuotationModel` to explicitly display panel quantity (`cantPaneles`) and length (`largoPanel`) in the BOM (e.g. "ISODEC ... (12 paneles × 4.80 m)"). Test artifacts updated in `.runtime/`. Template approved and ready for production. `simple` remains the recommended layout.

**2026-07-07 (feat — PDF quote preferred template):** Made the refined R3-C version of "Presupuesto Simple" the preferred production template for client PDF quotes (used by "PDF Cliente", Drive exports, WA, etc.). The `simple` layout is now the refined production theme (32px logo, updated header styling, etc.). The previous pre-refinement version is preserved and selectable as `simple-previous`. All generations were exported to `.runtime/pdf-generations-2026-07-07/` for comparison before switching the default. Updated `src/pdf-templates/{simple.js, index.js}` + `simple-previous.js`.

**2026-07-07 (feat — plantilla PDF clásica "Hoja Visual Cliente" recuperada como opción seleccionable):** El PDF cliente original (`generateClientVisualHTML`, la HOJA VISUAL CLIENTE usada antes del sistema de plantillas) había quedado inaccesible desde la UI: el dropdown "Diseño PDF" solo ofrece `LAYOUT_OPTIONS` y el default es `'simple'`, así que ningún valor seleccionable llegaba al fallback clásico de `buildClientePdfHtml`. Recuperada como layout `classic` ("Clásico — Hoja Visual Cliente (formato anterior)", grupo legacy): `buildQuotationModel()` ahora preserva los inputs crudos en `q.raw` y `renderPdfLayout('classic', q)` los enruta a `generateClientVisualHTML` (import dinámico, sin ciclo; modelos sin `raw` caen al mapa de templates). Aparece automáticamente en ambos selectores (calculadora + GoogleDrivePanel) y persiste en `localStorage bmc.pdfLayout` como cualquier otro layout. Test nuevo [`tests/pdfClassicLayout.test.mjs`](../../tests/pdfClassicLayout.test.mjs) (10/10, agregado a `test:core`); `tests/pdf-pipeline.test.mjs` ajustado (classic es HTML de flujo único, sin contenedores `.page`; su SAMPLE.appendix ahora incluye `totals` como el payload real de `buildPdfAppendixPayload`). CLAUDE.md actualizado (12 layouts + classic). Nota: los ✗ pre-existentes de `pdf-pipeline` para la familia simple (pages:false) ya estaban antes de este cambio.

**2026-07-06 (perf — prompt caching en agentCore (Anthropic prompt cache) + fix historial Gemini):** Cierra el PR 3 del programa de 3 sabios, con el diseño que evita el footgun (cachear el prompt entero backfire porque `buildSystemPrompt` embebe `calcState` → nunca pega → 1.25x write). **Solución:** nuevo `buildSystemPromptParts()` en `chatPrompts.js` que separa el prompt en `{ staticPrefix, dynamicTail }` — el prefijo estático (identidad, catálogo, precios canónicos, tools ≈ **75.7k chars / ~20k tokens**, request-independiente) y la cola dinámica (calcState, ejemplos KB, prefs, rag ≈ **~700 chars**). `buildSystemPrompt()` queda como wrapper **byte-idéntico** (todos los callers existentes intactos; `brainBlockStr` se mantiene en su posición original en el prefijo → byte-idéntico en todo modo, y con el flag brain ON simplemente no cachea, no-prod). En `agentCore.js`, la rama Claude arma un `system` estructurado con `cache_control: {type:"ephemeral"}` sobre el prefijo; `systemPrompt` (string) sigue igual para los otros proveedores + logs. **Cobertura de cache ~99% del prompt** (el prefijo estable entre calcStates → lecturas 0.1x sobre hit dentro del TTL de 5min). Además, **fix del historial Gemini**: la rama pasaba solo `${systemPrompt}\n\n${lastUser}` (descartaba todo el historial multi-turno); ahora usa `systemInstruction` + `contents[]` desde `messages`. Tests `tests/promptCacheSplit.test.js` (7/7: byte-identity, prefijo estable entre calcStates, >90% del prompt, calcState solo en la cola). `gate:local` verde (la única falla es `gate-secrets-drift` de WHATSAPP_*, **pre-existente**, confirmada en árbol limpio — no la causa este PR). PR #<pending>.

**2026-07-06 (fix — keyword monitor SERP uses system Chromium in Cloud Run):** `keywordSerpPlaywright.js` resuelve `CHROMIUM_EXECUTABLE_PATH` (mismo contrato que PDF/TraKtiMe) con fallback a `/usr/bin/chromium-browser` y `/usr/bin/chromium` antes del bundle de Playwright. Args headless estándar para distro Chromium en contenedor. Cierra el gap de refrescos programados P1 (scheduler 04:00 UTC) que fallaban en prod. Tests `keywordSerpPlaywright.test.js`. PR #603.

**2026-07-06 (feat — Hub Wolfboard operator overview):** `/hub` muestra `OperatorOverview` — resúmenes de Control Plane IA (`/api/assistants/status`) y KPIs Finanzas con links a módulos oficiales. Aditivo puro en `BmcWolfboardHub.jsx`; sin cambiar rutas existentes.

**2026-07-06 (fix — PDF generation & export):** Diagnosed and fixed primary PDF export path ("PDF Cliente"). Root causes: (1) server route always attempted @sparticuz Linux binary on macOS dev → ENOEXEC + noisy 503 (now early clean guard + explicit detail); (2) templates reference `/bmc-pdf/assets/bmc-logo.png` which fails to load inside puppeteer `setContent` (no origin) → logo missing on server PDFs (now inlined as data: URL at render time); (3) explicit `margin` in `page.pdf` overrode template `@page` rules → wrong spacing (added `preferCSSPageSize:true` + reduced fallback margins). Also hardened client html2pdf fallback (longer settle 800ms, filename passthrough, imageTimeout). Server PDFs now honor modern "simple" family layouts + crisp vector when available (prod Cloud Run). Fallback path improved for dev. No behavior change for clients; gate:local lint clean. 

**2026-07-04 (feat — Market Intel live keyword monitor, Playwright SERP + UI en Hub):** Nuevo monitor de keywords orgánicas para `bmcuruguay.com.uy` en **Hub → Market Intelligence → Inteligencia** (primer bloque de `IntelPanel`). Backend: `keywordMonitor.js` (25 seeds P1/P2/P3, Autocomplete volume proxy, SERP Playwright Google→Bing con reintentos ×3, preserva snapshot ante rate-limit), `keywordSerpPlaywright.js`, estado JSON + migración opcional `009_create_keyword_monitor.sql`. API: `GET/POST /api/marketing/keywords`, `POST /api/marketing/keywords/refresh` (202 + poll). Scheduler P1 04:00 UTC, full domingos 05:00 UTC. UI `KeywordMonitor.jsx`. Tests `keywordMonitor.test.js`. Setup: `npm run playwright:install`. Verificado: 13/13 P1 con posición SERP. PR #585.

**2026-07-04 (feat — `/hub/admin-ingreso` P0 LIVE):** Módulo **Ingreso y actualización Admin** — cola de consultas pendientes (Admin I, M vacía), chat React nativo con Gemini vía `panelin-calc/chat`, guardar J/K/L con confirmación. Ruta `/hub/admin-ingreso` (grant admin); card en Hub; link «Abrir módulo completo» en Respondamos Rapido. Fix `bmcChatSheets.writeBmcChatInterpretation` cuando `missing_L` es array. Docs: [`docs/team/features/INGRESO-ACTUALIZACION-ADMIN-DESIGN-BRIEF.md`](./features/INGRESO-ACTUALIZACION-ADMIN-DESIGN-BRIEF.md), [`INGRESO-ACTUALIZACION-ADMIN-UX-SPEC.md`](./features/INGRESO-ACTUALIZACION-ADMIN-UX-SPEC.md). **Pendiente P1:** precarga calculadora + cotización Drive (col M).

**2026-07-04 (feat — Panelin living assistant UX: character, sidebar, floating, TTS speed):** Calculadora (`PanelinCalculadoraV3_backup.jsx`): personaje `PanelinCharacter` en header (solo chat cerrado; click abre chat); chat **no modal** como 3.er `Panel` redimensionable (`embeddedMode`); botón **Flotar** → ventana in-page arrastrable/redimensionable (`PanelinFloatingChatShell` + portal); **Volver a sidebar**; persistencia `sessionStorage` de modo y rect flotante. `PanelinChatPanel`: avatar con anillos speaking/thinking, TTS con slider 0.75–1.35× (`panelin-tts-speed`). Archivos nuevos: `PanelinCharacter.jsx`, `PanelinFloatingChatShell.jsx`. Verificado Playwright en prod (`calculadora-bmc.vercel.app`) y localhost:5174.

**2026-07-04 (control plane — `panelin` FLIPPED ON en prod; `ASSISTANTS_ACTIVE=canales;ml;panelin`):** Fase 2 del control plane, gateada por el failover hardening (entrada de abajo) ya live. El asistente `panelin` (ruta `/api/agent/chat`, chat Panelin) pasó de `disabled` (503) a **`live`**. Mecanismo durable: repo Variable `ASSISTANTS_ACTIVE` de `canales;ml` → `canales;ml;panelin` + dispatch de `deploy-calc-api.yml` (que renderiza `${{ vars.ASSISTANTS_ACTIVE }}` a Cloud Run env, línea 175 — separador `;` por el fix de #561). **Verificación post-flip:** `/api/assistants/status?deep=1` muestra `active=[canales,ml,panelin]`, los tres `live` sobre claude; `POST /api/agent/chat` autenticado devuelve 200 (antes 503); 0 logs de error en prod en los 5 min post-flip; `providerCooldowns` registró fallos transitorios de claude/grok **sin** entrar en cooldown (el hardening funcionando como diseñado). Rollback en un comando: `gh variable set ASSISTANTS_ACTIVE --body "canales;ml"` + redeploy. Nota de seguridad: `/api/agent/chat` es público-por-diseño detrás de `publicLimiter` (10/min) + origin checks (no requiere auth como `suggest-response`); el flip no cambia eso.

**2026-07-04 (reliability — failover hardening en agentCore: timeouts + cooldown + orden capability-aware):** Consenso de los 3 sabios (gate para poder flipear `panelin`). Tres gaps del failover cerrados. **(1) Timeouts por proveedor:** antes, una llamada colgada bloqueaba el request entero (SDK de Anthropic default ~10 min) y colgaba el SSE; ahora `callWithTimeout` (default 30s, `AGENT_PROVIDER_TIMEOUT_MS`) aborta vía `AbortSignal` **y** rechaza vía `Promise.race` (garantiza que el loop avanza aunque el SDK ignore el signal) — envuelto en las 4 ramas SDK de `callAgentOnce`. **(2) Cooldown por proveedor:** tras 3 fallos/60s (`AGENT_PROVIDER_COOLDOWN_*`) un proveedor se **deprioriza** (se prueba último), **no** se elimina — reordenar en vez de saltear evita un apagón auto-inflingido si todos entran en cooldown; un success limpia la racha. Expuesto en `/api/assistants/status` vía `providerCooldowns` (responde el "¿por qué ml sirve en gemini?" que antes era invisible — el health era solo presencia de key). **(3) Orden capability-aware** (`agentChat.js`): el `defaultOrder` pasó de `claude→grok→gemini→openai` a `claude→gemini→grok→openai` porque en el path de chat solo claude y **gemini** ejecutan las tools nativas; grok/openai son text-only ahí. Así un Claude-down cae a Gemini (cotiza bien con tools) en vez de Grok, que inventaría precios (viola el invariante duro). La preferencia explícita `aiProvider` del usuario sigue ganando; solo cambia el fallback AUTO. Tests nuevos `tests/agentCoreFailover.test.js` (8/8: timeout rechaza/aborta/propaga, cooldown entra/limpia/ignora stale); `gate:local` verde. Diferido (PR 3, con footgun identificado): prompt caching + fix de historial Gemini en agentCore. PR #<pending>.

**2026-07-04 (security — auth + rate-limit en `POST /api/crm/suggest-response`, cierre de exposición anónima):** Consenso de la deliberación de 3 sabios (ver plan): la ruta de generación IA `suggest-response` respondía con LLM pago a **cualquier caller anónimo** (verificado por curl en vivo — sin token, sin sesión, sin límite). Era la única de las 6 rutas gated por `requireAssistantEnabled` sin auth: las otras ya la tenían (`email`→`requireCrmCockpitWrite`, `wa`→`requireWaAccess`, `wolfboard`→`requireWolfboardWrite`; `agent/chat` es público por diseño detrás de `publicLimiter`). **Fix:** nuevo modo `authOnly` en `requireServiceOrUser` (acepta el token estático de servicio **o** cualquier JWT de identidad activo; rechaza anónimo) — se eligió `authOnly` sobre RBAC por módulo porque los dos cohortes que llaman la ruta usan grants distintos (ML cockpit = `canales:read`, cotizaciones = `role:admin`), y un guard de un solo módulo/rol daría 403 a uno. Montaje en `server/index.js`: `aiGenLimiter` (20/min por IP) → `requireServiceOrUser({authOnly:true})` → gate de asistente. El smoke (`scripts/smoke-prod-api.mjs`) ahora presenta `API_AUTH_TOKEN` como `x-api-key` (env agregada al job `smoke:prod` en `ci.yml`). Tests nuevos `tests/requireServiceOrUserAuthOnly.test.js` (6/6: acepta token vía Bearer/x-api-key, acepta usuario sin grant de módulo, rechaza disabled/anónimo/`?key=`); `gate:local` verde. Follow-up documentado: endurecer a RBAC por módulo una vez unificados los grants; narrow de CORS `chrome-extension://` (requiere el ID real de la extensión WA Cockpit) y `BUDGET_ENABLED=ON` quedan como ops/env. PR #<pending>.

**2026-07-04 (RUN BLOCKERS — go-live 6.0 cerrado + WA canonical FLIPPED ON en prod + fix separador `ASSISTANTS_ACTIVE`):** Run ejecutivo que convirtió los "bloqueos externos" en acciones concretas. **(1) Go-live 6.0 CERRADO:** `smoke:prod` pasó de 7/8 a **9/9 verde** — `POST /api/crm/suggest-response` responde 200 (gemini). Cadena de fixes: inyección puente vía `gcloud run services update` → hallazgo de que el redeploy la pisaba porque el action deploy-cloudrun **parte `env_vars` por comas** (la Variable `canales,ml` llegaba truncada a `canales`) → fix del parser en `server/config.js` para aceptar `;` (PR **#561**, mergeado) → repo Variable final **`ASSISTANTS_ACTIVE=canales;ml`** → deploy por workflow renderiza bien y el smoke final 9/9 corre contra revisión sin overrides manuales. **(2) verify-tabs VERDE:** credencial de la SA materializada desde Doppler `bmc-backend/prd` a `~/.config/bmc/service-account.json` (fuera del repo) + IDs reales leídos de Cloud Run + `BMC_SHEET_SCHEMA=CRM_Operativo` → los 5 workbooks accesibles; ítem 1.4 del checklist (compartir workbook con la SA) queda verificado de facto. **(3) PR #560 MERGEADO** (rebase sobre main con interleave de PROJECT-STATE): react-router 6.30.4 (audit **0 vulnerabilidades** — nodemailer ya venía fixeado en #548), trigger 🚀 canónico (`triggerWaCrmSyncNow`), wiring `ASSISTANTS_ACTIVE`, diagnósticos smoke/verify-tabs. **(4) WA CANONICAL FLIPPED ON EN PROD:** gate local `omni:local-e2e` **30/30** con Postgres real; migraciones **011–015 aplicadas a prod** vía workflow `wa-canonical-cutover.yml` action=migrate (requirió otorgar `secretmanager.secretAccessor` sobre el secret `DATABASE_URL` a `github-deployer@` — scopeado al secret, no proyecto-wide); `flip_on` del workflow falló por bug propio (usa `gh variable get` con `github.token`, que no puede leer Variables → reporta MISSING aunque estén en 1; **pendiente fix del workflow**), así que el flip se ejecutó manualmente con los mismos dos comandos (`gh variable set OMNI_WA_CANONICAL=1` + dispatch del deploy). Cloud Run corre con `OMNI_WA_CANONICAL=1` + bus + orchestrator ON. **Soak snapshot post-flip: limpio** (0 dead-letter, invariante de coalescing OK, 0 tráfico WA en 24h — baseline esperado). Rollback instantáneo disponible: `gh workflow run wa-canonical-cutover.yml -f action=flip_off`. **Pendientes que siguen abiertos:** UAT visual 6.1–6.3 (humano), spot-check del 🚀 con tráfico WA real, decidir endurecer `SMOKE_SKIP_SUGGEST=1` en el deploy smoke, y fix del prerequisite-check del workflow cutover.

**2026-07-04 (WA canonical + go-live — 🚀 canónico implementado; repo Variable lista):** Se cerró otro paso del loop. En la rama `chore/react-router-6-30-4`, el modo **WA canonical** ya preserva el affordance manual del **🚀** sin volver al path legacy: cuando entra un mensaje con `🚀`, `server/index.js` acelera el job durable `wa_crm_sync` existente (o lo encola inmediato si aún no existe) mediante `triggerWaCrmSyncNow()` en `server/lib/omni/orchestrator/aiWorker.js`. Resultado: se mantiene la semántica operativa "corré esto ahora" pero sobre la cola canónica/debounce, no sobre `processWaConversation`. Test offline agregado en `tests/omniHardening.test.js` (expedite + fallback enqueue). Además, el bloqueo operativo de `suggest-response` quedó aún más reducido: la repo Variable **`ASSISTANTS_ACTIVE=canales,ml` ya fue seteada en GitHub**; desde repo ya sólo resta **redeploy** para que Cloud Run tome la variable y re-verificar `smoke:prod`.

**2026-07-03 (feat — `omni_suggestions.resolved_at`: timestamp de resolución HITL, cierra diferido de Fase 2):** Migración **`015_omni_suggestions_resolved_at.sql`** (`ADD COLUMN IF NOT EXISTS resolved_at TIMESTAMPTZ`, barata e idempotente). `resolveSuggestion()` (`server/lib/omni/orchestrator/suggestions.js`) ahora estampa `resolved_at = now()` al accept/reject, con **fallback dormancy-safe**: si la columna no existe todavía (error `42703`, entorno pre-015) reintenta el UPDATE legacy — deployar el código antes de la migración **no** rompe los endpoints de approve (mismo patrón que el fix de `run_after` de PR #482). El read-adapter del cockpit WA (`omniReadAdapter.js`) vuelve a mapear `chosen_at` desde `resolved_at` (antes quedaba `null` fijo tras el hallazgo Codex P2). **Prerequisito nuevo para `OMNI_WA_READS=1`:** aplicar la 015 antes del flip (el SELECT del adapter la referencia sin guard). Tests: nuevo `omniSuggestionResolve.test.js` (8 asserts: estampado accept/reject, fallback 42703, errores no-42703 propagan, guard de pending) + `omniReadAdapter.test.js` actualizado (chosen_at desde resolved_at, pre-015 → null); ambos en `test:core`. `gate:local` verde.

**2026-07-03 (chore(ci) — gh#495: los smokes de prod ya no fallan en silencio):** Nuevo composite action [`.github/actions/prod-alert`](../../.github/actions/prod-alert/action.yml): ante fallo de un smoke no-gating abre un issue de tracking `🚨 Prod smoke failing: <job>` (dedupe por título exacto — si ya hay uno abierto, comenta en vez de duplicar); al recuperar, lo **auto-cierra** (sin alarmas stale); todo `gh` degrada a warning (la alerta nunca rompe el job). Wireado como step final `if: always()` en `smoke` / `channels_pipeline` / `voice_health` de `ci.yml` + el cron `smoke-prod-scheduled.yml`, con `permissions: issues: write` por job (patrón `catalog-diff.yml`); `voice_health` gana checkout (requisito del action local). **`continue-on-error` intacto** — los deploys siguen sin gatearse (la motivación de diseño de esos jobs no cambia). **E2E verificado con el ciclo completo** vía `workflow_dispatch --ref` del branch: fallo forzado (`api_base` inválido) → creó #539; segundo fallo → comentó #539 sin duplicar; run verde (`skip_suggest=1` para esquivar el 503 pre-existente de suggest-response) → auto-cerró #539. PR #538; cierra gh#495. Recordatorio operativo: mientras el 503 de `suggest-response` siga (keys IA en Cloud Run/GSM), el próximo cron (8/20 UTC) va a abrir el tracking issue correspondiente — eso es el sistema funcionando, no un bug.

**2026-07-03 (fix — health probe del asistente `email` alineado a `isChatwootConfigured()`, sin falso "live"):** Hallazgo del reporte [`AI-CONNECTIONS-2026-07-02.md`](./AI-CONNECTIONS-2026-07-02.md): el probe de deps del asistente `email` en `assistantRegistry.js` chequeaba **solo** `config.chatwootApiToken`, mientras la ruta (`emailAgentChat.js`) y todas las tools de Chatwoot gatean con `isChatwootConfigured()` = `CHATWOOT_API_BASE + CHATWOOT_API_TOKEN + CHATWOOT_ACCOUNT_ID` — con token presente pero base/accountId ausentes, el panel `/hub/admin/assistants` reportaba **live** con cada tool tirando `chatwoot_not_configured` (falso verde). Fix: el probe ahora usa el **mismo gate** (`isChatwootConfigured()`, import directo de `chatwootClient.js` — sin dependencia circular) y el `detail` nombra las 3 vars faltantes. Nota: el email vivo (Gmail/Omni) corre bajo el asistente `canales`, no bajo `email` — este asistente es la superficie Chatwoot dormida. Regresión cubierta en `tests/assistantControlPlane.test.js` (token-only → `down`, config completa → `live`; 30/30). PR #536.

**2026-07-03 (WA canonical — owner decisions captured in loop, 3/3 cerradas):** En el run iterativo "decision by decision" quedaron cerradas las 3 decisiones del runbook `wa-canonical-flip`: (1) **AF–AG retirement = sí** → en modo canónico WA se retira el auto-fill AF–AG y queda **Omni `suggest`** como único camino IA; (2) **modelo CRM = una sola fila por teléfono** → los recontactos / distintas cotizaciones del mismo teléfono se distinguen en metadata, no creando filas nuevas por default; (3) **trigger manual = conservar el mismo affordance UI del 🚀** → el operador debe seguir viendo el mismo botón/affordance, aunque por debajo dispare el flujo canónico. Resultado: el bloque de decisiones del owner ya no está ambiguo; lo que queda pendiente es bajar esas decisiones a implementación + staging soak + pasos de prod.

**2026-07-03 (go-live/E2E — diagnóstico afinado de smoke + verify-tabs):** Seguimiento del punto 1 abierto tras el re-check de go-live. Se confirmó que el `503` de `POST /api/crm/suggest-response` en prod **no** venía de keys IA rotas sino del **gate del control plane**: la respuesta real es `{"reason":"assistant_disabled","assistant":"ml"}` porque `/api/crm/suggest-response` está detrás de `requireAssistantEnabled("ml")` y Cloud Run está corriendo con `ASSISTANTS_ACTIVE` sin `ml` (canales-only). Además, el bloqueo local de `npm run verify-tabs` quedó afinado: en el `.env` actual los workbook IDs (`BMC_SHEET_ID`, `BMC_PAGOS_SHEET_ID`, etc.) están vacíos y `GOOGLE_APPLICATION_CREDENTIALS` apunta a `docs/bmc-dashboard-modernization/service-account.json`, archivo que hoy **no existe** en el repo local. Mejoras de tooling/documentación en la rama `chore/react-router-6-30-4`: `smoke-prod-api.mjs` ahora reporta explícitamente `assistant_disabled`/`ASSISTANTS_ACTIVE` en vez de culpar genéricamente a keys IA, y `verify-sheets-tabs.js` falla temprano con mensaje claro si falta el JSON de credenciales.

**2026-07-03 (go-live/E2E — decisión tomada + wiring de deploy para `ASSISTANTS_ACTIVE`):** En el loop de cierre se decidió que `POST /api/crm/suggest-response` **sí** debe quedar operativo para go-live (no capability opcional). La causa real del 503 era que el workflow de deploy **no propagaba** `ASSISTANTS_ACTIVE` a Cloud Run, dejando al servicio en el default prod `canales-only` aunque existan keys/probe OK para `ml`. Fix en la rama `chore/react-router-6-30-4`: `.github/workflows/deploy-calc-api.yml` ahora inyecta `ASSISTANTS_ACTIVE=${{ vars.ASSISTANTS_ACTIVE }}` junto a los demás flags `OMNI_*`. La repo Variable ya quedó creada como **`ASSISTANTS_ACTIVE=canales,ml`**; desde repo el pendiente externo exacto pasa a ser **redeployar** y re-correr `smoke:prod`. El smoke del workflow sigue con `SMOKE_SKIP_SUGGEST=1`; queda pendiente decidir si ese gate debe endurecerse una vez confirmada la variable en prod.

**2026-07-02 (feat — drawer "Agregar producto" siempre disponible en la calculadora):** Menú desplegable (slide-out desde el borde derecho) con **un solo buscador sobre TODO el catálogo** (paneles por espesor, perfilería, tornillería/fijaciones+herramientas, selladores) por nombre o SKU, disponible en **cualquier escenario/paso** de la calculadora. Hallazgo clave: la capa "aditiva" ya existía — el drawer **escribe en el estado presupuesto-libre existente** (`librePerfilQty`/`libreFijQty`/`libreSellQty`/`librePanelLines`), que ya se fusiona a cualquier BOM vía `additiveLibreGroups` + `mergeLibreGroups` (`PanelinCalculadoraV3_backup.jsx`), fluyendo a totales (`grandTotal`), PDF, WhatsApp y guardado `.bmc.json` **sin tocar** esas rutas. Nuevos: [`src/utils/productCatalogIndex.js`](../../src/utils/productCatalogIndex.js) (flattener unificado de los 9 exports, reusa `flattenPerfilesLibre`) + [`src/components/ProductQuickAddDrawer.jsx`](../../src/components/ProductQuickAddDrawer.jsx) (FAB+drawer, patrones de `BmcChatPanel`/`UserDetailDrawer`, estilos inline + CSS vars) + test [`tests/productCatalogIndex.test.js`](../../tests/productCatalogIndex.test.js) (22/22, agregado a `test:core`). Paneles se agregan con cantidad en m² (evita el skip `m2<=0` del motor libre). `gate:local` verde (lint+test+test:api, exit 0); los 3 archivos compilan vía transform de Vite. Pendiente: click-through E2E en navegador (extensión no conectada en esta sesión).

**2026-07-02 (deps/docs — react-router GHSA fixed on branch + go-live re-check):** Rama aislada `chore/react-router-6-30-4` creada para cerrar las 2 vulns moderate de `react-router`/`react-router-dom` sin mezclar el worktree sucio de `feat/panelin-live-detachable-floating`. El fix real no requería migración mayor a v7: `react-router-dom` subió **6.30.3 → 6.30.4** (el `package-lock` ya resolvía `react-router`/`react-router-dom@6.30.4` en `node_modules`, faltaba alinear el manifest raíz). `npm audit --json` queda en **0 moderate**; sólo persiste **1 high** en `nodemailer<=9.0.0` (fixAvailable `9.0.3`, major). Validación en la rama: `npm ci`, `npm ls react-router-dom` → `6.30.4`, `npm run build` verde. Re-check go-live/E2E: `npm run smoke:prod` ahora da **7/8 OK** y falla sólo `POST /api/crm/suggest-response` con **503**; `npm run verify-tabs` quedó **bloqueado** en esta sesión porque `BMC_SHEET_ID` no está cargada localmente, así que 1.4/2.x/3.x/6.x siguen manuales o dependientes de credenciales.

**2026-07-01 (WhatsApp unificado — PR #482 MERGEADO a `main`, sigue dormido):** Cierre del branch `claude/whatsapp-unified-interaction-0mdl68`: el flip Omni canónico + hardening + `run_after` + outbound + read-model (todas las entradas de abajo) quedaron en `main` vía squash `2bc6e988`, **dormido** tras `OMNI_WA_CANONICAL`/`OMNI_WA_READS` OFF (sin cambio de comportamiento en prod). Camino a merge: **(1) resolución de conflicto con `main`** — `main` (`ac2ae68d`) había reemplazado la escritura hardcodeada de CRM_Operativo por el contrato **header-anchored + key-based + CSV-sanitizado** (`crmRowMapper.js` `buildCrmRow`/`validateCrmRow`/`sliceCrmRange` + `sanitizeCellValue`); el merge **adopta** ese contrato dentro del helper compartido `server/lib/wa/crmIngestWrite.js` (nuevo `writeWaCrmAiTail` para el tail AF:AG) → ambas rutas (legacy OFF + job `wa_crm_sync`) escriben filas anti-column-shift, sin reintroducir los rangos posicionales. **(2) CodeQL log-injection (CWE-117)** — resuelto pasando los 4 log sinks WA a **logging estructurado pino** (`logger.info({chat_id,…}, "msg")`): el valor de usuario va al objeto de metadata (JSON, newlines escapados), nunca al string de mensaje que es el sink real; el sanitizer regex `/[\r\n]/g` **no** era reconocido por la config CodeQL de la repo. **(3) 4 hallazgos de reviewers (Codex + Copilot) verificados y corregidos antes del merge:** `run_after` referenciado sólo cuando hay delay en `enqueueAiJob` INSERT + predicado del claim gateado en `omniWaCanonical` (restaura dormancy a nivel DB — el deploy no auto-migra, así el worker shadow ya vivo no rompe contra un schema pre-012); `buildOmniSuggestionsSql` deja de seleccionar `s.resolved_at` (columna inexistente en `omni_suggestions` → `/api/wa/suggestions` rompía con `OMNI_WA_READS` ON), `chosen_at` queda `null`; `chooseWaIngestMode` exige `omniEventBusEnabled + omniAiOrchestratorEnabled` antes de flipear a canónico (si no, fallback a legacy → sin drop silencioso de leads); `requireWaAccess` corre antes de `readLimiter` para keying por operador. CI verde en el SHA final (Lint, Validate, CodeQL/Analyze, Deploy preview, Env drift, dependency-review). **Pendiente operador para pasar ON (sin cambios):** aplicar `omni:migrate` (011+012) antes de `OMNI_WA_CANONICAL=1`; validar `OMNI_WA_READS` en staging. **Diferido Fase 2 (nombrado):** columna `resolved_at` en `omni_suggestions` + poblarla en el approve; surface de `omni_suggestions` en el cockpit WA.

**2026-07-01 (fix — colisión de migración `011` entre dos PRs, atrapada tras el merge en paralelo):** El merge de `main` (WA-canonical flip) a esta rama se resolvió dos veces en paralelo — una vez por esta sesión, y ya aplicada por otra sesión (`0322b38a`, "Union test:core scripts, ALLOWED_AI_JOB_TYPES..."). Esa resolución arregló correctamente el conflicto de **git** (`ALLOWED_AI_JOB_TYPES` en `aiWorker.js` → unión `assist`+`wa_crm_sync`), pero **no** el conflicto que git no puede ver: `011_ai_job_type_assist.sql` (esta rama, agrega `'assist'`) y `011_wa_crm_sync_job.sql` (main, agrega `'wa_crm_sync'`) son **archivos distintos** con el mismo número, así que no chocan como conflicto — pero ambos hacen `DROP CONSTRAINT` + `ADD CONSTRAINT` sobre la **misma** `omni_ai_jobs_type_valid`, cada uno con su propia lista (no aditivo). `omni-migrate.mjs` ordena por nombre de archivo (`readdirSync().sort()`): `011_ai_job_type_assist.sql` < `011_wa_crm_sync_job.sql`, así que la de WA corre **segunda** y su `CHECK` (sin `'assist'`) habría revertido silenciosamente el fix de **C1** la próxima vez que alguien corriera `omni:migrate` — el INSERT de `/omni/.../assist` volvería a fallar silencioso (`.catch`), reproduciendo el bug original de C1 mediante una vía distinta. **Fix (PR #531):** `011_wa_crm_sync_job.sql` (el que "gana" por orden alfabético) ahora tiene el `CHECK` unión completa: `('classify','suggest','extract_deal','embed','assist','wa_crm_sync')`, con comentario explicando la colisión cross-branch para que quien la lea después no la deshaga por error. **Corrección (Codex + Copilot, ambos atraparon el mismo hueco de forma independiente):** editar `011_wa_crm_sync_job.sql` NO alcanza para un ambiente donde ese archivo **ya se aplicó** — `omni-migrate.mjs` trackea migraciones aplicadas por **nombre de archivo** en `omni_schema_migrations` y saltea (`skip`) cualquier nombre ya registrado, sin importar si el contenido del archivo cambió. Agregada `014_ai_job_type_union.sql` — mismo `DROP`+`ADD CONSTRAINT` con la unión completa, pero con un nombre de archivo **nuevo** que nunca fue aplicado en ningún ambiente, así que siempre corre y converge el estado sin importar qué combinación de los dos "011" ya esté registrada. `npm run omni:migrate` sigue siendo el comando correcto — la corrección es que ese comando ahora SÍ hace el trabajo (vía 014), no que 011 se re-ejecute (no lo hace). `test:core` verde tras el fix.

**2026-07-01 (Calculadora UX live-edit + panelin-live/auth branch delta):** Sesión live-edit en `localhost:5173`: panel derecho sin duplicado de **Lista de precios**; barra sticky **TOTAL c/IVA** al scrollear BOM (desktop). Calculadora: `librePanelDimensions.js` + `PlantaBorderGlassPicker.jsx`; steppers grandes en paso Dimensiones; focus/select en cadena. Auth: `authGoogle.js` + `localDevAuth.js` + ajustes `BmcAuthProvider`/`AuthGateModal`. Panelin-live: scripts E2E/voice-probe/bootstrap + tests. Omni: `OmniAdminCockpit` error vs cola vacía. `gate:local:full` verde antes de push.

**2026-07-01 (feat — Assistant fallback LINE: siempre hay un agente disponible + fix del pin claude-only del copilot canales):** Follow-up a PR #524. Dos gaps cerrados. (1) **Copilot canales sin fallback de proveedor:** el endpoint `/omni/conversations/:id/assist` (`server/routes/omni.js`) pasaba `provider: "claude"` a nivel top de `callAgentOnce`, lo que fuerza `chain = ["claude"]` y **anula el fallback** — si Anthropic caía, el copilot devolvía 502 sin backup. Fix: se quitó el pin top-level (se mantiene `override.provider:"claude"` como *preferencia*), restaurando `claude→grok→gemini→openai`. (2) **`dispatchAssistant` estaba library-only (sin wirear):** reescrito a una **línea de fallback ordenada** (`ASSISTANT_PRIORITY = [canales, panelin, email, wa, ml, wolfboard]` → `seam`): prueba el asistente pedido, y ante indisponibilidad (chequeo proactivo `isNodeAvailable`: enabled + deps ok + ≥1 proveedor) o error, avanza al **siguiente asistente ENABLED** (los disabled se saltan — respeta `ASSISTANTS_ACTIVE`, canales-only sigue canales-only), terminando en el `seam` always-on (disponible mientras exista ≥1 API key de proveedor). Ahora **wireado** en el copilot canales (handler primario = la llamada actual, luego la línea). Nuevos exports `buildFallbackLine` + `ASSISTANT_PRIORITY`; logs `assistant_skip`/`assistant_error`/`assistant_failover`. Test extendido a 27 asserts (composición de la línea, skip de disabled, "sin proveedores → sin agente disponible" sin gastar llamada). `gate:local` verde. Con canales-only la línea es `[canales, seam]` (≈ cadena de proveedores); escala solo al habilitar más asistentes.

**2026-06-30 (WhatsApp — convergencia read-model cockpit→omni, Fase 2b/2c, dormido):** Detrás del flag nuevo **`OMNI_WA_READS`** (default OFF), los 3 endpoints de lectura del cockpit `/hub/wa` (`/api/wa/conversations|messages|suggestions`, `server/routes/wa.js`) leen de `omni_*` en vez de `wa_*`, mapeando a la **misma shape** que el cockpit ya consume → sin cambio de frontend. Nuevo `server/lib/wa/omniReadAdapter.js`: mappers puros (`mapOmniConversation/Message/Suggestion`) + SQL builders. **2c** incluido: las sugerencias del cockpit pasan a salir de `omni_suggestions` (mapeadas a `options[{text,tone}]`, tono desde metadata, `chosen_idx` desde `approval_state`). Mensajes: `direction` desde `sender`, `msg_id` desde `metadata.wa_msg_id`. **⚠️ Campos lossy (decisión de dueño + validación en staging antes de ON):** `status` omni (open/pending/snoozed/resolved) ↔ wa (new/pending/quoted/closed) — "quoted" no tiene equivalente omni; `owner_op`/`lead_sheet_row`/`intent_last` se leen best-effort de `omni_conversations.properties`. Con flag OFF, lecturas idénticas a hoy (`wa_*`). Test `omniReadAdapter.test.js` (33 asserts, mappers+SQL builders) en `test:core`. `OMNI_WA_READS` cableado en `deploy-calc-api.yml`. `gate:local` verde. **Pendiente para ON:** validar en staging contra el cockpit real + confirmar mapeos de status/owner. Con esto, el dual-write `wa_messages` se puede retirar en una fase posterior una vez verificado.

**2026-06-30 (WhatsApp — unificación de outbound, Fase 2a):** Refactor puro (sin cambio de comportamiento, sin flag). Había **dos senders** que pegaban al **mismo** endpoint Graph API con lógica duplicada: `sendWhatsAppText` (`server/lib/whatsappOutbound.js`, throw-on-error, 4 callers: `wa.js /api/wa/outbound`, `bmcDashboard.js`, `agentTools.js`, `transportistaOutboxWorker.js`) y `sendWaReply` (`server/lib/omni/outbound/waReply.js`, shape `{ok,...}`, 1 caller: `omni.js reply`). Extraído **un único** `postWhatsAppMessage({to,text,accessToken,phoneNumberId,signal})` (construye URL+body, cap 4096, timeout 15s+signal, devuelve `{ok,status,data}` sin throw). Los dos senders quedan como adapters finos que **preservan sus contratos** (sendWhatsAppText tira en !ok; sendWaReply normaliza a E.164 `598…` y mapea a `{ok,...}`) → los 5 callers sin tocar. `buildWhatsAppText` (texto de cotización client-side en `helpers.js`) queda aparte (otra responsabilidad). La URL/body/cap del Graph API viven ahora en un solo lugar (sin drift). Test nuevo `whatsappOutboundUnified.test.js` (14 asserts, fetch stub) en `test:core`. `gate:local` verde (sólo `suggestResponseKb` ambiental).

**2026-06-30 (WhatsApp flip — debounce de ráfaga `run_after`):** Cierra el único trade-off que quedaba del hardening (insert-once creaba la fila CRM desde el *primer* mensaje → resumen fino). Nueva columna `omni_ai_jobs.run_after` (migración `012`, `ADD COLUMN` barato) + flag `OMNI_WA_CRM_SYNC_DELAY_MS` (default 60s). El job `wa_crm_sync` se retiene hasta que la conversación queda quieta: cada mensaje nuevo **re-stampa** `run_after` vía `ON CONFLICT ... DO UPDATE` (antes era `DO NOTHING`), y el claim del worker agrega `AND (run_after IS NULL OR run_after <= now())`. Resultado: el job corre **una vez** tras la ráfaga y parsea el transcript completo (resumen rico), reproduciendo el debounce de 5-min legacy sobre la cola durable. Otros job types quedan `run_after=NULL` (corren inmediato, sin cambios). Tests: `waCrmSyncJob` (re-stamp + run_after), `omniWaCanonicalFlag` (default), gate WA-CANONICAL del e2e (no-claimable futuro / re-stamp / claimable pasado). `OMNI_WA_CRM_SYNC_DELAY_MS` cableado en `deploy-calc-api.yml`. `gate:local` verde (sólo falla ambiental pre-existente `suggestResponseKb`).

**2026-06-30 (WhatsApp flip — hardening pre-flip tras review adversarial):** Un workflow de review multi-dimensión (5 dimensiones × 30 agentes, cada hallazgo verificado adversarialmente) produjo **15 hallazgos verificados, 0 refutados**. Todo latente tras el flag OFF (sigue dormido), pero varios eran bloqueantes reales para pasar `OMNI_WA_CANONICAL` a ON. Corregidos en un commit de hardening: **(1) clobber (HIGH)** — el `upsertByPhone` reescribía una fila CRM_Operativo existente en cada mensaje repetido, pisando Estado(J)/Observaciones(W) del operador y reseteando el lock "Bloquear auto"(AK). Reemplazado por **insert-once**: si ya existe fila para el teléfono (match por `chatId` estable, no por `d.telefono` del LLM) → se omite parse + escritura por completo; sólo se crea en el primer contacto, nunca se sobrescribe (`server/lib/wa/{crmIngestWrite,waCrmSyncJob}.js`, nuevo `findCrmRowByPhone`). **(2) costo por mensaje (HIGH)** — al omitir repeats se elimina el parse-conversation LLM por mensaje. **(3) acople a budget (HIGH)** — `wa_crm_sync` se frenaba cuando los `suggest` agotaban `OMNI_AI_DAILY_BUDGET_USD`, parando la captura de leads; el gate de budget ahora **scope sólo a `suggest`** (los jobs bookkeeping siguen drenando) (`aiWorker.js` tick + `runAiJobById`). **(4) carreras de coalescing** — índice parcial ampliado a `status IN ('pending','failed')` (migración 011, renombrado `..._active_dedup`) + **lock advisory pg global** alrededor del append para que dos leads nuevos nunca tomen la misma fila. Además: **dead-letter alert** (`logger.warn "wa_crm_sync DEAD"` + métrica `omni_ai_jobs_24h{job_type,status}`), nota de write-lock de la migración. Tests nuevos/reescritos (`waCrmSyncJobRun`, `waCrmIngestExtract` con asserts de **valores** no sólo rangos, `aiWorkerBudgetScope`, `omniWaParity` sender) + gate WA-CANONICAL del e2e ahora valida coalescing en estado `failed`. `gate:local` verde (sólo falla ambiental pre-existente `suggestResponseKb`). **Diferido (calidad, no seguridad):** debounce 5-min real vía columna `run_after` (insert-once crea desde el primer mensaje → resumen más fino); contabilizar el costo de parse en el budget.

**2026-06-30 (WhatsApp flip — Fase 0: deployable + validado):** Seguimiento del flip de WhatsApp (abajo). Cerradas dos brechas que impedían pasarlo a prod profesionalmente: **(1) el flag no era desplegable** — `OMNI_WA_CANONICAL` faltaba en `.github/workflows/deploy-calc-api.yml` (que inyecta los `vars.OMNI_*` a Cloud Run), así que la repo Variable nunca llegaba al servicio; agregado junto a los otros `OMNI_*`. **(2) no había test contra DB real** del path canónico — `scripts/omni-local-e2e.mjs` ahora incluye un gate **WA-CANONICAL** (migración 011: CHECK acepta `wa_crm_sync` + rechaza bogus, índice parcial existe; `enqueueAiJob` coalesce un único `wa_crm_sync` pending por conversación; classify/suggest NO se coalescen → prueba que el índice está scopeado; integración `enqueueIngestAiJobs` con flag ON). El harness sigue **skippeando limpio** sin binarios Postgres (CI no lo corre; se ejecuta en staging/local con `npm run omni:local-e2e`). Nuevo runbook de cutover/rollback [`runbooks/wa-canonical-flip.md`](./runbooks/wa-canonical-flip.md) (precondiciones, soak 24h, flip, rollback instantáneo). **Pendiente operador:** setear repo Variable `OMNI_WA_CANONICAL` + resolver las 4 decisiones del runbook antes de pasar a ON. **Fase 2 (PRs separados):** outbound unification (un solo sender Graph API), convergencia read-model cockpit→`omni_*`, reconciliación de sugerencias (3 tonos vs HITL single-body).

**2026-06-30 (WhatsApp unificado — flip Omni canónico tras flag `OMNI_WA_CANONICAL`, ships dormant):** `branch claude/whatsapp-unified-interaction-0mdl68`. Hoy WhatsApp corre por **dos rutas en paralelo** en prod: la legacy (`server/index.js` → mapa en memoria `waConversations` + timer 5min → `processWaConversation` → Sheets CRM + `callAgentOnce` AF–AG + auto-learn) y Omni en **shadow** (`normalizeAndPersist` → `omni_messages` → jobs classify/suggest). Resultado: cada mensaje se procesa y llama a la IA **dos veces**, estado partido entre mapa y Postgres. Este cambio ejecuta el "flip" de ADR-009 detrás del flag nuevo **`omniWaCanonical`** (`OMNI_WA_CANONICAL`, default **OFF** → arranca dormido, webhook byte-idéntico). **Con ON:** el webhook hace `await normalizeAndPersist` como fuente de verdad; se apagan el mapa en memoria, el timer 5min, el trigger 🚀 y el `callAgentOnce` duplicado; los side-effects legacy (ingest CRM_Operativo + auto-learn) corren como **job durable `wa_crm_sync`** en la cola `omni_ai_jobs` (retries/dead-letter/budget/SKIP-LOCKED ya existentes) en vez de un subscriber frágil. **Coalescing** sin timer vía índice único parcial `(conversation_id) WHERE status='pending' AND job_type='wa_crm_sync'` (migración `011`) → un único job/escritura CRM por ráfaga de conversación; CRM_Operativo pasa a **upsert por teléfono** (decisión #3). AF–AG **retirado** en modo canónico (el cockpit lee `wa_suggestions`/`/api/crm/suggest-response`, no AF–AG; decisión #4). El espejo `wa_messages` sigue corriendo en ambos modos → el cockpit `/hub/wa` no se ve afectado por el toggle. **Nuevos:** `server/lib/wa/{crmIngestWrite,waCrmSyncJob,ingestMode}.js`, `server/migrations/omni/011_wa_crm_sync_job.sql`, tests `omniWaCanonicalFlag`/`waCrmSyncJob`/`waCrmIngestExtract` (en `test:core`). `gate:local`: lint + `test` verdes; única falla en `test:api` = `suggestResponseKb` (ambiental "OpenAI sin key", **pre-existente**, idéntica en `HEAD~6`). **Pendiente operador (bloquea pasar ON, no el merge dormido):** aplicar `npm run omni:migrate` (011) en prod; confirmar que ningún consumidor de Sheet/wolfboard depende del auto-relleno AF–AG WA; confirmar upsert-por-teléfono vs cómo esperan ver los leads WA en CRM; decidir si el botón 🚀 sigue haciendo falta. **Diferido (Fase 2, nombrado por honestidad):** convergencia del read-model del cockpit (`wa_messages`→`omni_*`), unificación de outbound (`buildWhatsAppText`/`whatsappOutbound`/`omni/outbound/waReply`), surface de `omni_suggestions` en el cockpit WA. Rollback = `OMNI_WA_CANONICAL=0` (sin redeploy de datos; dedup hace idempotente la ventana ON).

**2026-07-01 (Inbox AI-First · CodeQL "1 alerta persistente" — investigación exhaustiva, requiere acceso directo del dueño):** Después de la primera guessed-fix (4 rutas C2, entrada anterior), CI siguió reportando **exactamente "1 new alert, 1 high severity"** en 5 corridas más consecutivas — cada una después de un fix nuevo, evidente y verificado. Cronología completa (todas confirmadas verdes en tests, ninguna resolvió la alerta): (1) `omniWriteLimiter` en `POST /omni/conversations/:id/assist` (llama LLM + 2 queries, incluye el mismo `omni_ai_jobs` INSERT que **C1** de esta sesión modificó). (2) `omniReadLimiter` en `GET /omni/conversations/:id/messages` (3 queries incl. `conversationVisibleTo()`) y `GET /omni/conversations` (la lista principal del inbox, su predicado de team-isolation fue refactorizado a `appendTeamIsolationFilter()` en Wave 8, de esta misma sesión). (3) `omniWriteLimiter` en `POST /omni/ingest`, cuyo `normalizeAndPersist()` llama a `resolveContact()` — la función exacta que **C3** modificó (retry `ON CONFLICT`). (4) Un floor global: `router.use(omniReadLimiter)` montado antes de **todas** las rutas del archivo, garantizando cobertura al 100% sin importar cuál ruta específica esté trazando CodeQL. **Ninguna de las 5 movió la aguja.** Verificación exhaustiva vía `git diff $(git merge-base HEAD origin/main) HEAD -- server/` confirmó que `server/routes/omni.js` es el **único** archivo con rutas Express tocado por el diff real de este PR (18 archivos server/ en total, ninguno más define un router); con el floor global, **cada una de las 11 rutas tocadas por el diff tiene un rate limiter**, sin excepción. **Hallazgo clave, tardío:** de las 6 alertas originales, solo 4 tuvieron confirmación textual ("CodeQL / Missing rate limiting" en comentarios inline, todas `is_resolved:true` ahora) — la alerta persistente **nunca** posteó un comentario inline en ninguna de las 8+ corridas de CI desde que empezó esta sesión a rastrearla, ni antes ni después de los 6 commits de fix. Esa ausencia sistemática de anotación inline (a diferencia de las 4 que sí la tuvieron) sugiere que podría ser un tipo de alerta distinto — un "path problem" (p.ej. `js/resource-exhaustion`, evaluado en los logs de CI junto a `MissingRateLimiting.ql` bajo el mismo CWE-770, con fuente/sumidero potencialmente en archivos distintos) que la UI de comentarios inline de GitHub no renderiza igual que una alerta de una sola línea. **Herramientas agotadas sin éxito:** `get_check_run` (sin detalle de línea), `get_review_comments` (sin anotación nueva, 8+ veces), `WebFetch` directo a la tab de Security y a `/security/code-scanning/{id}` (404, auth-gated, para el dueño y para esta sesión), `get_job_logs` (sin volcado de resultados, solo timing de evaluación BQRS), `list_workflow_run_artifacts` (sin SARIF adjunto), llamada cruda a la API REST de anotaciones de check-run (403). **No existe** una herramienta MCP de GitHub que liste alertas de code-scanning directamente. Se intentó 2 veces preguntarle al dueño vía `AskUserQuestion` cómo proceder (chequear la tab de Security él mismo, que sí puede tener acceso; seguir investigando; o dejarlo) — ambos intentos fallaron con error de stream de la herramienta, sin llegar a mostrarse. **Decisión autónoma dado el bloqueo de herramienta:** se detiene el loop de fixes-a-ciegas — los 6 commits ya aplicados son mejoras de seguridad reales y verificadas (rate limiting genuino donde antes no había ninguno), no se revierten, pero no se seguirá adivinando sin evidencia directa. **Pendiente real:** el dueño debe abrir `https://github.com/matiasportugau-ui/Calculadora-BMC/security/code-scanning?query=pr%3A486+tool%3ACodeQL+is%3Aopen` directamente (tiene permisos de owner que esta sesión no tiene) y compartir la regla/archivo/línea exactos para un fix quirúrgico. **No bloquea nada más** en el PR — sigue en DRAFT, `gate:local` sigue verde salvo el flake preexistente ya documentado (`suggestResponseKb.test.js`).

**2026-07-01 (Inbox AI-First · fix CodeQL "missing rate limiting" — última alerta persistente):** CI reportó 6 alertas CodeQL en `server/routes/omni.js`; el owner fijó 4 en `3247a0d1` (`omniReadLimiter`/`omniWriteLimiter` en `/admin/overview`, `/contacts/duplicates`, `/contacts/merge`, `/actions/urgent` — confirmadas resueltas vía review-thread). **1 alerta persistió** en `f014b0bf` sin poder inspeccionarse directamente: tanto la tab de Security de GitHub como un `WebFetch` directo devolvieron 404 (vista auth-gated) para el owner y para esta sesión — no hay herramienta MCP de GitHub que liste el detalle SARIF del alert. Diagnóstico por `git diff <merge-base> HEAD -- server/routes/omni.js` en su lugar: el análisis en modo PR de CodeQL marca líneas de acceso a DB *nuevas* dentro del diff — no solo rutas enteramente nuevas. El fix de team-isolation `conversationVisibleTo()` (C2, de antes en la sesión) agregó una query Postgres nueva a 4 rutas preexistentes sin agregarles rate limiting: `POST /omni/conversations/:id/reply` (el endpoint real de envío a cliente), `PATCH /omni/conversations/:id`, `PATCH /omni/conversations/:id/read`, `POST /omni/conversations/:id/notes`. Agregado `omniWriteLimiter` (ya definido, 30 req/min) a las 4, mismo patrón/posición que en `/contacts/merge`. **Presentado como mejor evidencia disponible, no confirmado contra el alert exacto** — a esperar la próxima corrida de CodeQL sobre `4bfe8ab5` para confirmar. `node --check` OK; sin cambio de comportamiento del gate de aprobación humana para envío a cliente (el fix es solo rate limiting adicional sobre una ruta que ya estaba gateada).

**2026-06-30 (Inbox AI-First · merge `main` en PR #486 + fixes ground-truth/migrate):** Merge `origin/main` en `claude/inbox-ai-first-system-h39oyo` (conflictos resueltos en `PROJECT-STATE.md` + `package.json` — `test:core` une tests omni Waves 1–10 con `crmRowMapper.test.js` de #489). **`scripts/verify-prod-ground-truth.mjs`:** probe `email_ingest_log` usa `ingested_at` (no `created_at`). **`scripts/omni-migrate.mjs`:** `ON CONFLICT (name) DO NOTHING` al registrar migraciones (evita duplicado cuando el SQL self-inserta). Post-merge: `omni:migrate` aplicó `010`–`013` OK; `omni:ground-truth` Tier 2 confirma `012`/`013` queryables (0 filas esperado), email ledger 35 filas; gaps restantes: WA 0 msgs/24h, `quote_embeddings` pre-migración pgvector `0001`. PR #486 **MERGEABLE** (CI pendiente). `test:core` verde.

**2026-07-01 (Inbox AI-First · Wave 10 — tiered AI model routing, mecanismo dormant · sin migración):** Siguiente ítem del blueprint (5.3, control de costos IA) — confirmado por 2 pasadas de investigación (Explore + Plan agent) que **no requiere migración**: `omni_model_registry` ya está indexado por `task_key` string arbitrario sin CHECK constraint, así que un segundo tier es solo una fila nueva, no un cambio de schema. **Decisión de scope:** se construye el *mecanismo* de ruteo (dormant, sin cambio de comportamiento con el registry actual), NO la *política* (qué categorías van a qué tier) — esa es una decisión real de calidad/costo para respuestas customer-facing, misma categoría que el pivote de Wave 4 y la pausa de Wave 6b, dejada al dueño vía runbook nuevo en vez de decidirse sola. Nuevo `server/lib/omni/orchestrator/modelTiering.js` (`resolveSuggestTaskKeyCandidates(category)` puro + `resolveSuggestModel(pool, category)`, reusa `getEnabledModel()` sin modificarlo) — intenta `suggest:<category>` antes de caer al `suggest` base; con cero filas de tier sembradas (estado real actual) siempre resuelve al mismo `suggest` de siempre, **verificado por test** (0 queries extra cuando `category` es null). Cableado en `aiWorker.js` (`body_ai_category` agregado al SELECT del mensaje, reemplaza el `getEnabledModel(pool,"suggest")` directo, registra `model_task_key` en el output del job). **Caveat verificado, no asumido:** el orden entre los jobs `classify`/`suggest` no está garantizado — `suggest` puede correr antes de que `classify` estampe la categoría; el mecanismo degrada solo a la tier base en ese caso (documentado en el runbook, no una falla). Runbook nuevo `docs/team/runbooks/omni-ai-tiered-routing-enable.md` con el SQL de ejemplo para sembrar un tier + la decisión de política explícitamente marcada como del dueño. Test: `omniModelTiering` (8) en `test:core`. `test:core` + `omniAiWorker`/`omniHardening` verdes (sin regresión en el job `suggest` existente). Nota: `npm run lint` solo cubre `src/` — no valida `server/`, verificación es la cadena de tests.

**2026-07-01 (Inbox AI-First · Wave 9 — ground-truth doctor confirma migraciones 012/013 · sin migración nueva):** Owner está aplicando `011`/`012`/`013` en prod. En vez de encolar una 4ª migración especulativa (delivery ticks — `2.2` requeriría columna nueva `delivered_at`) antes de confirmar que las 3 pendientes aterrizaron, se cerró un hueco real en la propia herramienta de verificación: `scripts/verify-prod-ground-truth.mjs` Tier 2 no chequeaba las tablas nuevas de esta sesión (`omni_frt_breaches` de la `012`, `omni_contact_merge_log` de la `013`) — el owner iba a correr `omni:ground-truth` después de migrar y no habría tenido señal de si esas dos migraciones realmente aplicaron. Agregados 2 probes nuevos: **`0 filas es el estado esperado`** para ambas (worker `OMNI_FRT_WORKER_ENABLED` sigue en OFF; merges de contactos son acción manual ocasional) — `flowKey: null` + sin `GROUP BY` confirma tabla-existe-y-es-queryable sin marcar ⚠ por estar vacía. Runbook actualizado con la explicación + snapshot Tier 1 re-capturado (`gitSha` avanzó de `65df704c` a `492d19f7` — `main` desplegó de nuevo desde la última corrida; **WA sigue en 0 msgs/24h, aún sin explicar**, pendiente de revisión en la próxima corrida Tier 2 con `DATABASE_URL`).

**2026-06-30 (Inbox AI-First · Wave 8 — auto-review adversarial de las 7 waves del loop, 10 fixes reales):** Antes de seguir agregando superficie, pausa para auditar lo ya shippeado (8 agentes en paralelo, 8 ángulos: line-by-line, comportamiento eliminado, trazador cross-file, reuse, simplificación, eficiencia, altitud, convenciones CLAUDE.md). **Hallazgo más severo, confirmado contra el schema real:** `server/lib/omni/urgency.js` comparaba `conv.priority === "high"` pero `omni_conversations.priority` es **INTEGER** (no hay enum de strings en ningún lado del código) — el boost de +30 por prioridad **nunca disparaba en producción**, y mi propio test lo enmascaraba usando un fixture string en vez de datos realistas. Fix: comparación numérica (`Number(conv.priority) > 0`) + test corregido con enteros reales. **Otros 9 fixes reales:** (1) `OmniAdminCockpit.jsx` tragaba el error de `useOmniUrgentActions` y mostraba «bandeja al día 🎉» ante un fetch fallido — ahora distingue error de cola-vacía. (2) Deep-link a una conversación fuera del filtro/página activa dejaba el sidebar vacío aunque el hilo cargaba — `OmniInboxPanel` ahora cae a `useOmniMessages` directo cuando la lista no la tiene (sin fetch extra en el caso común). (3) `duplicateContacts.js` solo miraba `phone` O `wa_phone` (el que esté truthy primero) — un contacto con ambos campos distintos perdía la mitad de sus duplicados; ahora chequea los dos. (4) `scan_bounded` (límite de 5000 contactos) nunca se mostraba al operador — agregado banner. (5) `winnerId` podía quedar obsoleto tras un reload concurrente y tirar dentro del `window.confirm` — ahora se re-deriva si ya no está en el cluster. (6) El 503 de merge siempre culpaba "migración 013" aunque el `42P01` viniera de `omni_conversations`/`omni_deals` — ahora usa el mensaje real de Postgres. (7) `console.warn` en `kbBridge.js` violaba la regla CLAUDE.md de pino-only en `server/` — ahora recibe el logger del único caller real (`aiWorker.js`). (8) Código muerto en `rag.js` leyendo `m.cliente_nombre` después de que `sanitizeQuoteMetadata` ya lo había quitado del whitelist — eliminado + corregido el ejemplo del docstring que aún mostraba "Juan García". (9) El predicado de team-isolation estaba copy-pasteado una 3ª vez en `GET /omni/actions/urgent` — extraído a `server/lib/omni/teamIsolation.js` (`appendTeamIsolationFilter`), reusado en ambas rutas de lista. **2 comentarios defensivos agregados** en `contactMerge.js` (guard para futuras FKs a `omni_contacts`, aclaración de que el admin-gate es responsabilidad del caller). **1 hallazgo bajado a skip documentado:** inconsistencia `fmtAgo`/`timeAgo` entre paneles nuevos — no se tocó por riesgo de regresión visual en tablas preexistentes del cockpit. Tests nuevos/actualizados: `omniTeamIsolation` (4), `omniDuplicateContacts` (+1 caso), `omniUrgentActions` (corregido). `lint` 0 errores; `test:core` + `rag.test.js` (37) + chain omni de `test:api` + `build` verdes.

**2026-06-30 (Inbox AI-First · Wave 7 — UI de cockpit para duplicados + fix de re-aparición post-merge):** Cierra el loop de Wave 6 (igual que Wave 3 cerró el de Wave 1): nueva pestaña **«Duplicados»** en `/hub/canales` (`OmniDuplicateContactsPanel.jsx`, detrás de `VITE_OMNI_INBOX`) que lista los clusters de `GET /api/omni/contacts/duplicates`, muestra cada contacto (nombre/email/teléfono/conversaciones/antigüedad), sugiere un "ganador" (más historial, desempate por más antiguo) y — **solo si el usuario tiene grant `canales:admin`** (`useModuleGrants().has("canales","admin")`, el botón **no se muestra**, no solo se deshabilita, para usuarios sin permiso) — permite fusionar de a pares con `window.confirm()` previo (mismo patrón que las acciones destructivas de `TasksModule.jsx`). Clusters de 3+ contactos se fusionan de a pares, nunca en lote automático. **Bug atrapado antes de shippear la UI:** el contacto perdedor archivado por un merge conserva su propio email/teléfono (nunca se tocan), así que sin un filtro el mismo cluster ya resuelto volvía a aparecer en cada scan — agregado `AND co.properties->>'merged_into' IS NULL` a la query de `GET /omni/contacts/duplicates`. Nuevos hooks `useOmniDuplicateContacts`/`mergeOmniContacts` en `useOmniConversations.js` (mismo patrón `omniFetch` que el resto). `lint` 0 errores; `test:core` + `build` verdes.

**2026-06-30 (Inbox AI-First · Wave 6b — ejecución de merge de contactos, admin-only, aprobado por el dueño · migración nueva owner-run):** Wave 6a dejó la ejecución del merge sin construir, marcada explícitamente como la única decisión de esta corrida que requería consultar al dueño (cambia a qué cliente pertenece qué historial). **El dueño aprobó construirla** con el diseño verificado-seguro. Nuevo `server/migrations/omni/013_contact_merge_log.sql` (tabla de auditoría, **sin** `ON DELETE CASCADE` en sus FKs — actúa además como guard contra un futuro hard-delete accidental de un contacto con historial de merge) + `server/lib/omni/identity/contactMerge.js` (`mergeContacts()`: transacción `BEGIN`/`COMMIT`/`ROLLBACK` mirroring el patrón ya usado en `normalizer.js`, **lockea ambos contactos con `FOR UPDATE`**, repuntea `omni_conversations`/`omni_deals` al ganador, **nunca borra** al perdedor — solo lo archiva vía `properties.merged_into` — y registra cada merge en el log de auditoría; deliberadamente NO copia email/teléfono/wa_phone/ml_user_id del perdedor al ganador para evitar choques con las constraints `UNIQUE`) + `POST /omni/contacts/merge` (`requireGrant.admin("canales")` — el nivel más alto, acción manual de operador, nunca disparada por IA ni automática). Tests: `omniContactMerge` (5, mock de pool/cliente pg cubriendo happy-path, contacto inexistente, fallo a mitad de transacción — confirma `ROLLBACK` + `client.release()` siempre se ejecutan) en `test:api`. **Owner-run:** aplicar `013` vía `npm run omni:migrate` antes de usar el endpoint (degrada a 503 explicado si falta). `lint` 0 errores; `test:core` + chain omni de `test:api` verdes.

**2026-06-30 (Inbox AI-First · Wave 6a — detección de contactos duplicados, solo lectura · sin merge):** Antes de codear, un Explore agent mapeó el blast radius completo de un merge de contactos: `omni_contacts` tiene **CASCADE DELETE** hacia `omni_conversations` y `omni_deals` (y desde ahí, en cascada, mensajes/sugerencias/AI jobs/notas/breaches FRT) — un `DELETE` directo del contacto perdedor destruiría todo su historial. El patrón seguro confirmado es repuntar FKs dentro de una transacción + archivar (nunca borrar) el contacto perdedor con un `properties.merged_into`. **Decisión:** la ejecución del merge altera semántica real de negocio (a qué cliente pertenece qué historial) de forma difícil de deshacer en la práctica aunque técnicamente nada se borra — se documenta el diseño seguro pero **no se construye la ejecución sin antes consultar al dueño**. Lo que sí se shippea ahora, de bajo riesgo y solo lectura: `server/lib/omni/identity/duplicateContacts.js` (`findDuplicateClusters()`, helper puro que reusa `normalizeEmail`/`normalizeWaPhone` de `types.js` — los mismos campos no-únicos por los que `resolveContact()` NO puede deduplicar al ingest, ya que `integration_uuid`/`ml_user_id`/`wa_phone` son `UNIQUE` por diseño) + `GET /omni/contacts/duplicates` (RBAC `canales:read`, acotado a 5000 contactos, degrada pre-migración). Tests: `omniDuplicateContacts` (7) en `test:core`. `lint` 0 errores; `test:core` verde.

**2026-06-30 (Inbox AI-First · Wave 5 — worker historiador de breaches FRT, dormant, migración nueva owner-run):** Pivote tras verificar en código que **Wave 4 (badges de confianza) no tiene datos reales**: `omni_ai_jobs.confidence` nunca se completa para jobs `suggest` — `callAgentOnce()` no devuelve confianza y el `markJobCompleted` del job `suggest` no la pasa (siempre `NULL`); solo `classify` la setea (heurística 0.5/0.85, job distinto al que genera el borrador). Mostrar un badge alimentado por esa columna sería engañoso para el 100% de las sugerencias — mismo antipatrón que el hallazgo de vectores stub en RAG (Wave 1). La señal de confianza real (grounding RAG) ya existe vía el badge «📎 Basado en N cotizaciones similares» (Fase 2). Se descartó construir el badge falso. **Pivote a Wave 5:** `GET /omni/actions/urgent` ya calcula el breach de SLA **en vivo** por request (`scoreConversationUrgency`); falta un **historial persistido** para reportes (breaches/semana, duración promedio). Nuevo `server/migrations/omni/012_frt_breaches.sql` (tabla `omni_frt_breaches`, índice único parcial 1-breach-abierto-por-conversación), `server/lib/omni/orchestrator/frtBreachWorker.js` (worker en proceso, reusa `scoreConversationUrgency` — nunca puede divergir de lo que ve el operador en vivo; cierra breaches resueltos, registra nuevos, degrada sin crashear si falta la migración), flag `OMNI_FRT_WORKER_ENABLED` (default OFF) + `OMNI_FRT_WORKER_INTERVAL_MS` (default 5 min), cableado en `index.js` mismo patrón que `startOmniSnoozeWorker`/`startOmniAiWorker` (start condicional al flag + cleanup en shutdown). **Owner-run:** aplicar `012` vía `npm run omni:migrate`, luego `OMNI_FRT_WORKER_ENABLED=1`. Tests: `omniFrtBreachWorker` (6) en `test:api`. `lint` 0 errores; `test:core` + chain omni de `test:api` verdes.

**2026-06-30 (Inbox AI-First · Wave 3 — click-to-open desde la cola urgente · aditivo, sin backend):** Cierra el "diferido" de Wave 1: las filas de «🔥 Para responder ahora» (`OmniAdminCockpit`) ahora abren la conversación. Lift de estado en `CanalesModuleInner` (`pendingConversationId` + `jumpToConversation`, sin URL sync — fuera de alcance, no regresión) → `OmniAdminCockpit` recibe `onSelectConversation(id)` (filas ahora son `<button>`, no-op si no se pasa el prop) → `OmniInboxPanel` recibe `initialConversationId`/`onInitialConversationConsumed` y abre el hilo en un `useEffect` de un solo disparo (se limpia tras consumirlo para que un click normal a la pestaña no reabra el mismo hilo). Confirmado por exploración de código: el `id` de `GET /omni/actions/urgent` es el mismo UUID que `OmniThreadPanel`/`useOmniMessages` ya usan — no depende de filtros/paginación. Sin ruta nueva, sin migración, sin flag. `lint` 0 errores; `build` verde.

**2026-06-30 (Inbox AI-First · Wave 1 — ground-truth doctor + cola de acciones urgentes + merge `main` · aditivo/dormant):** Arranque del loop autónomo "verificar realidad + seguir el blueprint". **Merge `main`** (conflicto resuelto en `PROJECT-STATE.md` + `package.json`) → PR #486 vuelve a ser mergeable. **Track A (verificación):** nuevo `scripts/verify-prod-ground-truth.mjs` (npm `omni:ground-truth`), doctor **read-only** de 2 tiers — Tier 1 público sin creds (`/health`, `/capabilities`, `/api/wa/health`: prueba API viva + Sheets/tokens + volumen WA) y Tier 2 con `DATABASE_URL` (recencia/conteos sobre `omni_conversations/messages/ai_jobs/suggestions`, `public.email_ingest_log`, `quote_embeddings`). Runbook [`runbooks/VERIFY-PROD-GROUND-TRUTH.md`](./runbooks/VERIFY-PROD-GROUND-TRUTH.md) con **claim ledger doc-vs-realidad**. Snapshot real capturado: API sana en prod (`gitSha 65df704`, `hasTokens/hasSheets=true`), **WA 0 msgs/24h (⚠ gap)**. **Corrección verificada en código (refuta claims "NOT STARTED" de un agente de planificación):** el ingest **ML→Omni ya está cableado** (`index.js:550` webhook `topic=questions` → `syncUnansweredQuestions` → `shadowWriteMlQuestions` → `shadowPersist`, bajo `OMNI_ML_SHADOW_WRITE`), igual que el **logging de feedback de aprobación** (`omni.js` accept/reject → `recordOmniPromptEval`). No se reimplementó nada existente. **Track B (usabilidad):** nuevo `GET /omni/actions/urgent` — cola **rankeada "reply-zero"** ("¿qué atiendo AHORA?") que el cockpit no daba (sólo COUNTS, y email-only). Helper puro `server/lib/omni/urgency.js` (pondera espera-1ª-respuesta envejecida, **SLA por canal** wa 0.5h/ml 4h/email 2h, sin asignar, sin leer, prioridad) con razones legibles; endpoint read-only, RBAC `canales:read`, team-isolated (mismo predicado que la lista → C2 intacto), degrada a cola vacía pre-009. **UI:** panel «🔥 Para responder ahora» en el cockpit (`OmniAdminCockpit` + hook `useOmniUrgentActions`, detrás de `VITE_OMNI_INBOX`; muestra contacto/asunto + razones + edad, acento rojo si SLA vencido; click→Bandeja diferido por requerir lift de estado entre tabs). **Sin migración, sin flag nuevo, sin auto-send (gate humano intacto).** Tests: `omniUrgentActions` (12) en `test:core`; `lint` 0 errores; `test:core` + `build` verdes.

**2026-06-30 (Inbox AI-First · P4 + review fixes — provider tag en embeddings + paridad PII en chat RAG · dormant):** (a) **Review de PR #486** (auto-review + Copilot): `formatRetrievedContextForPrompt` (`rag.js`, la otra ruta de inyección RAG usada por el chat Panelin vivo + tool `recuperar_casos_similares`) ahora también pasa por `sanitizeQuoteMetadata` → la PII de clientes (incl. nombre) nunca llega a un LLM externo vía RAG (cambio de comportamiento: el chat ya no muestra el nombre del cliente en "casos similares"; sí fecha/panel/área/total/score). Comentario de `resolveContact` corregido (email no es unique key; dedup vía integration_uuid). (b) **P4:** `quote_embeddings` ahora etiqueta el `provider` de cada vector (migración root `0002` + `embedQuotes.js` `activeProvider()`); `npm run omni:rag-precheck` **rechaza** habilitar RAG si hay vectores stub (no-semánticos) o sin tag (pre-0002) — cierra el hueco donde un backfill sin key pasaba el pre-check con vectores basura. Runbook actualizado (aplicar `0002` por psql). Todo dormant (RAG OFF). Tests: `rag.test.js` 37, `omniRagGrounding` 8.

**2026-06-30 (Inbox AI-First · Hardening — 4 fixes verificados de auditoría · C1–C4):** Tras auditoría adversarial (correctness/seguridad/datos) **verificada contra el código vivo** (falsos positivos descartados: HMAC de webhooks cierra con 503, Chatwoot usa `timingSafeEqual`, sin SQLi, CSV guards presentes). **C4 (PII):** `quote_embeddings.metadata` guardaba el lead completo (cliente_nombre/teléfono/email/ubicación/vendedor) y `kbBridge.formatOmniContextBlock` lo inyectaba en prompts LLM → fuga de PII de clientes a proveedores externos. Nuevo `server/lib/quoteMetadata.js` (`sanitizeQuoteMetadata`, whitelist de hechos no-PII) aplicado en storage (`scripts/training/embedQuotes.js`) y en inyección (`kbBridge`, defense-in-depth). RAG dormant → sin re-embed. **C1 (budget):** `POST /omni/conversations/:id/assist` insertaba `omni_ai_jobs(job_type='assist')` pero el CHECK de migración 002 sólo permitía classify/suggest/extract_deal/embed → el INSERT fallaba silencioso (`.catch`) y el gasto de assist nunca contaba al `omniAiDailyBudgetUsd`. Migración idempotente `011_ai_job_type_assist.sql` + `ALLOWED_AI_JOB_TYPES`. **C2 (seguridad — team isolation):** `POST /reply` (despacha al cliente), `PATCH /:id` (status/asignación/snooze), `PATCH /:id/read` y `POST /:id/notes` no llamaban `conversationVisibleTo()` (sus hermanos `/assist`, `/messages`, GET `/notes` sí) → un operador `canales:write` podía responder/mutar la conversación de otro equipo adivinando el UUID. Guard agregado a los 4 (admins ven todo; `team_id NULL` queda visible para todos → despliegues single-team intactos; sólo bloquea acceso cross-team real). **C3 (concurrencia):** `resolveContact` hacía SELECT-luego-INSERT sin `ON CONFLICT` → dos mensajes concurrentes del mismo contacto → violación de unique → transacción abortada → **mensaje entrante perdido en silencio**. Ahora `ON CONFLICT DO NOTHING` + re-resolución del contacto creado por la otra transacción. Tests offline nuevos/actualizados: `tests/omniResolveContact.test.js` (3), `omniRagGrounding` (8), `omniAiWorker` (5), `omniHardening` (10). `lint` limpio (0 errores); `test:core` verde. Migraciones/flags las aplica el dueño. _Issues potenciales documentados para más tarde (P1–P8): race de budget multi-instancia, dedupe-key Gmail/IMAP, divergencia shadow-write + AG–AK para el flip de SoT, tag de provider en embeddings._

**2026-06-30 (Inbox AI-First · Fase 2 — badge de citas RAG en la sugerencia Omni · UI):** La mayor parte del full-desk del Omni Inbox ya estaba implementada (asignación + snooze vía `PATCH /api/omni/conversations/:id` con `buildConversationPatch`, notas `useOmniNotes`/`omni_notes`, accept/reject de sugerencias, composer con `Usar`→prefill, cockpit admin con SLA/FRT/colas en `OmniAdminCockpit`). El gap real era que las **citas de grounding de Fase 1** (`omni_suggestions.metadata.grounding`) no se mostraban. Nuevo helper puro `groundingLabel()` en `panels/omniFormat.js` + badge «📎 Basado en N cotizaciones similares» bajo la sugerencia en `OmniThreadPanel.jsx` (sólo cuando `grounded`). Test en `tests/omniInbox.test.js` (15 checks). Detrás de `VITE_OMNI_INBOX`; sin cambios de backend. (También cubre parcialmente el "surface confidence/grounded badges" de Fase 5.)

**2026-06-30 (Inbox AI-First · Fase 1 — citas RAG en sugerencias Omni + pre-check de embeddings · dormant):** Continuación del blueprint. La investigación confirmó que el núcleo de la Fase 1 ya estaba implementado: el job `suggest` de `server/lib/omni/orchestrator/aiWorker.js` ya pasa por `callAgentOnce()` ("un cerebro") y ya inyecta casos RAG vía `buildOmniRetrievalContext`/`formatOmniContextBlock` (`kbBridge.js`) cuando `config.ragEnabled`. **Gap cerrado (citas):** los casos RAG llegaban al prompt pero **no se registraban como citas** en `omni_suggestions`. Nuevo helper puro `buildSuggestionMetadata(result, prompt, retrieval)` agrega `grounding:{rag_case_ids, rag_count, snippet_count, grounded}` al metadata (aditivo; con `RAG_ENABLED=false` el texto de la sugerencia es byte-idéntico y `grounded:false`). **Pre-check de embeddings:** nuevo `isSemanticEmbeddingAvailable()` en `embeddings.js`; `kbBridge.js` ahora **saltea RAG** (warn una vez `omni_rag_skipped_stub_embeddings`) si `RAG_ENABLED` está on sin key de embeddings usable → nunca aterriza sobre vectores stub no-semánticos. Nuevo `scripts/omni-rag-precheck.mjs` (npm `omni:rag-precheck`) valida tabla `quote_embeddings` poblada + provider semántico + retrieval de muestra antes de habilitar. Test offline `tests/omniRagGrounding.test.js` (7 checks) en `test:core`. Runbook de habilitación (gated por humano): [`runbooks/omni-ai-orchestrator-rag-enable.md`](./runbooks/omni-ai-orchestrator-rag-enable.md). **Todo dormant detrás de flags existentes** (`RAG_ENABLED`/`OMNI_AI_ORCHESTRATOR_ENABLED`/`OMNI_EVENT_BUS_ENABLED` default OFF). Rollback = togglear flags. La migración pgvector root `migrations/0001_*.sql` se aplica vía `psql` (no la cubre `omni:migrate`).

**2026-06-30 (docs — Blueprint Inbox AI-First + captura de prompt maestro · solo documentación):** Investigación y consolidación del sistema **Inbox AI-first** a partir del desarrollo real + research del dueño (síntesis Email→CRM, prompts maestros V1→V5 "Company-Knowledge-First", benchmark OSS Chatwoot/Inbox Zero/Mail0/FreeScout vs Missive/Front/Fin). **Hallazgo central:** ya existe ~80% del inbox AI-first (cerebro `agentCore`, backbone `omni_*`, pipeline Email→CRM en prod, UI estilo Chatwoot Fases 1+2); el trabajo es **consolidación**, no greenfield. **Decisión arquitectónica registrada:** Postgres `omni_*` = fuente de verdad operativa; Sheets `CRM_Operativo` = espejo editable por negocio. Nuevos docs (sin cambios de código): [`INBOX-AI-FIRST-BLUEPRINT.md`](./INBOX-AI-FIRST-BLUEPRINT.md) (arquitectura "un cerebro, un inbox, una SoT" + roadmap por fases 0–5 con semáforo/rollback, **supersede** `OMNI-HUB-ARCHITECTURE.md`), [`EMAIL-SOURCE-MAP.md`](./EMAIL-SOURCE-MAP.md) (índice canónico verificado de archivos/endpoints/scripts/env/tablas del canal Email/Omni), [`docs/prompts/BMC_EMAIL_PANELSIM_COMPANY_KNOWLEDGE_FIRST_AGENT_PROMPT_V5.md`](../prompts/BMC_EMAIL_PANELSIM_COMPANY_KNOWLEDGE_FIRST_AGENT_PROMPT_V5.md) (prompt maestro V5 verbatim + variante compacta) y regla `.cursor/rules/bmc-email-company-knowledge-first.mdc`. Todas las rutas citadas verificadas contra el repo (commit `73bf15b`); huecos marcados `#ZonaDesconocida`. Sin impacto en prod.


**2026-06-30 (chore — `voiceSupport.js` sacado de `src/utils/` para evitar redeploys espurios de Cloud Run):** PR #523 (`/panelin/live`) disparó un redeploy real e innecesario de la API en Cloud Run pese a no tocar `server/`. Causa raíz: `deploy-calc-api.yml` trata `src/utils/` como ruta "relevante" para la API (7 archivos de `server/` importan de ahí — el motor de cálculo puro compartido) y ese PR había agregado `src/utils/voiceSupport.js` (dos helpers de detección de navegador, `isBrowserSupported`/`isSafari`, sin relevancia de servidor) — el filtro de paths funcionó **correctamente** según su regla documentada, no es un bug de CI. **Fix:** movido `src/utils/voiceSupport.js` → `src/hooks/voiceSupport.js` (junto a sus únicos consumidores: `useHandsFreeVoice.js`, `useVoiceSession.js`, `usePanelinCharacterVoice.js`), actualizados los 2 imports (`PanelinVoicePanel.jsx`, `PanelinLivePage.jsx`). Solo relocación de archivo, sin cambios de lógica. `gate:local` y `vite build` verdes.

**2026-06-30 (nuevo — capa federada de acceso a KB por rol, `kbAccess()`):** Hasta ahora existían **cuatro** almacenes de conocimiento desconectados, todos escopeados solo por "surface" de cliente (chat/ML/WA/email/wolfboard), nunca por "rol" de agente: **Training KB** (`trainingKB.js`, JSON, token-overlap), **Brain KB** (`brainKB.js`, GCS `lessons.json`, políticas globales sin scoping), **RAG de cotizaciones** (`rag.js` + `quote_embeddings`, pgvector real) y los **archivos de conocimiento por rol** (`docs/team/knowledge/<rol>.md` + `PROJECT-STATE.md`), estos últimos leídos manualmente por los agentes de Claude Code, nunca embebidos ni buscados. **Diseño: federación, no consolidación** — ningún store existente se migra ni se toca; se agrega **una tabla nueva y aislada**, `team_kb_embeddings` (mismo shape que `quote_embeddings`: pgvector 1536-dim, ivfflat/cosine, más columna `domain`), vía [`kb-package/migrations/0001_team_kb_embeddings.sql`](../../kb-package/migrations/0001_team_kb_embeddings.sql) + [`scripts/run-kb-migrations.mjs`](../../scripts/run-kb-migrations.mjs) (`npm run kb:migrate`, mismo patrón que traktime/wa/transportista). **Ingesta** manual (`npm run kb:ingest-team-docs`, [`scripts/kb-ingest-team-docs.mjs`](../../scripts/kb-ingest-team-docs.mjs)): chunkea `docs/team/knowledge/*.md` y las entradas fechadas de "Cambios recientes" de este mismo archivo, embebe vía el `embedText()` ya provider-agnostic existente, upsert idempotente por `content_hash`. **Nuevo módulo** [`server/lib/kbAccess.js`](../../server/lib/kbAccess.js) + [`server/lib/kbDomains.js`](../../server/lib/kbDomains.js): perfil de acceso por rol (roles de cliente mantienen el comportamiento exacto de hoy — training KB + brain, sin team docs; roles internos como `bmc-security`/`bmc-calc-specialist` obtienen sus dominios de team-docs + `project_state` siempre, sin ruido de Q&A de cliente), degrada sin excepción si la tabla no está migrada o falta `OPENAI_API_KEY`/`DATABASE_URL`. Verificado: `agentChat.js` **no** tenía el bug de surface hardcodeado que se sospechaba inicialmente (`tests/agentChatSurfaceWiring.test.js` ya lo cubre) — sin cambios ahí. Tests nuevos `tests/kbAccess.test.js` (35/35, agregado a `test:kb`); `npm test` completo (todas las suites) y `npm run lint` verdes. **Fuera de alcance por diseño:** sin write-back de agentes (requeriría gate de aprobación como `training-kb.json`), sin re-ingesta automática, sin wiring al todavía-no-resuelto "dev-agent" (hilo separado de la ventana privilegiada de Panelín). Branch `claude/kb-access-layer`.

**2026-06-30 (fix API — 404 fantasma `/vad/silero_vad_legacy.onnx` → 204):** Los logs de prod mostraban `GET /vad/silero_vad_legacy.onnx -> 404` (y un `ort-wasm-*` acompañante) atribuidos a un modelo VAD faltante del "agente animado". **Investigación (2 pasadas Explore + historial git + grep directo):** ese archivo es el asset por defecto de **`@ricky0123/vad-web`**, que **nunca** estuvo en `package.json`/`package-lock.json`/`node_modules` ni en ningún commit; no hay `.onnx` ni `/public/vad/` en el repo; los únicos `vad` en `src/` son palabras en español (`desactivado`, `Activada`). La voz / "agente animado" usa **OpenAI Realtime** (`turn_detection: server_vad`, server-side) en `useVoiceSession.js` + **Web Speech API** en `useHandsFreeVoice.js` — ningún modelo local. **Conclusión:** 404 fantasma de un cliente con **service worker / caché viejo** (no lo pide ningún código actual). **Fix mínimo (sin dependencia ni binario):** [`server/index.js`](../../server/index.js) responde `204 No Content` a cualquier `/vad/...` (espeja el guard existente de `/favicon.ico`; ruta `RegExp` para no romper path-to-regexp de Express 5), acotado a `/vad/` — el catch-all 404 real queda intacto. Runbook nuevo [`docs/VAD-PHANTOM-404.md`](../VAD-PHANTOM-404.md) (causa raíz + remediación cliente: desregistrar SW / limpiar site data). Verificación: `npm run lint` + `npm run test:api` verdes; `curl /vad/silero_vad_legacy.onnx` → 204, `/api/inexistente` → 404. Branch `claude/vad-model-404-error-h5ue0a`.

**2026-07-01 (fix — Wolfboard batch: override.model de Haiku era ignorado):** Follow-up a PR #524. Los dos `callAgentOnce` de `server/routes/wolfboard.js` pasaban `override: { model: HAIKU_MODEL }` **sin** `provider`, y `callAgentOnce` solo honra `override.model` en el proveedor que matchea `override.provider` (con `eff.provider=null`, `usingOverrideModel` nunca era true) → el batch corría en el **default `claude-opus-4-7`** en vez de Haiku (regresión de costo, latente porque Wolfboard está gated OFF bajo canales-only). Fix: `override: { provider: "claude", model: HAIKU_MODEL, ... }` en ambas llamadas — Haiku aplica en el intento claude, el fallback grok/gemini/openai usa sus defaults. Detectado por Copilot + Codex en la review del PR. Corregido también el conteo "5 rutas"→"6 rutas" en la entrada anterior.

**2026-06-30 (feat — AI Assistant Control Plane: health + fallback de dos capas + master switch, ship canales-only):** Se agregó un **plano de control** para los ~7 asistentes IA (Panelin chat, ML, WhatsApp, Email Agent, Omni/canales copilot, Wolfboard batch, sugerencias WA/ML). **Registry único** (`server/lib/assistantRegistry.js`) como fuente de verdad: cada asistente declara `enabled`, un probe de dependencias, y un `fallbackTo` que termina en el **seam compartido** (`callAgentOnce`, que ya hacía fallback de proveedor claude→grok→gemini→openai). `dispatchAssistant()` camina la cadena de fallback ante error/unhealthy; `isAssistantEnabled()` lee el **master switch** `ASSISTANTS_ACTIVE` (env, default `canales`). **Gate** (`server/middleware/requireAssistantEnabled.js`) montado **solo en las 6 rutas de generación IA** (`/api/agent/chat`, `/api/email-agent/chat`, `/api/wa/suggestions/run`, `/api/wa/quotes/run`, `/api/crm/suggest-response`, `/api/wolfboard/quote-batch`) devolviendo `503 assistant_disabled`; **ingest/webhooks quedan sin gate** (los asistentes deshabilitados siguen *recibiendo* — cero mensajes perdidos, solo dejan de *responder*). `omniRouter` (canales) **nunca** se gate-a → no se puede apagar por accidente. **Health monitor** (`server/lib/assistantHealth.js`, cache 30s, disponibilidad = key-presence, `?deep=1` fuerza) expuesto en `GET /api/assistants/status(/:key)` (`server/routes/assistantsStatus.js`, admin-gated) y en el panel `/hub/admin/assistants` (`AssistantsStatusPanel.jsx`, badges live/degraded/disabled/down + proveedor sirviendo). **Fallback de proveedor extendido** a los 2 canales que lo bypassaban: Email drafter + Wolfboard batch ahora pasan por `callAgentOnce` con la **nueva opción `bareSystemPrompt`** (usa su prompt propio, sin la persona Panelin); el tool-loop de Email (tool-use Anthropic) degrada a borrador plano vía seam si Anthropic cae; Wolfboard relaja el guard "solo ANTHROPIC_API_KEY" a "cualquier proveedor". **Ship canales-only:** con `ASSISTANTS_ACTIVE=canales` (default), solo el copilot de `/hub/canales` responde; el resto da 503; ampliar el set es una sola env var. Test `tests/assistantControlPlane.test.js` (22 asserts, en `test:api`). `npm run gate:local` verde. **Pendiente:** setear `ASSISTANTS_ACTIVE=canales` en Doppler `bmc-backend/prd` + GCP Secret Manager `chatbot-bmc-live` (sin la var, el default ya es canales-only); ampliar el set por fases.

**2026-06-30 (feat — `/panelin/live`, personaje animado full-screen sobre la infra de voz ya segura):** Tras la review live de `GPT-PANELIN-Final` (WS sin auth, proxy abierto — ver entrada anterior), en vez de parchear esa app externa se construyó el personaje **dentro de este repo**, reusando la plumbing de voz **ya autenticada y rate-limited** que ya existía sin UI (`server/routes/agentVoice.js` — sin cambios). **Nuevo:** `src/hooks/usePanelinCharacterVoice.js` (deriva emoción — neutral/listening/thinking/speaking/happy — de las señales que ya expone `useVoiceSession`), `src/components/PanelinLiveCharacter.jsx` (canvas, sprites reusados del review anterior en `public/panelin-character/`), `src/components/PanelinLivePage.jsx` (página full-screen, gesture-gate, status pill), ruta `/panelin/live` en `App.jsx` (detrás de `RequireGrant module="calc" minLevel="write"`, mismo requisito que ya tenía el endpoint). **Extendido (aditivo):** `useVoiceSession.js` ahora expone `remoteVuLevel` (analyser sobre el audio remoto del asistente vía `pc.ontrack`, antes solo existía para el mic local) — hook no tenía consumidores previos, cero riesgo de regresión. **Hallazgo durante verificación manual (screenshot vs headless Chromium):** los sprites `eye_L`/`eye_R`/`mouth.png` **no corresponden a partes aisladas** — `screen_bg.png` ya trae la cara completa horneada (cap+cuerpo+ojos+boca); animar `mouth.png` como boca independiente distorsionaba contenido de ojos. Simplificado a v1 honesto: partes estáticas + glow de color por emoción + anillo al escuchar, en vez de lip-sync/blink falsos sobre sprites mal identificados. `npm run gate:local` verde, `vite build` verde (`PanelinLivePage` es su propio chunk lazy, 12KB gzip).

**2026-06-30 (Writers CRM restantes — WA migrado + ML sanitizado + mapa canónico):** Continuación del contrato key-based (PR #489) a los otros dos writers de `CRM_Operativo`. **WhatsApp** (`processWaConversation`, `server/index.js`): los 3 writes posicionales (`B:K`/`R:T`/`V:W`) → **un** write `B:W` header-anchored vía `crmRowMapper` (lee cabeceras fila 3) + un write `AF:AK` (respuesta sugerida + provider + gate defaults), **sanitiza** cada celda + `ai.respuesta` (CSV/formula guard, antes ausente). Como el path es fire-and-forget y borra su buffer al terminar (sin reintento), valida ventanas `B:W`/`AF:AK` **warn-only** y **siempre escribe best-effort** (fallback a letras documentadas si la cabecera falta) — nunca pierde el lead por un skip. **ML-sync** (`syncUnansweredQuestions`, `server/ml-crm-sync.js`): se mantiene la fila densa posicional (15+ columnas ML-specific con cabeceras sin verificar — literal fijo de 36 celdas B:AK), pero ahora **sanitiza** todas las celdas antes de escribir — cierra el gap de inyección de fórmulas. **Contrato:** `CRM_WRITE_CONTRACT` suma `respuestaSugerida`→AF. **Consolidación (finding #4):** nuevo `docs/team/CRM-OPERATIVO-COLUMN-MAP.md` (mapa canónico de columnas: letra·índice·cabecera·key·BMC key·writers/readers) + comentarios `@see` en las 4 fuentes (`crmRowParse.js`, `crmOperativoLayout.js`, `crmRowMapper.js`, `CRM_TO_BMC`); se difiere la unificación de código (el read-side `parseCrmRowAtoAK` tiene 9 consumidores sin tests). Test `tests/crmRowMapper.test.js` (49 asserts). **Diferido:** migración header-anchored de ML-sync (requiere confirmar ~15 cabeceras contra la planilla real).

**2026-06-30 (Contrato de escritura CRM key-based — anti column-shift):** Endurecido el contrato de datos entre la app y `CRM_Operativo` para que un campo faltante (ej. `Teléfono`) **nunca** desplace las columnas siguientes. **Nuevo** `server/lib/crmRowMapper.js` (single source of truth): `buildCrmRow()` ancla cada valor por **nombre de cabecera** (fila 3, accent/case-insensitive) con fallback a la **letra de columna documentada**, y rellena ausentes con `""` (jamás omite la clave); `validateCrmRow()` rechaza filas estructuralmente inválidas (cabeceras ausentes/angostas) antes de escribir; `sliceCrmRange()` extrae la ventana A1. **Wiring:** `POST /api/crm/ingest-email` (`bmcDashboard.js`) ahora hace **un** write `B:W` header-anchored (+ `AG:AK`) en vez de 3 writes posicionales, **sanitiza** cada celda (CSV/formula guard, antes ausente) y **degrada con gracia** → si la estructura no valida, no escribe la fila; intenta capturar el lead en el omni shadow store (`OMNI_EMAIL_SHADOW_WRITE`) y **solo marca idempotencia si la captura tuvo éxito** — si no (shadow off por default, o falla), el lead queda **reintentable** (se re-ingesta al corregir la estructura), nunca se descarta en silencio. `server/lib/crmAppend.js` (`appendQuoteToCrm`) reemplaza el array posicional `B:AK` hand-counted por el mapper (lee cabeceras, fallback a letras si falla). **Schema:** `docs/openapi-email-gpt.yaml` define `CrmLead` (todas las claves requeridas, vacío = `""`, no se omiten) + respuesta tipada (`degraded`/`degradeReason`). **Gobernanza:** `.cursor/agents/sim-reviewer-agent.md` y `MATPROMT-RUN-THEME-SIM-2026-03-23.md` suman gate de validación estructural del payload antes de ejecutar escritura. Test `tests/crmRowMapper.test.js` en `test:core`. Cero regresión: `quoteDualWrite`/`agentTools`/`sheetsCsvGuard` verdes. **Hardening post-review (PR #489):** `validateCrmRow()` ahora chequea **pertenencia a la ventana de escritura** (`window: B:W` email / `B:AK` cotización) — un campo cuya cabecera derivó **fuera** del slice contiguo ya no se descarta en silencio: email degrada a "Pendiente", `appendQuoteToCrm` aborta con `{ok:false}`, WA loguea warn (best-effort). `validateCrmRow()` también rechaza **columnas resueltas duplicadas** (dos keys al mismo índice — ej. `Teléfono` borrado y `Ubicación` cae en D — evita sobrescritura silenciosa). Logging de `messageId` (input del cliente) sanitizado contra log-injection (CodeQL). Logging en `crmAppend.js` migrado a `pino` (convención `server/`).

**2026-06-30 (Respondamos Rapido — chat panel + tutorial UX):** `BmcChatPanel.jsx` renombrado a **Respondamos Rapido**: pestaña fija abajo a la derecha (estilo launcher), health dot, panel con header/cerrar, iframe a `localhost:3000` en dev y Cloud Run en prod. Ya no exige login para mostrar el launcher (auth se postea al iframe si hay token). **Tutorial:** cierre con `dismissTutorial` descarta overlay + botón flotante por sesión (`sessionStorage`); fix `z-index` del cartel centrado; default del botón tutorial desplazado para no tapar la pestaña del chat.

**2026-06-30 (fix — respuestas IA de Gemini truncadas a media frase en `/api/crm/suggest-response`):** Bug en vivo en `/hub/cotizaciones`: filas "Generadas por gemini" quedaban con la respuesta cortada (p. ej. "Buenas noches. Con gusto" y nada más) ante consultas que requerían una cotización completa. Causa raíz: `server/lib/agentCore.js` (`callAgentOnce`, el path compartido detrás de `suggest-response`/CRM/WA) abre la rama Gemini sin `thinkingConfig:{thinkingBudget:0}` — `gemini-2.5-flash` "piensa" por defecto y ese razonamiento oculto consume el presupuesto de `maxOutputTokens` (120 para canal `ml`, 400 para `wa`) antes de emitir texto visible, cortando la respuesta a mitad de frase. El mismo gotcha ya estaba documentado y resuelto en `server/routes/agentChat.js:969-975` (PR #472, Gemini function-calling) pero nunca se propagó a `agentCore.js`. **Fix:** un `generationConfig.thinkingConfig:{thinkingBudget:0}` en la rama Gemini de `callAgentOnce` (este path no usa tools, así que el thinking no aporta nada — solo arriesga truncar). Diff de 8 líneas, sin tests nuevos (no hay infra de mock del SDK Gemini en este módulo, ver `tests/suggestResponseKb.test.js`); verificado con `npm run gate:local` (lint + test + test:api) verde.

**2026-06-30 (Review live — Panelin "Interactive Character" en Cloud Run):** Revisión live (behavioral + seguridad + UX) del avatar de voz Panelin en `https://panelin-q74zutv7dq-ue.a.run.app` (servicio **FastAPI** aparte, no la API Express de este repo; fuente probable `matiasportugau-ui/GPT-PANELIN-Final`, **no** clonada). Informe: [`reports/REVIEW-PANELIN-CHARACTER-LIVE-2026-06-30.md`](./reports/REVIEW-PANELIN-CHARACTER-LIVE-2026-06-30.md) (+ screenshots desktop/mobile). **Hallazgos HIGH:** (1) WebSocket de voz `/ws` **sin auth** ni rate-limit — `session_id` lo genera el cliente, saluda con audio TTS al conectar (greeting cacheado/estático), y cualquier visitante anónimo puede mantener turnos de conversación con costo LLM/TTS; (2) proxy `/api/proxy/{path}` **abierto y sin auth** hacia la API BMC de producción — **no es allowlist** (reenvía rutas arbitrarias) y filtra diagnósticos internos (`hasTokens`, `hasSheets`, nombres de tabs CRM). **MEDIUM:** `/api/kb` expone el system-state interno a anónimos; el WS **no valida `Origin`** (CSWSH). **LOW:** `/health` no refleja salud de dependencias. Recomendaciones: token efímero + rate-limit en `/ws`, validar `Origin`, allowlist estricta en el proxy, depurar `/api/kb`, readiness real en `/health`. Sin tocar código del app (review live-only); fixes quedarían como follow-up sobre `GPT-PANELIN-Final`.

**2026-06-30 (fix API — rutas Sheets devuelven 503, no 500, ante fallo de Sheets · #493):** 16 handlers `catch` Sheets-backed en [`server/routes/bmcDashboard.js`](../../server/routes/bmcDashboard.js) respondían `500` cuando el backend de Google Sheets fallaba, violando el contrato del frontend (**503** = Sheets caído, **200**+vacío = sin datos, **nunca 500**). Convertidos 15 al helper existente `sheetsUnavailable(res, e.message)` (`/productos-maestro` read/reconcile/push, `/actualizar-precios-calculadora`, `/matriz/push-pricing-overrides`, y `/crm/cockpit/*` (excepto `send-approved`) + `/consultations` + colas ML/WA/unified). **Dejados en 500 a propósito** (no son fallos de Sheets): `/productos-maestro/links` GET+PUT (JSON local), `/email/poll-gmail` (Gmail API), `/email/panelsim-summary` (lectura IMAP local), `/crm/cockpit/send-approved` (ruta mixta envío+Sheets — no enmascarar un fallo de envío WA/ML como 503), `/crm/cockpit/sync-ml` (domina el fallo del cliente ML). Verificación: `test:api` → `crmCockpitDualAuth` 10/10, `sheetsCsvGuard` 20/0; diff quirúrgico de 16 líneas. Cierra #493.

**2026-06-30 (Market Intelligence — rediseño del tab `/hub/marketing`):** `MarketingHubModule.jsx` reescrito como módulo **skineable** (`SkinProvider` + `.adminCot data-skin`, nueva skin **`intel-dark`** zinc/teal) y **tabbeado**: **Resumen** (KPI strip real · **Matriz de productos** · **Estrategias activas** · chat **Market Intel AI**), **Inteligencia** (mapa de 31 competidores por tier · auditoría Meta Ads · pulso MercadoLibre, todo desde la investigación ya capturada) y **Detalle** (widgets salvados: brief AI, deltas, alertas, mystery shopping). **Backend (aditivo, `requireMarketing` admin):** `GET /api/marketing/intel` ({competitors, ads, ml} desde los loaders), `GET /api/marketing/product-matrix` (baseline BMC vs referencia ponderada por tier + Δ% + posición) y `POST /api/marketing/ai/chat` (SSE, inyecta todo el contexto de mercado vía `callAgentOnce`). Lógica de brecha de precio extraída a `server/lib/marketIntel/priceGap.js` (`buildAnalisisPrecios` + `buildProductMatrix`) y reusada por `strategicBrief.js`. Componentes nuevos token-driven en `src/components/marketing-hub/` (ProductMatrix, StrategyCards, IntelPanel, MarketIntelChat) + seed `data/strategies.json`. `npm run lint` (0 errores), `npm test` (OK) y `vite build` verdes. Branch `claude/market-intel-tab-lrijwt`.

**2026-06-30 (Presupuesto libre aditivo en cualquier escenario):** Las líneas manuales del formato "Presupuesto libre" (paneles, perfilería, tornillería/herrajes, selladores, extraordinarios) ahora pueden agregarse **sobre cualquier escenario** (Solo Techo, Solo Fachada, Techo+Fachada, Cámara), sumándose al BOM y al total — antes solo existían en el escenario dedicado `presupuesto_libre`. **Implementación (sin tocar el motor):** nuevo helper puro `mergeLibreGroups(baseGroups, libreGroups)` en `src/utils/helpers.js` (fusiona por título anexando los ítems libres al final, preservando los `lineId`/overrides del escenario); nuevo memo `additiveLibreGroups` en `PanelinCalculadoraV3_backup.jsx` que corre `computePresupuestoLibreCatalogo` con **`flete:0`** (evita doble conteo: el flete lo maneja el paso del wizard) y se fusiona en el memo `groups` → `grandTotal`, tabla BOM, PDF, TSV y snapshots lo recogen solos. **UI:** la sección libre pasó de estar gateada por `scenarioDef?.isLibre` a un `<details>` plegable siempre visible ("Agregar productos manuales (Presupuesto libre)"; abierto en el escenario dedicado, colapsado como aditivo); el sub-acordeón Servicios/Flete queda solo en el escenario dedicado. Escenario `presupuesto_libre` sin regresión (early-return `[]`). Tests: nueva SUITE 16c en `tests/validation.js` (flete:0 sin SERVICIOS, fusión por título, estabilidad de lineId, aditividad del total, no-op con inputs vacíos). `node tests/validation.js` → 421/421, `vite build` y `npm run lint` verdes.

**2026-06-29 (Agente IA — Gemini ejecuta la calculadora + interpretación de zonas + hardening):** Tres PRs encadenados, **todos live en prod** (Cloud Run `panelin-calc`, última rev `00630-zct`). **(1) PR #472 — Gemini function-calling:** con Claude sin saldo el fallback de Gemini solo *narraba* `<tool_code>` (precios inventados); ahora ejecuta las tools reales. Nuevo `server/lib/geminiTools.js` (adapta `AGENT_TOOLS` → `functionDeclarations`: dropea `default`/`format`/`pattern`, colapsa `type:["string","number"]`→escalar+`nullable`, omite `parameters` en tools sin args) + loop de 8 rondas en el branch gemini de `agentChat.js` (reusa `executeTool`, mismas auth-gates y emits que Claude). **Gotcha clave:** `gemini-2.5-flash` "piensa" por defecto y eso lo hace narrar en vez de ejecutar → se pasa `thinkingConfig.thinkingBudget:0`. Test `tests/geminiTools.test.js` (137 asserts) en `test:agent`. **(2) PR #475 — interpretación cantidad→zonas:** "N paneles de L m" se leía como 1 zona (misquote). Nueva "REGLA DURA — Geometría de zonas" en `buildSystemPrompt()` (`chatPrompts.js`): zona `ancho` = ancho TOTAL del techo; "N paneles" → `ancho = N×ancho_útil` (ISOROOF 1.0m, ISODEC 1.12m); "techo de 6×4m" se usa tal cual. **(3) PR #478 — hardening post-review:** abort-checks en el loop gemini (no quema rondas ni ejecuta tools con side-effects si el cliente se desconecta), log de observabilidad si ronda-0 no trae functionCalls, scope **SOLO TECHO** (pared usa `perimetro` no `zonas`), ejemplo ISODEC, self-check apretado. **Verificado live:** "4 paneles de 5m" → 4 paneles/20 m²/USD 1702.68; frase ambigua "5m×1m" ahora **confirma la interpretación** antes de cotizar (decisión tunable: confirmar=seguro vs auto-asumir=rápido, quedó en confirmar). Invariante registrado: **toda superficie IA cotiza solo vía la calculadora nativa, nunca de cabeza** (ya enforced en el toolsBlock). Handoff: [`HANDOFF-2026-06-29-gemini-tools-quote-interp.md`](./HANDOFF-2026-06-29-gemini-tools-quote-interp.md).

**2026-06-29 (Market Intelligence — auth fix + DB migrations aplicadas):**
`/hub/marketing` devolvía 401 a usuarios autenticados por usar `requireAuth` (solo token de servicio) + leer `bmc_cockpit_token` de localStorage. **Backend:** `requireAuth` → `requireServiceOrUser({ role: 'admin' })` en todas las rutas de `server/routes/marketing.js`. **Frontend:** `MarketingHubModule.jsx` usa `useBmcAuth().accessToken` en vez de localStorage (mismo patrón que el resto del hub). **DB:** 8 migraciones `bmc_market_intel` aplicadas a prod vía `npm run migrate:market-intel`. El módulo ahora funciona para usuarios con rol admin/superadmin.

**2026-06-26 (Hotfix — `drive_config_unavailable` 503 en tab Drive):** Causa: tabla Postgres `identity.user_drive_config` **no existía en prod** (código desplegado 2026-06-24 pero migración pendiente). Aplicada idempotentemente `supabase/migrations/20260624000001_user_drive_config.sql` contra prod `DATABASE_URL` (verificado: tabla presente). **Nuevos:** `npm run identity:golive:apply`, `npm run identity:drive-config:check`, `scripts/check-drive-config-table.mjs` (paso 7 en `pre-deploy-check.sh`), mensaje operador en español vía `formatDriveConfigError()` en `driveConfigApi.js` / `DriveFolderConfig.jsx`. UAT: usuario autenticado debe poder elegir carpeta sin 503.

**2026-06-26 (Chatwoot shared inbox + AI lead extraction + in-app Email Agent — code shipped dormant):** `branch feat/chatwoot-email-agent` (PR #450). Buzón compartido para todos los operadores logueados vía **Chatwoot CE** (off-stack, MIT) + extracción AI de leads + nuevo **Asistente de Correos BMC** (segundo agente en el panel derecho de la calculadora: redacta, edita, responde, reporta, reusa plantillas, tría y organiza correos). **Nuevo backend:** `server/lib/chatwootClient.js` (REST), `server/lib/emailLeadIngest.js` (extracción Zod+AI Gateway, reusa el prompt de `/api/crm/ingest-email`), `server/routes/chatwoot.js` (`POST /api/chatwoot/webhook` secret-verificado, async, dedupe por message.id), `server/lib/emailAgentTools.js` (11 tools `email_*`; send/assign con `user_confirmed`), `server/routes/emailAgentChat.js` (`POST /api/email-agent/chat` SSE, auth `canales:write`). **Frontend:** `src/components/EmailAgentPanel.jsx` (panel derecho, flag `VITE_FEATURE_EMAIL_AGENT`, default OFF). Config `chatwoot*` en `config.js`; placeholders en `.env.example`. **Boot-safe:** con `CHATWOOT_*` sin setear las rutas devuelven 503 y la app arranca normal → merge ships dormant. `gate:local` verde. **Pending [H] infra (runbook [`runbooks/chatwoot-email-agent-setup.md`](./runbooks/chatwoot-email-agent-setup.md)):** levantar Chatwoot CE (VM+docker), verificar SMTP `s111.nty.uy:465`/Gmail app-pass, IMAP del buzón ventas, generar `CHATWOOT_API_TOKEN`/ids/`CHATWOOT_WEBHOOK_SECRET` (admin UI) → Doppler `bmc-backend/prd` + GCP Secret Manager, wire webhook, `VITE_FEATURE_EMAIL_AGENT=true` en Vercel. Investigación OSS (Inbox Zero AGPL+comercial, Zero/Mail0 MIT pero personal, FreeScout AGPL, Helper repo ya privado) → Chatwoot elegido como único shared-inbox real OSS.

**2026-06-26 (BMC Chat Gemini Sheets — DONE · PR #447):** Merge squash a `main` (`a35f4bf`). Express `/chat` en `panelin-calc` (`bmcChat.js`, Gemini+Sheets, UI estática); `BmcChatPanel.jsx` (iframe default → Cloud Run `bmc-chat-642127786762.us-central1.run.app`; Express opt-in `VITE_BMC_CHAT_LOCAL=1`); Panelin empty-state cleanup. **Verificado prod:** `GET panelin-calc/chat` → 200 HTML; `bmc-chat` → 200; Vercel → 200; `npm run smoke:prod` 9/9 OK; CI `deploy-calc-api` + `deploy-vercel` success. **UAT pendiente [H]:** login → botón 💬 flotante → iframe carga consultas Admin. Handoff: [`HANDOFF-2026-06-25-0744.md`](./HANDOFF-2026-06-25-0744.md).

**2026-06-25 (operator email — thin slice: unattended ingest + reply-from-cockpit):** `branch feat/email-thin-slice → PR #442`. Reuses the ~80%-built email path instead of building a custom inbox UI. (a) **Reply**: `handleCrmCockpitSendApproved` now handles `origen=Email` — resolves recipient + receiving casilla from new `public.email_ingest_log`, sends via `server/lib/emailReply.js` (per-casilla SMTP from `accounts.json`, reusing existing `EMAIL_<CASILLA>_PASS`), threads with In-Reply-To, stamps `Enviado el`. (b) **Idempotent ingest**: `POST /api/crm/ingest-email` dedupes by `messageId` (migration `20260625000001_email_ingest_log.sql`) so an unattended runner won't write dup leads. Coexists with the new omni shadow-write. Tests: `tests/emailReply.test.js` (16) wired into `test:core`; `validation.js` 399/0; `test:api` 0 fail. **Pending [H]:** apply migration (Supabase MCP), verify `s111.nty.uy:465` SMTP, create `EMAIL_INGEST_TOKEN`, deploy Cloud Run Job + Scheduler — see [`runbooks/email-cloud-run-poller.md`](./runbooks/email-cloud-run-poller.md). Custom `/hub/bandeja` UI deferred (master prompt at `~/goal-prompt-email-admin-operators.md`).

**2026-06-25 (cerebro IA centralizado — lessons brain → system prompt de Panelin, GOING LIVE):** `PR #439`. Wire del cerebro auto-evolutivo (lessons humanas verificadas, `gs://bmc-ml-tokens/bmc-brain/lessons.json`, mismo store que la sheet-quote pipeline) al system prompt del agente: una sola costura `buildSystemPrompt()` → los 5 canales (chat, voz, WhatsApp, MercadoLibre, CRM-suggest) inyectan el mismo bloque `CONOCIMIENTO ACUMULADO`. Unifica 3 fuentes de conocimiento antes desconectadas (pipeline, KB calc-bmc, panel chat-web). **Nuevo** [`server/lib/brainKB.js`](../../server/lib/brainKB.js): cache GCS (ADC, espejo de `trainingKB.js`) o `BRAIN_LOCAL_PATH` (dev), `brainBlock(query)` **sync read-only** (ranking confidence·0.6+overlap·0.4), fail-soft a `""`. Flag `brainEnabled` (`VITE_FEATURE_BRAIN`) en `config.js`, default OFF. **Con flag OFF la salida de `buildSystemPrompt()` es byte-idéntica** (verificado por diff vs HEAD) → merge ships dormant. **Go-live (decisión del dueño):** merge #439 + `VITE_FEATURE_BRAIN=true` en Cloud Run `panelin-calc` (SA `panelin-runner` ya tiene read en `bmc-ml-tokens`). `gate:local` verde. ⚠️ Revertir = togglear el flag a OFF (sin redeploy).

**2026-06-25 (ML catálogo + /hub/ml-manager fixes):** Auditoría de catálogo ML (46 act + 197 paus; 132 bulk-paused 2026-04-11; 85 con `moderation_penalty`) → `product-clips/out/ml-audit-179969104.{md,json}` + reconciliación Bromyros↔ML `product-clips/out/bromyros-ml-gap.csv` (29 paneles: 13 act/5 paus/11 missing). **Reactivadas** ISP150 (`MLU445010304`) + IF40 (`MLU444372549`, 194 vend.) → activas **46→48**; ISP50/200/250 **retenidas** (penalizadas — requieren fix de calidad antes de reactivar). **Bug raíz /hub/ml-manager:** `vercel.json` no proxeaba `/ml` (catch-all SPA devolvía HTML 200 → `JSON.parse` rompía todo); agregado rewrite `/ml/:path*`→Cloud Run + CORS. **QuestionsTab:** corregido pull (`.questions` no `.results`) + agregado botón "Generar con IA" (reusa `/api/crm/suggest-response`/agentCore). **OverviewTab:** contador preguntas (`.total` no `.paging.total`). **ListingsTab:** filas con título/estado/precio/stock/salud + pausar/activar inline (guardrail anti-penalizadas). Pendiente: listar IsoFrig 60–200mm (checklist en `docs/team/ML-ISOFRIG-LISTING-CHECKLIST.md`; bloqueado por falta de endpoint create + fotos). gate:local OK (lint+test+test:api+build). **IA generación — RESUELTO (PR #433):** el endpoint devolvía `"All providers failed"` porque la cadena `claude→openai→grok→gemini` caía hasta gemini pero estaba fijada al modelo **retirado `gemini-2.0-flash`** ("no longer available"). Fix: `aiProviderConfig.js` → **`gemini-2.5-flash`** (verificado vivo con la key actual). **Verificado en prod:** `POST /api/crm/suggest-response` → `{ok:true, provider:"gemini", model:"gemini-2.5-flash"}` — el botón "Generar con IA" ahora genera. claude(crédito)/openai(quota)/grok(key) siguen muertos pero la cadena los saltea (fail-fast); recargarlos es upgrade opcional de calidad (claude es preferido si tiene saldo). Runbook: `docs/team/ML-AI-KEYS-REMEDIATION.md`. **Deploy:** PR #431 (dashboard, Vercel) + PR #433 (IA fix, Cloud Run) merged a `main`.

**2026-06-25 (CI — channels_pipeline solo en push a `main`):** El job `Channels — automated pipeline (prod)` de [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) quedó acotado a `push` sobre `main`, igual que otros probes de salud de producción (`smoke`, `voice_health`). **Motivo:** en PRs estaba ejecutando `scripts/channels-automated-pipeline.mjs` contra la revisión ya desplegada en Cloud Run y podía quedar rojo por un incidente de prod ajeno al branch, marcando la corrida como `action_required`. El cambio mantiene la señal de salud en `main` sin contaminar la validación de pull requests.

**2026-06-25 (feat — Omni Inbox UX estilo Chatwoot · Fases 1+2):** Rediseño del Omni Inbox (`/hub/canales` → Omni) tomando patrones UX de Chatwoot sin adoptar la plataforma, detrás del flag existente `VITE_OMNI_INBOX=1`. **Fase 1 (frontend):** tabs de estado (Todas/Abiertas/Pendientes/Pospuestas/Resueltas vía el filtro `?status=` que ya existía), filas ricas (avatar, chip de canal, timestamp relativo, contador, dot de no-leídos), búsqueda client-side, composer con Enter-envía/Shift+Enter-salto y respuestas rápidas `/` (seed + localStorage), timestamps por mensaje, sidebar de contacto en acordeón. Restyle con tokens `--ac-*`. **Fase 2 (backend fino, sin migración):** nuevo `PATCH /api/omni/conversations/:id` (`{status?,tags?,priority?}`) que reutiliza columnas + validación (`ALLOWED_CONVERSATION_STATUSES`) ya existentes; `GET /api/omni/conversations` ahora devuelve `tags` + `unread_count`. Botones **Resolver/Posponer/Reabrir** (header del thread) y **etiquetas add/remove** (sidebar) cableados vía `updateConversation()` en `useOmniConversations`. Validación extraída a `server/lib/omni/conversationPatch.js` (+ `conversationStatus.js` como fuente única dependency-free). Archivos: `server/routes/omni.js`, `src/components/hub/canales/panels/{OmniInboxPanel,OmniThreadPanel,OmniContactSidebar}.jsx` + nuevos `omniFormat.js`, `cannedReplies.js`, `omniInbox.css`; tests `tests/omniInbox.test.js` (14) + `tests/omniConversationPatch.test.js` (8), wired a `test:api`. **Pendiente (full-desk tier, requiere migración):** asignación, notas internas, delivery ticks, real-time SSE. _Deploy: backend (Cloud Run `panelin-calc`) antes que frontend (Vercel) para que los botones no peguen a un 404. Gate ESLint/vitest/build corre en CI (fresh npm ci); en el worktree solo se validaron los helpers con `node`._

**2026-06-25 (docs — Arquitectura de Canales & Cotizaciones):** Estudio arquitectónico y de specs de los 5 módulos operativos `/hub/{canales,wa,ml-manager,ml,cotizaciones}` (componentes de entrada, rutas backend, modelos de datos, RBAC, estado, cross-links), validado contra el código vivo. Nuevo doc de referencia [`ARCHITECTURE-CHANNELS-COTIZACIONES.md`](./ARCHITECTURE-CHANNELS-COTIZACIONES.md). Sin cambios de comportamiento en prod.

**2026-06-24 (Configurador de Carpeta Drive por usuario — tab "Drive"):** Cada usuario interno configura, una sola vez,
la carpeta de Google Drive donde se guardan sus cotizaciones (resuelve "Sandra/Ramiro/Martín no pueden guardar"). El
"Guardar" del panel Drive ahora hace **doble guardado**: (1) primario **client-side por usuario** (`saveQuotation` con su
propio token `drive.file`) hacia la carpeta configurada, y (2) best-effort en la carpeta BMC compartida vía service account
(`DRIVE_QUOTE_FOLDER_ID`) para el dataset consolidado de Fase 2. Selección de carpeta vía **navegador in-app con la Drive API**
(sin API key extra): lista/crea carpetas que la app administra en el Drive del usuario (limitación del scope `drive.file`: no
enumera carpetas pre-existentes arbitrarias — para eso haría falta Google Picker + API key). Validación de permiso de escritura
client-side y persistencia en **Postgres** `identity.user_drive_config` (no Firestore). Sin carpeta configurada → se bloquea el
guardado por usuario con aviso. **Nuevos:** migración `supabase/migrations/20260624000001_user_drive_config.sql` (aplicar con
`scripts/identity-golive-apply.sh`), ruta `server/routes/driveConfig.js` (`GET`/`POST /api/drive/config`),
`src/utils/driveConfigApi.js`, `src/components/DriveFolderConfig.jsx`, test `tests/drive-config-routes.test.js`. ~~**Setup pendiente:** aplicar la migración contra `DATABASE_URL`.~~ **Migración prod aplicada 2026-06-26** (ver entrada hotfix arriba). Fase 2 (vista consolidada "todo de todos") fuera de alcance.

**2026-06-25 (CI/Deploy — consolidación de PRs por variable reservada `PORT` en Cloud Run):** Cloud Run
**reserva** e inyecta `PORT`; setearla explícitamente en el step de deploy hacía fallar el despliegue. El bug
(introducido en `40f3a65`, `PORT=8080`) ya estaba **corregido en `main` por `0937d10`** («remove PORT=8080 from
Cloud Run env vars»). Los PRs **#399** y su duplicado **#401** re-proponían el mismo arreglo: se consolidó
**cerrando #401** y **mergeando #399**, cuyo squash (`8049bc7`) quedó **vacío** (no-op) porque el fix ya estaba
aplicado. Verificado en `main`: el bloque `env_vars` del step de deploy **no** incluye la reservada `PORT`
(sólo `SMTP_PORT`, que no es reservada). **Primer caso de uso real del tablero ágil** (`AGILE.md`): la disciplina
WIP/dedupe atrapó dos PRs redundantes para un fix que ya vivía en `main` — la tarjeta se cierra como
consolidación verificada, no como cambio de código.

**2026-06-24 (Tablero ágil / Kanban para organizar el desarrollo):** Marco de organización del trabajo de
**desarrollo de software** sobre GitHub (flujo continuo Kanban, sin sprints ni story points). Agregados:
[`AGILE.md`](./AGILE.md) (reglas: columnas `Backlog → Ready → In Progress → In Review → Done` con límites WIP,
labels `type`/`priority`/`area`, DoD = `gate:local` verde + merge a `main`, mapeo `/nxt`→prioridades y `area:*`→agente),
[`BACKLOG.md`](./BACKLOG.md) (espejo versionado), y plantillas `.github/ISSUE_TEMPLATE/` (feature/bug/tech-debt) +
`PULL_REQUEST_TEMPLATE.md`. Backlog inicial sembrado como issues **#416–#420** (npm audit, go-live, E2E D1.x,
ML re-auth, Omni pricing) — mapean a los ítems abiertos de «Pendientes de sincronización». Setup único pendiente en
la UI de GitHub (labels + Project board «BMC Dev»), documentado en `AGILE.md` §7: el tooling MCP crea issues pero no
labels ni Projects v2. `/nxt` ahora referencia el backlog del tablero.

**2026-06-23 (Omni WAVE 3+4 — FULLY OPERATIONAL en prod):** Activado el omnicanal end-to-end en producción y
**probado** (ingest → classify → suggest Claude → HITL accept → H4 eval). Cloud Run `panelin-calc` rev ≥`00532`
con **todos** los flags `OMNI_*`=1 (WA/ML/EMAIL shadow, event bus, AI orchestrator, automation; budget USD 50/día),
gobernados por **GitHub repo Variables** (sobreviven a deploys vía `deploy-calc-api.yml`). UI: `VITE_OMNI_INBOX`+`VITE_OMNI_DEALS`=1
→ pestañas Omni Inbox + Pipeline Deals en `/hub/canales`. Migración `005` siembra el AI registry (model+prompt enabled, claude-sonnet-4-6).
Un E2E controlado en prod descubrió y arregló **3 bugs** (PR #413 + migraciones 006/007): (1) `omni_conversations.properties`
faltaba en prod (001 viejo) → todo ingest/shadow-write fallaba en silencio; (2) CHECK `body_ai_category_valid` rechazaba
`cotizacion` → classify moría; (3) pg devuelve NUMERIC como string → `temperature`="0.30" → proveedores 400/422, sin
sugerencias (fix: cast a float8 en `aiRegistry.getEnabledModel`). Las 3 compuertas WAVE 4 pasan (HITL/F3/H4). PRs: #406, #407, #411, #413.
Pendiente menor: confirmar modelo/pricing + cuota OpenAI (ajustable sin código); backfills históricos ML/EMAIL difer (falta BMC_SHEET_ID en Doppler).

**2026-06-23 (hotfix — GCS token store usa metadata-server, no la SA key de Sheets):** Todas las rutas `/ml/*`
devolvían **500** (y `/auth/ml/status` 503): `server/tokenStore.js` hacía `new Storage()`, pero google-auth resuelve
`GOOGLE_APPLICATION_CREDENTIALS` primero — y esa env apunta a la SA key de Sheets/Drive (montada para el feature de
Drive del 2026-06-22), cuyo intercambio JWT en `googleapis.com/oauth2/v4/token` falla con *"Premature close"*. Fix:
`new Storage({ authClient: new GoogleAuth({ authClient: new Compute() }) })` — fija la identidad del runtime de Cloud
Run (`panelin-runner`, con `objectAdmin` en `gs://bmc-ml-tokens`) vía metadata server SOLO para el cliente GCS,
sin tocar la auth de Sheets/Drive. Verificado el API contra storage@5 + google-auth@9 (cachedCredential = Compute).
El comentario previo en el código documentaba la intención pero `new Storage()` nunca la lograba. Sin esto, ML Manager
muestra "Verificá la conexión con Mercado Libre".

**2026-06-23 (ML Manager re-cableado al backend live `panelin-calc`):** El esqueleto de `/hub/ml-manager`
(shipped en PR #403) apuntaba a un *connector* separado inexistente (`VITE_ML_CONNECTOR_URL`) y a 13 endpoints
fantasma (ads, analytics, message-packs, visits, ai/daily-brief). Re-cableado a la realidad: `mlFetch.js` ahora
usa `getCalcApiBase()` (mismo backend que la calculadora, sin API key — auth OAuth server-side); `useMlConnector.js`
reescrito a los 9 endpoints reales (`/auth/ml/status`, `/ml/users/me`, `/ml/listings`, `/ml/items/:id`,
**PATCH `/ml/items/:id`**, **POST `/ml/items/:id/description`**, `/ml/questions`, POST answer, `/ml/orders`).
Tabs reducidas a las respaldadas por backend: **Resumen / Publicaciones / Preguntas / Pedidos** (borradas Ads/Analítica/
Mensajes/Envíos). La tab **Publicaciones** edita precio, stock, estado, **fotos** (vía `pictures:[{source:url}]` — ML
descarga la URL) y descripción, con confirmación de dos pasos antes de escribir en vivo a MercadoLibre. Badge de
conexión corregido a `status.ok`. La conexión OAuth ya existe y es persistente: token cifrado en
`gs://bmc-ml-tokens` (GCS, sobrevive cold starts de Cloud Run), scope incluye `write`/`publish-sync`, último refresh
2026-05-29 (recuperable). Verificado: ESLint limpio + `vite build` OK. Pendiente: si el refresh del token dormido
falla, re-autorizar una vez vía `/auth/ml/start`. Clips de video NO disponibles por API para cuenta MLU doméstica.

**2026-06-23 (Omni WAVE 4 local E2E gate — PR #407):** Agregado `npm run omni:local-e2e` — un gate E2E
autoprovisionado (`scripts/omni-local-e2e.sh` + `scripts/omni-local-e2e.mjs`) que levanta su propio
Postgres descartable, aplica migraciones omni y verifica los 3 *code paths* de las compuertas WAVE 4
(HITL accept/reject, H4 eval stats, F3 reconcile drift<10) con 14 asserts, luego destruye el cluster.
Triple-guardado (`OMNI_E2E_DATABASE_URL` dedicado, host local, nombre con `e2e`, distinto de `DATABASE_URL`)
para que **nunca** toque una DB real. Opt-in en `wave4-exit-gate` vía `OMNI_E2E=1` (CI no lo corre).
Validado además el camino real HTTP+auth+route (7/7) contra schema identity + JWT local. Runbook de las 3
compuertas de staging documentado en [`orientation/OMNI-STAGING-ROLLOUT.md`](./orientation/OMNI-STAGING-ROLLOUT.md).
Las compuertas siguen pendientes de **datos reales de staging**, no de código.

**2026-06-23 (Omni WAVE 3 hardening fold-in — PR #407):** Plegados a `feat/omni-wave4` los fixes de hardening diferidos del review de PR #406, todos sobre archivos que WAVE 4 ya tocaba: (1) **bug** doble-incremento de `attempts` en `aiWorker.js` (ahora se incrementa una sola vez por claim, consistente entre worker batch y ad-hoc); (2) allowlist de `job_type` a nivel app (`ALLOWED_AI_JOB_TYPES`, 400 antes de enqueue); (3) `system_prompt` retirado del contrato HTTP `getActivePromptContract`; (4) `trigger_event` validado por `z.enum`; (5) whitelist de `status` de conversación en `set_conversation_status`; (6) guard ReDoS (cap de longitud) en operador `matches` de automation; (7) `/omni/health` deja de exponer flags de existencia de tablas. Nuevo test offline `tests/omniHardening.test.js` (10 asserts) + ReDoS assert en `omniAutomationConditions`; registrado en `test:core` y `wave4-exit-gate`. `gate:local` 0 lint / tests OK, `wave4:exit-gate` `ready_for_wave5: true`. Todo flag-gated OFF + operator-auth — sin impacto en prod. Pendiente humano: 3 checks de staging + quitar `do-not-merge`.

**2026-06-22 (Omni issue-and-fix — PR #406 Bugbot):** `/issue-and-fix` sobre `feat/omni-wave3`: emit `message.ingested` post-commit (`OMNI_EVENT_BUS_ENABLED`); lookup conversación solo por `(contact_id, channel, channel_conversation_id)`; reply API pasa `contact_id` en hint; reglas automation con `{}` no matchean; registry `system_prompt` aplicado en jobs `suggest` vía `agentCore.systemPrompt`. WAVE 4 WIP quedó en stash local (no incluido). Staging soak + flags: [`OMNI-STAGING-ROLLOUT.md`](./orientation/OMNI-STAGING-ROLLOUT.md).

**2026-06-22 (Omni WAVE 3 ship procedure — PR #406):** Rama `feat/omni-wave3` pusheada; PR [#406](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/406) abierta (4 commits: issue-and-fix + WAVE 3 + PDF docs + bootstrap config import fix). Gates locales: `gate:local` OK, `wave3:exit-gate` OK (`ready_for_wave4`), `test:omni:parity` OK, `omni:migrate` OK (001–003), `smoke:prod` OK. Staging/prod rollout doc: [`orientation/OMNI-STAGING-ROLLOUT.md`](./orientation/OMNI-STAGING-ROLLOUT.md). **Pendiente operador:** merge PR → WAVE 2 soak 24h/canal en staging → WAVE 3 flags → prod deploy con `OMNI_*` OFF.

**2026-06-22 (Procedimiento pre-PR local):** Checklist canónico [`PROCEDIMIENTO-PRE-PR-LOCAL.md`](./PROCEDIMIENTO-PRE-PR-LOCAL.md) — Issue-and-Fix (`/issue-and-fix`) + `gate:local` + split commits + pre-deploy opcional; enlazado en `SESSION-WORKSPACE-CRM` §1.4, `EXPERT-DEV-TRACEABILITY` paso 3.5, `docs/team/AGENTS.md` y `docs/AGENTS.md`.

**2026-06-22 (Omni WAVE 3 — Intelligence + Platform, 4 squads paralelos):** Migration `002_ai_automation.sql` (`omni_ai_jobs`, `omni_suggestions`, registries, `omni_automation_*`). **Squad AI E1–E4:** `aiWorker.js` + `startOmniAiWorker`, classify/suggest on `message.ingested`, `POST /api/internal/omni/ai/run`. **Automation F1–F4:** CRUD + simulate routes, `automationEngine.js`, `omni:migrate-wa-rules`. **Observability K1–K4:** `trace_id` en normalizer, `GET /api/omni/metrics`, `smoke:omni`, OTel hooks (`OTEL_ENABLED`). **Reliability M2–M4:** `omni:reconcile-channels`, `test:omni:ml-parity`, `wave3:exit-gate`. Flags: `OMNI_AI_ORCHESTRATOR_ENABLED`, `OMNI_AUTOMATION_ENABLED` (default OFF). Doc: [`docs/transformation/wave-3/`](../transformation/wave-3/README.md).

**2026-06-22 (Production ship — calculator UX + ML Manager + Drive + Omni):** `merged PR #403 → main`. Bundle consolidado en rama `feat/ml-manager-integrated` (7 commits squash-merged). **Calculator:** wizard edge nav sticky prev/next en todos los breakpoints; paso `bordes` ya no bloquea avance; PDF/WA requiere datos proyecto (teléfono, dirección, etc.); gate identidad sigue OFF. **Hub:** ML Manager en `/hub/ml` y `/hub/ml-manager`. **Drive:** auto-archivo PDF + `.bmc.json` vía `POST /api/quotes/drive-archive` (best-effort, `DRIVE_QUOTE_FOLDER_ID`). **Omni Core WAVE 1+2:** `/api/omni/*` + shadow adapters — flags `OMNI_*_SHADOW_WRITE` default OFF. **Gates:** `gate:local:full` OK local; `smoke:prod` OK pre-merge (MATRIZ CSV, suggest-response). **Deploy:** CI Vercel preview + merge trigger; verificar prod post-deploy en calculadora-bmc.vercel.app y Cloud Run `panelin-calc`.

**2026-06-22 (Omni WAVE 1+2 — runtime Omni Core + 4 squads paralelos):** Implementado `server/lib/omni/` (A1 DDL `npm run omni:migrate`, A2 identity, A3 `normalizeAndPersist` + Zod types), `GET /api/omni/health`, inbox API D1–D3 (`/api/omni/conversations`, messages, read, reply). Shadow adapters: WA webhook + extension (B1–B2), ML sync + send-approved mirror (C1/C3), email ingest (E1). Backfills: `omni:backfill-wa`, `omni:backfill-ml-crm`, `omni:backfill-email-crm`. Flags en `config.js` (`OMNI_*_SHADOW_WRITE`, default OFF). Tests: `omniTypes`, `omniIdentity`, `omniDb`, `test:omni:parity`. Doc: [`docs/transformation/21-wave-execution.md`](../transformation/21-wave-execution.md).

**2026-06-22 (OmniCRM Transformation Program V2 — paquete de diseño completo):** Publicado el paquete canónico de transformación a AI-Native OmniCRM en [`docs/transformation/`](../transformation/README.md): 20 documentos (executive summary, target state, domain/event/identity models, AI governance, automation, deals, security, observability, testing, migration, PR roadmap tracks A–H, risks, metrics, ownership, debt, evolution, build-vs-buy, ops readiness) + 10 ADRs (`adrs/ADR-001` … `ADR-010`) + [`SELF-CRITIQUE.md`](../transformation/SELF-CRITIQUE.md). Basado en discovery audit SHA `d04a7f4` ([`docs/discovery/`](../discovery/README.md)). **Solo diseño — sin código runtime.** Decisiones clave: evolucionar `/hub/canales` (ADR-010), shadow→flip migration (ADR-009), reuse agentCore (ADR-004), Sheets-first money 90d (ADR-006). Próximo paso implementación: Track A1 (`omni:migrate`).

**2026-06-22 (auto-archivo presupuestos → Drive BMC compartido):** Cada exportación de presupuesto (PDF cliente, PDF enriquecido, **Exportar presupuesto** con contador) archiva automáticamente **PDF + `.bmc.json`** en la carpeta compartida `DRIVE_QUOTE_FOLDER_ID` vía service account (`POST /api/quotes/drive-archive`). Estructura: `cliente → código cotización → archivos`. El botón manual **Guardar en Drive** usa la misma carpeta empresa (ya no el Drive personal OAuth del vendedor). Best-effort: la descarga local no se bloquea si Drive falla. Archivos: [`server/lib/driveUpload.js`](../../server/lib/driveUpload.js), [`server/routes/quoteDriveArchive.js`](../../server/routes/quoteDriveArchive.js), [`src/utils/companyDriveArchive.js`](../../src/utils/companyDriveArchive.js), [`PanelinCalculadoraV3_backup.jsx`](../../src/components/PanelinCalculadoraV3_backup.jsx).

**2026-06-21 (Google login prod — guardrails tokeninfo_aud_mismatch):** Verificado alineamiento prod: bundle Vercel `App-*.js` + Cloud Run `GOOGLE_OAUTH_CLIENT_ID` = client web `642127786762-hbkkonaqp9vvfk2qa9sv5go4bd8u4sj3`. **Endurecimiento:** smoke post-deploy en [`.github/workflows/deploy-vercel.yml`](../../.github/workflows/deploy-vercel.yml) falla si el Client ID no está incrustado en el bundle (detecta `VITE_GOOGLE_CLIENT_ID` vacío en build). [`.env.example`](../../.env.example): `VITE_GOOGLE_CLIENT_ID` y `GOOGLE_OAUTH_CLIENT_ID` unificados al mismo client sj3 con nota de paridad obligatoria.

**2026-06-20 (auth prod fixes — API_AUTH_TOKEN Cloud Run + VITE_GOOGLE_CLIENT_ID Vercel):** `shipped (PRs #389 + #391)`. Dos fixes de producción ortogonales: (1) **Panelin 503 `API_AUTH_TOKEN not configured`**: `API_AUTH_TOKEN` nunca estaba en `--set-secrets` del deploy (`deploy-calc-api.yml`) y era silenciosamente borrado en cada redeploy; añadido a `--set-secrets` (PR #389) + paso de migración one-time para remover el plain env var antes de montarlo como secret (PR #391, fija el error de type-conflict `Cannot update environment variable to the given type`). Smoke test post-deploy confirmó OK. (2) **Google login 401 `tokeninfo_aud_mismatch`**: `VITE_GOOGLE_CLIENT_ID` en Vercel tenía el placeholder del `.env.example`; actualizado al valor real (`642127786762-...`) vía `drive:vercel-env` + rebuild de producción. Archivos modificados: `.github/workflows/deploy-calc-api.yml` (pre-step remove-env-vars + API_AUTH_TOKEN en --set-secrets), `.github/required-cloud-run-secrets.txt` (añadida línea `API_AUTH_TOKEN`), `.claude/hooks/session-start.sh` (idempotente: guard npm install + guard BMC_DISK_PRECHECK_SKIP). Nuevo: `scripts/check-api-auth.js` (diagnóstico de auth con 8 secciones) + scripts `check:auth`/`check:auth:live`.

**2026-06-17 (capa de orquestación dev/ops — recon READ-ONLY + /version + limpieza de cola de PRs):** `shipped (PR #373)`. Recon de la "capa viva de orquestación de desarrollo" → [`RECON-ORQUESTACION.md`](../../RECON-ORQUESTACION.md): la Fase A **no es de cero** (el SHA-embed ya ships end-to-end vía `Dockerfile`+`deploy-calc-api.yml`+`/capabilities`); el único gap real era un endpoint `/version` dedicado + un reconciler de versión. **Construido el gap:** `GET /version` que consolida `gitSha`+`CALCULATOR_DATA_VERSION`+`built_at` ([`server/lib/versionInfo.js`](../../server/lib/versionInfo.js) + mount en `server/index.js`), reconciler [`scripts/reconcile-version.mjs`](../../scripts/reconcile-version.mjs) (compara prod `/version` vs git HEAD vs local) y test `tests/versionInfo.test.js`; `BUILT_AT` allowlisted en `.github/ALLOWED_ENV_DRIFT.txt` para limpiar el gate env-drift. **Triage del backlog (104 PRs):** cerrados en bloque **79** drafts de agente stale/superseded (reversible — ramas/commits preservados, reabribles/cherry-pickeables); excluidos #203 (rescate Tier B de #240), #298 (toca deploy/secret `API_AUTH_TOKEN` → revisar vs #290) y #346 (rescate incompleto). Cola **104 → ~22 PRs reales** (7 decisión-dueño + 10 mergeable-candidato + 5 duda). Audit del cierre en #240.

**2026-06-17 (catálogo — limpieza DISC + accesorios presupuesto libre; EPS documentado):** `PR pending`. Limpieza de catálogo en `src/data/constants.js`: **borrados 5 nodos DISC** (Isowall 100, IsoFrig 120/180, Perfil U cámara 120/180) + sus mapeos en `matrizPreciosMapping.js`. **Agregados 2 accesorios** seleccionables por unidad en Presupuesto libre: **Embudo / Conexión de Bajada PVC** (`PERFIL_TECHO.embudo._all._all`, SKU CONBPVC, proveedor BECAM, costo 10.97 / venta 12.09 / web 15.35) y **Vaina ISODEC Sist 2000** (`PERFIL_TECHO.vaina.ISODEC._all`, SKU 6825, costo 7.67 / venta 9.20 / web 10.74). `flattenPerfilesLibre` ahora muestra `· unid ·` (en vez de `· barra Xm ·`) para ítems sin `largo`. Pistola APL-DX03 ya existía (`HERRAMIENTAS.pistola_apl_dx03`), sin cambios. Verificación: validation 399/0, catalogDiff 58/0. **Pendiente documentado:** categoría **"Espuma EPS"** (6 SKUs EPS planchas) puesta de lado — spec completa en [`docs/product-catalog/PENDIENTE-ESPUMA-EPS.md`](../product-catalog/PENDIENTE-ESPUMA-EPS.md) (requiere wiring nuevo, no sólo data). PR #376 abierto (refresh de precios + DISC + accesorios) hacia main.

**2026-06-14 (customer PDF layouts — official BMC brand identity for client quotes):** `frontend shipped to prod for evaluation`. All recommended modern "Simple *" PDF layouts (the active customer-facing ones: PDF Cliente, WA export, Drive) now strictly follow the official bmcuruguay.com.uy + COMPANY identity.
- Real logo asset (`/bmc-pdf/assets/bmc-logo.png` — bold navy block "BMC" with tagline) now used in every Simple variant header (previously synthetic "B" circle).
- `BRAND = COMPANY.brandColor` (#003366 navy) made single source of truth: imported in templates, inlined in CSS for headers, names, badges, group rows, totals, bank blocks, accents.
- Default `pdfLayout` changed to `'simple'` ("Presupuesto Simple" — the cleanest navy match); `simple-carbon` (premium dark) + color variants remain as options but are explicitly documented as secondary.
- `src/pdf-templates/index.js` LAYOUT_OPTIONS + all simple-*.js headers updated with "align to bmcuruguay.com.uy + COMPANY.brandColor" notes and recommended status on the brand-true layout.
- Classic paths (`generateClientVisualHTML`, legacy) and `bmc-pdf` Blueprint were already on-brand (heavy #003366 / #022255 navy); this brings the modern lightweight customer default in line too.
- Build green (Vite), our files lint-clean. Pure frontend (the generated HTML for clients now matches site visual language: navy primary, real logo, clean professional commercial quote).
- How to eval in prod: open https://calculadora-bmc.vercel.app, build any quote (techo/pared/libre), use the PDF layout dropdown (or accept new default), click "PDF Cliente" (or WA). Compare header/logo/colors/totals/conditions against site and previous PDFs.

**2026-06-13 (Gate 0 ítem 6 / PR-2 — OAuth state single-use en Postgres):** `migración aplicada a prod; listo para merge`. El `state` OAuth ya vivía en Postgres (`public.oauth_states`, migración `20260607_add_oauth_states.sql`) y ML ya lo usaba — la premisa de "en memoria" estaba **stale**. Los huecos reales eran: (1) el callback de ML hacía `get()` sin consumir → **reuso posible**; (2) **Shopify** (`server/routes/shopify.js`) seguía con un `Map` en memoria. Fix: `server/lib/oauthStateStore.js` gana `consume()` atómico (`UPDATE … SET consumed_at WHERE consumed_at IS NULL AND expires_at > now() RETURNING payload`) — single-use real; `get()` eliminado (sin doble vía). ML (`server/index.js`) y Shopify consumen atómicamente; se quitó el `Map` de Shopify. Migración nueva `supabase/migrations/20260613000001_oauth_states_single_use.sql` (solo agrega `consumed_at`, sin RLS — el backend usa el pool directo `DATABASE_URL`, no el service role PostgREST). Google Tasks ya era single-use (tabla propia `tasks.oauth_state`, sin cambios); el login Google es popup GIS sin `state`. Test offline nuevo `tests/oauthStateStore.test.js` (5 casos: consumo único, reuso rechazado, expirado, desconocido, concurrencia) en `test:api`. **Hallazgo del review (2026-06-13):** `public.oauth_states` **no existía en prod** (la migración `20260607` nunca se aplicó y `oauthStateStore.js` no crea la tabla → el state de ML/Shopify estaba sin respaldo, fallo latente que también rompería el deploy). Por eso `20260613000001` se rehízo **self-contained + idempotente** (crea tabla + índice + `consumed_at`) y **se aplicó a prod** (4 columnas verificadas), corrigiendo el hueco y dejando el auto-deploy seguro. Redeploy: automático vía `deploy-calc-api` al mergear.
**2026-06-13 (TraKtiMe — Fase 4: Tauri desktop spike):** `scaffold (PR draft, sin compilar acá)`. Wrapper de escritorio Tauri v2 en `src-tauri/` que cierra el recorrido: ventana **always-on-top** nativa sobre la vista `?tkDetached=1` del timer + **system tray** (Mostrar/Ocultar, Salir) + **hotkey global** `Cmd/Ctrl+Shift+T`. Lógica en `src-tauri/src/lib.rs` (~1 archivo); la UI sigue en la web app. Scripts `npm run tauri:dev|build|icon` vía `npx @tauri-apps/cli@^2` — **sin nuevas deps** (no toca el lockfile ni la CI web). Carga por defecto la app de prod (shell remoto, cero bundling); modo bundleado documentado. No compilado en esta sesión (sin toolchain Rust). Doc: `docs/team/traktime/TAURI-DESKTOP-SPIKE.md`. Pendiente: compilar/probar por OS, decidir remoto vs bundleado.
**2026-06-13 (TraKtiMe — Fase 3: ActivityWatch opt-in, OFF por defecto):** `spike (PR draft)`. Observación de SO opcional para que el agente responda "¿en qué trabajé hoy?" y proponga entradas. **Estrictamente opt-in:** flag `traktimeAwEnabled` (default false) + `traktimeAwBaseUrl` en `server/config.js`; nada se instala/recolecta sin consentimiento. Nuevos: `server/lib/activityWatchClient.js` (REST a `aw-server`, `getTodaySummary` agrega ventanas por app en día UY-local), `server/routes/activity.js` (`/api/activity/status|today|buckets`, gateado → 404 `aw_disabled` cuando OFF, `requireUser`), montado en `index.js`. Tool de agente `traktime_activity_today` (degrada con hint de cómo habilitar). Solo aplica donde la API está co-ubicada con el daemon (dev/self-host); en Cloud Run queda OFF. Doc: `docs/team/traktime/ACTIVITYWATCH-OPTIN.md`. Test `tests/traktime-activity.test.js` (11 asserts). Pendiente: Fase 4 (Tauri).

**2026-06-13 (WOLF-2026-0001 — carga familia ISOFRIG completa + KB de productos):** `en eval (PR #332)`. Familia `ISOFRIG_PIR` cargada en `constants.js`: 7 espesores 40–180 con las **3 listas** — `web` ex-IVA (Matriz BROMYROS), `costo` ex-IVA (`.accessible-base/matriz_precios.json`, dato duro) y `venta` ex-IVA = **costo × 1.15** (markup BMC confirmado por Matias; venta_local estaba vacía en el origen → WOLF-0004; el factor reproduce la venta histórica al céntimo). Fila 200 clonada excluida; au **1.10** validado contra ficha oficial Kingspan (legacy traía 1.14 copiado de ISOPANEL), núcleo PIR, solo Blanco sanitario; perfil U ISOFRIG 80/100/150 (PU* compartidos; U 40/60/120/180 sin precio, omitidos sin error); visible en `camara_frig` y `presupuesto_libre`. Golden case **GC-0001 verde** validando **ambas listas** (web 76.9454 / venta 58.01 @100 mm; presupuesto_libre 10 m² → 769.45 web / 580.10 venta), GC-0002 sigue verde; lint + test verdes. Smoke UI corrido contra el preview de Vercel (familia visible, 7 espesores sin 200, precios y BOM OK). Extra: base de conocimiento de productos `docs/product-catalog/` (generador + JSON + MD, 97 ítems del Shopify real, con desfasaje Shopify↔Matriz documentado). Ledger v0.7, D5 cerrada.

**2026-06-08 (TraKtiMe — recorrido: widget desprendible + AI wired):** `en progreso (PR #328 draft)`. Recorrido de investigación + decisión + prototipo para los 3 saltos pedidos por Matias (ventana flotante, casi-automático, AI cableada). Dossier: `docs/team/traktime/RECORRIDO-TIME-TRACKER.md`. **Fase 1 (actuación AI):** `server/lib/traktimeLoopbackClient.js` + 9 tools `traktime_*` en `agentTools.js` (el agente acciona TraKtiMe *como el usuario*, reenviando su JWT; writes con confirmación; reads en `TOOLS_REQUIRING_AUTH`), test `tests/traktime-agent-tools.test.js` (25 asserts). **Fase 2 (widget Document PiP):** `FloatingTimer.jsx` + `Timer/detach.js` (Document Picture-in-Picture con fallback popup `?tkDetached=1`) + botón "Desprender" + sync `BroadcastChannel`. Decisiones: PiP ahora/Tauri después; tracking casi-automático sin daemon; ActivityWatch como opt-in OFF (Fase 3 pendiente).

**2026-06-08 (TraKtiMe — jornada/gaps + PDF mensual de horas, ítems 14/15/16):** `shipped (PR draft)`. Implementadas las 3 capacidades faltantes del Time Tracker del reporte de Ramiro (Obs 02). Nuevo `server/lib/traktimeJornada.js` (cómputo puro, read-time sobre `tk_entries`, **sin cambio de schema**): gaps entre tareas consecutivas del mismo usuario/día etiquetados `coordinacion` o `pausa` (umbral 30 min configurable vía `?pausa_min`/`TRAKTIME_PAUSA_THRESHOLD_MIN`), jornada = span primera→última tarea, efectivo = Σ `duration_seconds`, idle = jornada − efectivo; días bucketeados en `America/Montevideo`. Nuevo `server/lib/traktimeHoursPdf.js` (PDF de **horas**, patrón Chromium+GCS copiado del invoice **sin tocarlo**). Endpoints `GET /api/traktime/day-report?date=` y `GET /api/traktime/month-report?month=` (auth `requireUser`; admin puede `?user=`; month-report devuelve `pdf_url` GCS). UI: bloque "Reporte mensual de horas" en `ReportsView.jsx`. Tests: `tests/traktime-jornada.test.js` (31 asserts; fixture Ramiro exacto efectivo 10440s/coordinación 360s/jornada 10800s) + contrato 401 para los 2 endpoints. Muestra: `audit-output/traktime-hours-sample.pdf`. Ledger: `BUG-TRIAGE-RAMIRO.md` lane TIME TRACKER → cerrada (v0.7).
**2026-06-13 (voz — dictado por navegador + fixes 503/voz):** `shipped (PR #324)`. El dictado por micrófono del chat dependía 100% de Whisper server (`/api/agent/transcribe`), así que rompía si `OPENAI_API_KEY` faltaba o estaba en cuota (429) — error "Whisper API 401/429" que parecía bug de auth. Fixes: (1) `useDictation` ahora usa la **Web Speech API del navegador** (Chrome/Edge/Safari) como vía principal — gratis, sin key, sin server — con Whisper de fallback (Firefox); (2) `server/lib/apiKeyUtils.js` `isUsableApiKey()` trata placeholders (`sk-your-...`) como ausentes → mensaje claro en vez de 401 confuso (también en embeddings/RAG); (3) `/api/quotes/counter` devuelve **503 (no 500)** cuando Postgres está caído (convención del proyecto). Diagnóstico en vivo: prod `transcribe` daba 429 (cuota OpenAI), chat texto IA OK vía Claude. Tests offline nuevos (`apiKeyUtils`, `quotesCounterDbDown`, `dictationLang`) en `test:api`. **Nota:** el "modo voz fluido" (OpenAI Realtime/WebRTC) sigue necesitando OpenAI; esto cubre el dictado.

**2026-06-11 (demo-ready — verificación end-to-end + runbook + respaldo):** `verificado`. Objetivo: dejar la app corriendo end-to-end lista para presentar, con respaldo. **Resultado:** lint ✅, `npm run build` ✅ (PWA, 92 entradas precache), y **27/27 rutas cargan sin crash** contra el build de producción (smoke nuevo [`scripts/demo-route-smoke.mjs`](../../scripts/demo-route-smoke.mjs), `npm run smoke:routes`). Hallazgos de entorno (no bugs): este sandbox tiene `NODE_ENV=production` (hay que instalar con `npm install --include=dev` o Vite/ESLint se omiten), fuerza `PORT=8000` (Vite proxea a :3001 → levantar API con `PORT=3001`), sin Postgres :5432 (rutas WA/TraKtiMe/Transportista → 503 por diseño) y sin credenciales (`hasSheets/hasTokens=false`). Crashes en **`vite dev`** de `/calculadora` y `/especificaciones` = artefacto del **service-worker PWA + chunks dev**, NO ocurre en el build → para demo usar el build. Suite offline local falla ~3 casos ambientales (OpenAI sin key; 2 RBAC `identityAuth` con Node 22 vs engines 24.x), **verdes en CI**. Respaldo: tag local `demo-backup-2026-06-11` + puntos durables `origin/main` y rama de trabajo `@ bff358b` (el proxy git solo deja pushear la rama designada; tags/otras ramas → 403). Runbook completo: [`docs/team/DEMO-RUNBOOK.md`](DEMO-RUNBOOK.md). Fallback offline = build PWA (calculadora corre sin API).

**2026-06-11 (gate0 — secrets drift gate antes de (re)deploy):** `shipped (código, sin acción infra)`. `gcloud run deploy --set-secrets` es una lista de **reemplazo total**: cualquier secret omitido se borra de `panelin-calc` en cada deploy — la causa raíz de PRs #313/#315/#317 (IA keys, ML OAuth, token-encryption, WhatsApp token borrados). Nuevo manifiesto canónico [`.github/required-cloud-run-secrets.txt`](../../.github/required-cloud-run-secrets.txt) (single source of truth, 14 secrets) + gate [`scripts/gate-secrets-drift.mjs`](../../scripts/gate-secrets-drift.mjs) (`npm run gate:secrets`): **FATAL** si un secret requerido falta en `--set-secrets` (la regresión) o si el workflow lista uno fuera del manifiesto; **ADVISORY** (no falla) los provisionados a GSM pero no montados — hoy surface 7: `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_APP_SECRET`, `WHATSAPP_VERIFY_TOKEN`, `WEBHOOK_VERIFY_TOKEN`, `API_AUTH_TOKEN`, `SHOPIFY_CLIENT_SECRET`, `SHOPIFY_WEBHOOK_SECRET`. Wired en `test:api` (→ `gate:local`) vía `tests/gate-secrets-drift.test.js` (15 asserts, offline) y como paso 6 de `scripts/pre-deploy-check.sh`. Repo verde hoy. **Pendiente operador (decisión):** evaluar si alguno de los 7 GSM-only debe montarse en el deploy (p.ej. `WHATSAPP_ACCESS_TOKEN`, hoy parcheado post-deploy por `scripts/wa-refresh-access-token.sh`).

**2026-06-11 (WOLF-2026-0005 — provider IA del plan interpreter: VERIFICADO RESUELTO, sin acción):** `RESUELTO / verificación en vivo, sin cambio de infra`. Run de ops config-only para "Sin proveedor IA configurado" en el intérprete de planos (Cloud Run `panelin-calc`, proyecto `chatbot-bmc-live`). **Hallazgo:** el gap de runtime-config ya no existe — `ANTHROPIC_API_KEY` está montada y con billing activo en prod. Evidencia en vivo: `POST /api/crm/suggest-response` → `{"ok":true,"provider":"claude","model":"claude-opus-4-7"}`; `POST /api/plan/interpret` (multipart `file`) → **HTTP 200** con extracción real del modelo (no el 503 `Sin proveedor IA configurado` — nota: ese endpoint lanza 503 sin campo `code`; el `code: "IA_NOT_CONFIGURED"` es de `/api/crm/suggest-response`). Esto cierra la cadena de la entrada 2026-06-10: las keys habían sido remontadas (rev `00449-ncb`) pero daban `All providers failed` por crédito bajo de Anthropic; el billing ya fue recargado y el proveedor responde. **Durabilidad confirmada:** [`deploy-calc-api.yml`](../../.github/workflows/deploy-calc-api.yml) línea 175 ya incluye `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`GROK_API_KEY`/`GEMINI_API_KEY` en `--set-secrets`, así que el próximo deploy no vuelve a borrarlas (riesgo de regresión cerrado). **Corrección de doc:** el endpoint real es `POST /api/plan/interpret` (mount `/api` + ruta `/plan/interpret`, `server/index.js:999`), **no** `/api/planInterpret` — ese path da 404; además requiere subir un archivo multipart (`file`), un POST sin body da 400 antes de llegar al chequeo de proveedor. Sin cambios de código ni de secrets (rotar la key viva sería riesgo innecesario). Ledger: **WOLF-0005 → RESUELTO, GC-0005 verde**. Pendientes Gate-0 (revocar token GitHub fine-grained de Gate 0 + provisionar webhook secrets vía `provision-secrets.sh` + redeploy) quedan como follow-up con shell autenticada (no ejecutables desde la sesión web sin `gcloud`/`doppler`).
**2026-06-10 (live-fix — IA keys wiped by Cloud Run deploy):** `parcial / bloqueado operador (billing keys)`. Tras deploy manual PR3, `suggest-response` devolvía `IA_NOT_CONFIGURED` porque `deploy-calc-api.yml` `--set-secrets` omitía las 4 keys IA y cada deploy las borraba. **Fix código:** añadir `ANTHROPIC/OPENAI/GROK/GEMINI_API_KEY` al workflow (PR pendiente). **Fix infra:** remount GSM + sync Doppler→GSM → revisión `panelin-calc-00449-ncb`; ya no `IA_NOT_CONFIGURED` pero `All providers failed`: Anthropic crédito bajo, OpenAI 429 quota, Grok key inválida, Gemini API no habilitada en GCP. **Operador:** recargar Anthropic y/o OpenAI billing y/o rotar `GROK_API_KEY` y/o habilitar Generative Language API.

**2026-06-10 (S5 Phase B PR3 — remove cockpit-token browser endpoint):** `shipped`. Elimina `GET /api/crm/cockpit-token` y `server/lib/cockpitTokenOrigin.js` (vector de fuga del token estático al browser). Limpieza en `operatorApiClient.js` (sin fetch deprecated), test offline `cockpitTokenRemoved.test.js` (404), script `scripts/rotate-api-auth-token.mjs` (dry-run por defecto). Docs: `CRM-OPERATIVO-COCKPIT.md` §4 auth dual; retirado `COCKPIT_TOKEN_ALLOWED_ORIGINS` de `.env.example` y `run_ml_cloud_run_setup.sh`. Sigue la secuencia S5: PR1 dual auth → PR2 hub JWT → **PR3 endpoint muerto**. **Pendiente operador post-deploy:** rotar `API_AUTH_TOKEN` (`node scripts/rotate-api-auth-token.mjs --apply` con aprobación explícita) y actualizar consumidores (GPT/MCP/CI).

**2026-06-07 (Complete Technical Audit — 10 professional deliverables):** `hecho`. Full honest audit of frontend (React/Vite), backend (1,196-line monolith Express on Cloud Run), integrations (Sheets/MATRIZ, ML, WA, Shopify, Drive, Supabase, email), tests, security, docs, infra/CI, and risks. Produced 10 structured reports in `.runtime/audit-bmc-2026-06-07/` (00-INVENTORY through 10-INDEX). Key findings: backend concentration risk (single Cloud Run + god entrypoint), extreme documentation sprawl (1,023 .md / 29 MB with "docs 2/" duplicate), brittle test runner (giant && chain, no coverage), strong recent hardening (GCP SAs, MATRIZ mapping, Finanzas Docker fix), and good gate culture. All findings include owners, effort, and business cases. Remediation roadmap (30/60/90) provided. Updated per audit requirement. See the audit dir for full details.
**2026-06-07 (precios WOLF-2026-0002 — verificación post-merge contra Matriz en vivo + Shopify; typo Kit):** `verificado / doc`. Revisión post-merge del PR #276 contrastando [`src/data/constants.js`](../../src/data/constants.js) con la planilla *MATRIZ de COSTOS y VENTAS 2026* (acceso directo) y el catálogo Shopify. **Validados al céntimo:** los 5 goteros de cámara (`venta`/`web`/`costo` vs columnas `Venta local USD ex IVA` / `Venta web USD ex IVA` / `Costo USD ex IVA` de BROMYROS — incluido `GSDECAM30 web=36.93` que era correcto, era el ex-IVA vs el c/IVA 38.62) y los 6 anclajes en `venta`+`costo` (vs `VENTA USD EX IVA` 5.96/1.60/1.60/1.20/1.20/1.09, con `Consumidor Final` = ×1.22 confirmando ex-IVA). Descartado un falso positivo (Codex P2 / sospecha inicial de IVA): 5.96 es ex-IVA legítimo, no c/IVA. **Pendiente conocido (no bloqueante):** el `web` de los anclajes no tiene columna en la Matriz; en el canal web (Shopify) estas fijaciones se venden **por paquete de 10 unidades**, por lo que el `web` por-unidad (~×1.34 sobre venta) no es contrastable hoy — revisar contra ML/Shopify a futuro (WOLF-0003/0004). La calculadora opera con lista **LOCAL** por defecto, ya validada. **Fix cosmético:** corregido typo de label `Kin`→`Kit` en `anclaje_kit_u_platea` (constants.js). Sin cambios de lógica ni de valores de precio.

**2026-06-05 (Tareas Phase D — campos ricos: hora, todo-el-día, repetición + paridad Google Tasks):** `hecho confirmado (código) / bloqueado: operador (verificación E2E)`. La creación/edición de tareas ahora soporta **hora del día, toggle todo-el-día y regla de repetición (RRULE)**, alcanzando paridad con el modal de creación de Google Tasks. Como la Tasks REST API v1 sólo persiste title/notes/due(fecha)/status, las dimensiones hora/repetición se respaldan en un **evento de Google Calendar emparejado** (BMC sigue siendo system-of-record; Calendar es el backend de storage de esas dimensiones). **Migración** [`20260605000001_tasks_phase_d_richfields.sql`](../../supabase/migrations/20260605000001_tasks_phase_d_richfields.sql) — aplicada a Supabase `htnwozvopveibwppyjhg`: `due_time TIME`, `is_all_day BOOLEAN NOT NULL DEFAULT TRUE`, `recurrence_rule TEXT` (CHECK `IS NULL OR LIKE 'RRULE:%'`), `calendar_event_id TEXT` (índice parcial único); `sync_conflicts.conflict_type` extendido con `'calendar_drift'`. **OAuth** ([`tasksOAuth.js`](../../server/routes/tasksOAuth.js)): scope ahora `tasks + calendar.events`; nuevo `GET /auth/tasks/scope-probe` (lectura de la columna `scope` persistida, sin round-trip a Google) para que la SPA detecte usuarios pre-Phase-D que necesitan reconectar. **Cliente** [`googleCalendarClient.js`](../../server/lib/googleCalendarClient.js) (nuevo) — espeja `googleTasksClient.js` (timeout + reintento-en-401, decrypt con cast `::text`, reusa `refreshAccessToken`); el 403 (scope faltante) **no** entra al refresh. **CRUD/sync** ([`tasks.js`](../../server/routes/tasks.js), [`tasksSync.js`](../../server/routes/tasksSync.js)): POST/PATCH aceptan los campos ricos y emparejan un evento Calendar cuando hay hora/repetición (fallo de Calendar es **no-fatal** — la tarea se guarda con `calendar_event_id` NULL y un flag `calendar_error`); DELETE borra el evento; el pull inbound detecta `calendar_drift` (de-dup por tarea, BMC gana, sin auto-resolver) y reporta `calendar_events_touched` en `sync_completed`. **UI** ([`TaskCreateModal.jsx`](../../src/components/hub/tasks/TaskCreateModal.jsx) nuevo + [`TasksModule.jsx`](../../src/components/hub/tasks/TasksModule.jsx) + [`useTasks.js`](../../src/components/hub/tasks/hooks/useTasks.js)): modal con título/fecha/hora/todo-el-día/repetición/descripción/lista, CTA "Reconectar" cuando falta el scope Calendar. `gate:local` verde (lint 0 errores / 10 warnings baseline, todos los tests pass). **Bloqueado: operador** — habilitar Google Calendar API en `chatbot-bmc-live` + agregar scope `calendar.events` al OAuth client; usuarios ya conectados deben re-consentir (las llamadas Calendar dan 403 hasta entonces). Ver [`OPERATOR-CHECKLIST.md`](../hub-tasks-module/OPERATOR-CHECKLIST.md) §Phase D.

**2026-06-04 (precios — WOLF-2026-0002 realineación a Matriz):** `resuelto / PR #276`. 11 entradas de catálogo en [`src/data/constants.js`](../../src/data/constants.js) (6 anclajes en `FIJACIONES`, 5 goteros de cámara en `PERFIL_TECHO`) realineadas a la **Matriz de Costos y Ventas Dashboard** bajo decisión **D1** (campos ex-IVA; `venta` ← columna "venta local ex IVA", `web` ← columna "web ex IVA"). Corrige un corrimiento de columna/fila en la extracción que subvaluaba precios (anclajes ~−25% web / −18% venta local; goteros de cámara con fila corrida — `GSDECAM80` cargaba valores de `GFSUP30`). Golden case **GC-0002** verde (`evals/golden-cases/GC-0002.test.mjs`): anclaje gris web ×100 = 215.00; gotero superior cámara 80 mm web = 37.07. `GLDCAM-DC _all` byte-idénticos (gated D3, sin tocar). Mergeado vía PR #276 (admin override, merge commit; commits `9c1ccf0` + `e937381` + `baa90a9`). Ledger [`BUG-TRIAGE-RAMIRO.md`](../../BUG-TRIAGE-RAMIRO.md) v0.4 RESUELTO. Fuera de alcance (gateado): WOLF-0001 (ISOFRIG/D5), WOLF-0003 (accesorios borde/D2-D3), WOLF-0004 (pipeline diff).

**2026-06-04 (/nxt — snapshot post-verificación centralizada de precios + mapeo + key + MCP ranking):** `hecho`. Invocación /nxt scoped al thread completo de verificación de precios (Sheets MATRIZ BROMYROS como fuente única, remapeo cols F→G / L→J / M→K / T→R / U→S, fix JWT bmc-dashboard-sheets, persistencia en Doppler + Secret Manager + Cloud Run, generación de CSV import + JSON live + helper start-api-with-doppler, gates/smoke, artifacts en .runtime/, bake parcial a constants, comparación numérica). Git clean en main; entry previa 2026-06-02 ya documentaba mapping+key como live. Ajuste de scores en reporte (Google Sheets/Data ↑ calidad por mapping correcto + key estable; Calculator pricing ↑ por acceso real + import path; Deploy por patrón rotación probado). Ranking MCPs por uso real del equipo (grok_com_github #1 para PRs/trace de este fix; GitKraken #2 git; grok_com_vercel #3 deploys; chrome-devtools/playwright #4 verif browser UI import; memory #5). Reporte estructurado con próximos pasos priorizados + "get it live" paths (import CSV inmediato, poblar col D en sheet para specials/full CSV prod, bake + gates, verif con MCP browser). Próximos accionables: 1) cargar CSV en PricingEditor, 2) hygiene sheet SKUs col D + re-sync specials. 

**2026-06-04 (xlsx HIGH resuelto — migración a build CDN SheetJS):** `hecho`. Migrado `xlsx` de `0.18.5` (npm, **high** sin fix: Prototype Pollution GHSA-4r6h-8v6p-xvw6 + ReDoS GHSA-5pgg-2g8v-p4x9) al build oficial **`https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz`** vía `npm rm --save xlsx` + `npm i --save-dev <cdn-url>`. Es devDependency, único consumidor `scripts/training/ingestDropboxQuotes.js` (offline, no prod/bundle). Verificado: `XLSX.read` + `XLSX.utils.sheet_to_json` presentes en 0.20.3; `npm audit` ahora **2 moderate** (solo react-router); `gate:local` verde (lint 0 + tests pass). `package.json` apunta al tarball del CDN (npm registry congelado en 0.18.5).

**2026-06-04 (npm audit — sync de estado real, sin cambio de código):** `doc`. Verificado `npm audit`: el ítem de Pendientes fechado 2026-05-07 (`basic-ftp`/`hono`/`ip-address`) estaba **stale** — esas vulns ya no existen. Estado real previo a la migración = **3 vulns**: `xlsx@0.18.5` high (resuelto, ver entrada de arriba) y `react-router`/`react-router-dom@6.30.3` **moderate** (open redirect GHSA-2j2x-hqr9-3h42, fix en 7.x major breaking — diferido a rama propia).

**2026-06-02 (MATRIZ precios — column mapping + key rotation deployed to prod):** `hecho`. Remapeo de columnas en BROMYROS para el motor de precios (costo G, venta local J, ref c/IVA K, venta web R, web c/IVA S) en `server/routes/bmcDashboard.js` (`MATRIZ_TAB_COLUMNS`), sync scripts (fijaciones/silicona), rename-headers, mapping comments, docs y skill. Nueva key para `bmc-dashboard-sheets@...` creada vía gcloud; actualizada en Doppler (bmc-backend/prd: GOOGLE_APPLICATION_CREDENTIALS + GOOGLE_SHEETS_CREDENTIALS + PANELIN_SERVICE_ACCOUNT); nueva versión en Secret Manager `panelin-service-account`; Cloud Run `panelin-calc` actualizado (mount + IAM) + full code deploy vía `deploy-cloud-run.sh` (nueva revisión con el mapping). Re-aplicado secret script post-deploy. Endpoint `/api/actualizar-precios-calculadora` 200 en prod (sin error de credenciales). Cambios persistentes vía Secret Manager + service config. Verificación smoke + curl OK. (Key fix + mapping ahora live.)

**2026-06-01 (Phase 0 GCP SA hardening — panelin-calc prod):** `hecho confirmado`. **P0-1:** migrados a Secret Manager (env refs, no literales) `API_AUTH_TOKEN`, `ML_CLIENT_SECRET`, `ANTHROPIC_API_KEY`, `GEMINI_API_KEY`, `GROK_API_KEY`, `TOKEN_ENCRYPTION_KEY` — revisión activa `panelin-calc-00424-jmd`+; patrón `--remove-env-vars` luego `--update-secrets` (Cloud Run rechaza cambio de tipo en un paso). **P0-2:** nueva key `bmc-dashboard-sheets` (`489cffaa…`) → secret `panelin-service-account` v2; keys viejas revocadas; MATRIZ CSV + `/health` OK (`panelin-calc-00425-g75`). **P0-3:** quitado `roles/editor` de `github-deployer@…` (queda `run.admin`, `artifactregistry.writer`, `iam.serviceAccountUser`). Smoke prod OK. Mapa: [`docs/team/infrastructure/GCP-SERVICE-ACCOUNTS-USAGE-MAP.md`](./infrastructure/GCP-SERVICE-ACCOUNTS-USAGE-MAP.md). Pendiente Phase 1: alinear `deploy-calc-api.yml`, `WA_JWT_SECRET`/`SMTP_PASS` placeholders, keys legacy compute/github-deployer.

**2026-06-01 (Finanzas 404 — cierre de deuda):** `RESUELTO EN PROD`. Incidente reportado vía paste de terminal completo (último curl 404 + intento de commit + `gh workflow run`). Causa: server/Dockerfile no copiaba el dashboard (a diferencia del Dockerfile.bmc-dashboard full-stack). Inspect script confirmó contexto OK → problema era revisión vieja de Cloud Run. **Acciones:** `gh workflow run` disparado sobre SHA con el COPY; nueva revisión desplegada; `/finanzas/` ahora 200 + HTML real en prod. Smoke prod endurecido con chequeo explícito de `GET /finanzas/` (falla el smoke si vuelve a 404). Repro script (`./scripts/repro-finanzas-404.sh`) pendiente de Docker Desktop encendido (usuario documentó "apagado"). Ver también entrada 2026-05-29 más abajo.

**2026-06-01 (Interactive In-App Tutorial System — Modo Tutorial):** `implementado + instrumentado + documentado`. 
Sistema completo de tutoriales interactivos con spotlight + guía paso a paso sobre la UI real.
- 4 flujos principales definidos y documentados (ver `src/components/tutorial/workflows.js` + `FLOWS.md`).
- Instrumentación exhaustiva con `data-tutorial-id` en Admin Cotizaciones (13+ puntos) y Calculadora (5+ puntos clave).
- Launchers contextuales en ambos módulos + botón flotante global.
- Integración en App.jsx + overlay con Portal + persistencia en localStorage.
- Soporte para eventos custom para lanzamiento fácil desde módulos.
- Documentación: `README.md`, `FLOWS.md` y entrada en este archivo.

Listo para uso por el equipo de ventas en la ventana de pruebas.

**2026-06-01 (Productos Maestro Phase 4 UX + smoke `/finanzas/` — shipped):** `hecho en prod` ([`53a7e23`](https://github.com/matiasportugau-ui/Calculadora-BMC/commit/53a7e23)). Edición inline precio/stock, banner cambios pendientes, modal confirm-before-write en Config → Productos (Maestro); API `/api/productos-maestro` push/reconcile; `scripts/smoke-prod-api.mjs` falla si `GET /finanzas/` ≠ 200; fixes test shims identity + `getModuleGrants` no degrada superadmin. **`npm run gate:local:full` OK** (2026-06-01); **`npm run smoke:prod` OK** (MATRIZ + `/finanzas/` + suggest-response). Runbook: [`docs/OPERATOR-RUNBOOK-PRODUCTOS-MAESTRO.md`](../OPERATOR-RUNBOOK-PRODUCTOS-MAESTRO.md). **Próximo run recomendado:** GCP Phase 1 ([`GCP-SERVICE-ACCOUNTS-USAGE-MAP.md`](./infrastructure/GCP-SERVICE-ACCOUNTS-USAGE-MAP.md) — alinear deploy workflow, placeholders WA/SMTP).

**2026-06-01 (Productos Maestro — sistema centralizado precio + stock):** `implementación inicial` ([`86ec4c8`](https://github.com/matiasportugau-ui/Calculadora-BMC/commit/86ec4c8)). Ver entrada shipped arriba para Phase 4 UX.

**2026-06-01 (Infra — mapa canónico service accounts GCP):** `hecho confirmado`. Auditoría consolidada del proyecto `chatbot-bmc-live`: arquitectura dual `panelin-runner` (runtime) + `bmc-dashboard-sheets` (ADC Sheets/GCS), inventario de 8 SAs, keys, Cloud Run, deriva vs scripts/docs, y plan P0–P3. Doc: [`docs/team/infrastructure/GCP-SERVICE-ACCOUNTS-USAGE-MAP.md`](./infrastructure/GCP-SERVICE-ACCOUNTS-USAGE-MAP.md). **Hallazgo top:** secretos sensibles aún como env vars literales en revisión Cloud Run; prod usa key Sheets más antigua (`ff8190cb…`).

**2026-05-29 (Finanzas Legacy Dashboard 404 Fix):** `hecho`. Se identificó y corrigió la causa raíz del 404 JSON en `/finanzas` (dashboard estático del operador).

**Root cause**: Reglas de `.dockerignore` extremadamente frágiles (negaciones múltiples) estaban excluyendo sistemáticamente la carpeta `docs/bmc-dashboard-modernization/dashboard/` del contexto de build de Docker, a pesar de que el `COPY` existía en el Dockerfile. Esto causaba que el middleware `express.static` no encontrara nada y cayera al catch-all final que devuelve `{"ok":false,"error":"Not found","path":"/finanzas"}`.

**Cambios entregados**:
- `.dockerignore` simplificado con allow-list explícita y clara (mucho más difícil de romper).
- `server/index.js`: chequeo defensivo `hasFinanzasDashboard` + log estructurado al startup (visible en Cloud Logging).
- `vercel.json`: actualizadas todas las URLs antiguas de Cloud Run al hash canónico actual.
- Automatización completa:
  - `npm run finanzas:repro` (script oficial que hace build + inspección profunda dentro del contenedor + smoke funcional).
  - `npm run finanzas:inspect` (simulación instantánea sin Docker del contexto de build).
- `gate:local` pasó (solo la baseline conocida de sheetsCsvGuard).

**Archivos nuevos**:
- `scripts/repro-finanzas-404.sh`
- `scripts/inspect-docker-context.cjs`
- `docs/team/HANDOFF-2026-05-29-finanzas-404.md`

**Estado actual**: Deploy a prod completado y verificado (curls reales post-deploy: 200 + HTML del dashboard). Smoke:prod ahora cubre explícitamente `/finanzas/` (falla si regresa el 404). El paso final pendiente es correr `./scripts/repro-finanzas-404.sh` con Docker Desktop activo para generar el log de inspección dentro del contenedor (como pedía el inspect script).

---

**2026-05-29 (Presupuestación Orchestrator Bootstrap — Phase 1 Foundation):** `hecho`. Se lanzó la Fase 1 del plan de automatización 100% con la creación del skill `presupuestacion-orchestrator` (Grok 4.3 Heavy). 

Entregables principales:
- `~/.grok/skills/presupuestacion-orchestrator/SKILL.md` (conductor completo con sub-agentes para pricing/BOM, PDF gate, approval routing, monitoring, etc.).
- `docs/team/PRESUPUESTACION-ORCHESTRATOR-ARCHITECTURE.md` (arquitectura detallada post-investigación paralela de Hub/Tasks, superficie AI y patrones históricos del proyecto).
- `docs/team/PRESUPUESTACION-ORCHESTRATOR-IMPLEMENTATION-ROADMAP.md` (roadmap en 5 fases con separación clara freeze vs post-freeze, quick wins priorizados y métricas alineadas a los targets 90%/99%).
- `docs/team/HANDOFF-2026-05-29-ORCHESTRATOR.md` (handoff de sesión con literal next prompt).

Estado actual: Phase 0 completada y pusheada (3 commits atómicos). Skill del orquestador vivo y listo para uso. Se recomienda iniciar Phase A del roadmap (limpieza de superficie AI + primeros promptfoo evals + higiene de docs).

**Próximos pasos recomendados (bajo feature freeze):**
- Migrar `agentCore.js` + wolfboard/superAgent a `aiProviderConfig` + cost logging (mayor ROI disponible).
- Crear primeros evals en promptfoo para los gates críticos (Pricing Reviewer, Document Gatekeeper).
- Actualizar AGENTS.md y este documento.

**2026-06-01 (Presup Orchestrator — Seg specialist + thin consumer):** `hecho`. Se agregó el especialista concreto `Post-Mortem & Learning (Seguimiento)` con output estructurado (JSON-like) y se creó el thin consumer oficial (`server/lib/presupSegConsumer.js`) que convierte la salida del seg en follow-ups reales vía el `followUpStore` existente.

Entregables:
- Prompt mejorado del seg specialist (ahora produce `title`, `detail`, `tags`, `nextFollowUpAt`, `reasoning` listos para consumo).
- `presupSegConsumer.js` (thin adapter limpio y reutilizable).
- Cableado básico en el scaffolding del conductor (closeFlow ahora puede producir side-effects reales vía el consumer).
- Documentación de uso dentro del skill.

Estado actual: El "run" path para seg **ya es funcional y verificado end-to-end** (consumidor con dryRun + ejemplo real en el skill). Scaffolding pulido (se removió la referencia rota a callSubAgent), docs de uso excelentes agregados ("Uso del especialista seg y consumidor"), y verificación manual con `followup due` exitosa. Todo dentro de feature freeze.

- **Actualización de documentación (2026-06-01):** Se modernizó la sección "Concrete Example: Full Presupuestación Run (High Level)" en el skill para reflejar el flujo real verificado: seg specialist → `createFollowupFromSegOutput(segOutput)` (con soporte dryRun) → followup visible vía `node scripts/followup.mjs due`.

Durante la verificación del run layer se ejecutó `npm run gate:local` → se detectaron y corrigieron 5 errores de ESLint pre-existentes en admin-cotizaciones (seguridad target=_blank + entidades). Commit `3b0c7b2` — ahora lint pasa sin errores (solo warnings conocidos).

**Próximos pasos recomendados (bajo freeze):**
- (Opcional) Añadir 1-2 casos promptfoo específicos para el seg structured output.
- Usar el orquestador en sesiones reales de presupuestos para acumular trazas.
- Mantener disciplina: cada mejora del conductor → gate + nota en este documento.

**2026-05-27 (Phase 0 Production Readiness — Execution Started):** `en curso`. Se inició la ejecución inmediata de Phase 0 del plan de estabilización.

**Acciones completadas hoy:**
- Baseline `gate:local` capturado (exit 0; solo fallas pre-existentes conocidas en sheetsCsvGuard).
- `smoke:prod` baseline: EXIT 0 — todos los checks de producción verdes (/health, MATRIZ CSV, suggest-response con Claude, WA, ML, etc.).
- Safety backup tag `backup/pre-cleanup-20260527-0759` creado y pusheado.
- Primera tanda segura de limpieza de branches ejecutada (con aprobación explícita del usuario):
  - 7 branches antiguas `cursor/critical-bug-inspection-*` (May 5-6 2026) archivadas como tags `archive/...` y eliminadas del remoto.
- Log oficial actualizado en `docs/team/housekeeping/cleanup-2026-05-27.md`.

**Recomendación fuerte para esta semana (Phase 0):**
**Feature freeze de 10 días hábiles** en trabajo no relacionado con estabilidad. Prioridad exclusiva a:
- Testing de identity + tasks
- Más limpieza de branches stale
- Cualquier fix de debt crítico identificado en el plan

Ver [`docs/team/PRODUCTION-READINESS-PLAN.md`](./PRODUCTION-READINESS-PLAN.md) para el plan completo, backlog accionable y definición de "Production State".

**2026-05 (Production Readiness Plan published):** `nuevo documento canónico`. Se creó [`docs/team/PRODUCTION-READINESS-PLAN.md`](./PRODUCTION-READINESS-PLAN.md) como plan ejecutivo y backlog priorizado para llevar el proyecto a un estado de producción estable y de bajo riesgo en 8–10 semanas. El plan define:

- Definición clara de "Production State" (7 criterios objetivos).
- Evaluación actual por área con gap size y prioridad.
- Roadmap en 4 fases (0–3) con entregables concretos.
- Backlog priorizado con ~15 tareas accionables (IDs P1-xx / P2-xx / P3-xx), estimaciones de esfuerzo, dueños sugeridos y dependencias.
- Métricas de éxito y riesgos.

Este documento pasa a ser la fuente única de verdad para el esfuerzo de estabilización. Se recomienda referenciarlo en todas las sesiones de agente hasta completar Phase 2 como mínimo. **Próximos pasos inmediatos:** baseline de gates, limpieza agresiva de branches stale, y freeze de features no-críticas por 10 días hábiles.

**2026-05-26 PM (Audit defensivo + Phase A cleanup):** `hecho confirmado`. Pause de 5 días post último merge OAuth con 7+ ships acumulados sin verificación formal. Usuario solicitó audit defensivo completo pre-nuevos cambios. Tres Explore agents corrieron en paralelo (work-areas, regression-risk, tooling-ecosystem) y produjeron un mapa concreto. **Hallazgos críticos:** los 5 archivos más modificados del último mes (`server/routes/tasks.js` 6×, `tasksSync.js` 3×, `tasksOAuth.js` 3×, `server/lib/userActivityLog.js` 3×, `googleTasksClient.js` 3×) tienen **CERO tests unitarios**; `scripts/smoke-prod-api.mjs` no cubre `/auth/identity/*`, `/auth/tasks/*`, `/api/tasks/*`, `/api/me/activity*`, ni `/api/admin/users/*`; `pre-deploy` no corre tests (solo lint+keys); `test:api` no corre en CI (solo local). Plan tiered de 4 fases (A=cleanup, B=tests hot-spots, C=CI/gates reinforce, D=strategic deps+visual diff) escrito en `~/.claude/plans/acceder-con-google-gentle-ullman.md`. **Phase A ejecutada:** [PR #248](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/248) (env drift fix mío, 5 días viejo) descubierto SUPERSEDED por [PR #252](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/252) durante el intento de rebase — `.env.example` ya documentaba `GOOGLE_TASKS_REDIRECT_URI` (línea 392) y `ACTIVITY_LOG_ORPHAN_TTL_HOURS` (línea 411). Env-drift CI verde en main HEAD `3779ebc`. **#248 cerrada sin merge**, branch + worktree purgadas. [PR #250](https://github.com/matiasportugau-ui/Calculadora-BMC/pull/250) (dependabot `qs` 6.15.0→6.15.2) mergeada vía `gh pr merge --admin --rebase --delete-branch` (auto-merge no habilitado en el repo, patch SemVer = bajo riesgo) — commit `6d50ddd`. **Worktrees cleanup:** `env-drift-allowlist-2-vars` y `tareas-write-flow-fixes` purgadas (bases en main, sin WIP); `finanzas-hub-integration` **INTENCIONALMENTE preservada** — 7 archivos modificados + nuevo `src/components/hub/FinanzasModule.jsx` = active WIP de otra sesión paralela. **Vercel CLI** upgrade 51.2.1 → 54.5.0. **Runbook nuevo** [`docs/team/runbooks/known-baseline-failures.md`](./runbooks/known-baseline-failures.md) cataloga los failures acknowledged (sheetsCsvGuard tab/CR prefixes, agentTranscribe pending re-verify, ActivityTracker:30 react-hooks/purity ya parcialmente resuelto en `c514a7f`) para que devs nuevos no los confundan con regresiones reales. **Lección persistente** guardada en `~/.claude/projects/-Users-matias/memory/feedback-stale-pr-superseded.md`: branches viejas (>3 días) pueden ser silently superseded por trabajo paralelo en main — SIEMPRE rebase + diff vs main ANTES de mergear, sin excepciones. Phases B/C/D agendadas pero **NO ejecutadas** (Phase B = tests para hot-spots `googleTasksClient`/`tasksOAuth`/`tasksSync`/`userActivityLog` + Playwright para `UserAdminModule`/`HistorialTab`/`AnalyticsModule`; Phase C = CI/gates reinforce con `test:api` + pre-commit hook; Phase D = strategic deps auditor + visual diff harness). **Pendientes operador:** ninguno nuevo. **Pendiente triage en próxima sesión:** 25 DRAFT PRs (16 mías + 9 Cursor) candidatas a `bmc-branch-cleanup` skill.

**2026-05-21 N (Google OAuth — fix `redirect_uri_mismatch` en login interactivo):** `hecho confirmado`. Desbloqueado el login con Google de la SPA — desde el lanzamiento del registro abierto (2026-05-20) cualquier login interactivo daba "Acceso bloqueado · Error 400: redirect_uri_mismatch" mientras las sesiones cacheadas seguían vivas (refresh backend funcional vía `client_secret`). Diagnóstico end-to-end vía Playwright contra prod capturó la URL OAuth real: `client_id=642127786762-hbkkonaqp9vvfk2qa9sv5go4bd8u4sj3` + `origin=https://calculadora-bmc.vercel.app` + `redirect_uri=gis_transform` + `gsiwebsdk=gis_attributes` → flow GIS Web SDK popup. **Causa raíz:** OAuth Client en GCP `chatbot-bmc-live` no tenía `https://calculadora-bmc.vercel.app` registrado en **Authorized JavaScript origins** (NO en redirect URIs — son listas separadas; GIS popup expande internamente `gis_transform` a `storagerelay://` validado contra JS origins). **Fix aplicada por operador:** Console GCP → OAuth Client → JS origins + redirect URIs `+= https://calculadora-bmc.vercel.app`. **Verificación E2E (Playwright 2026-05-21 06:42 UTC):** logout limpio → "Iniciar sesión" → "Continuar con Google" → accountchooser ✓ → "You're signing back in to calculadora-bmc.vercel.app" ✓ → consent summary ✓ → Continue → popup cierra ✓ → `GET googleapis.com/oauth2/v3/userinfo → 200` ✓ → `POST /api/auth/google → 200` ✓ → UI muestra el email logueado. **Capas alineadas verificadas:** `.env.local:28` = Vercel prod `VITE_GOOGLE_CLIENT_ID` = runtime prod = `-hbkkonaqp9vvfk2qa9sv5go4bd8u4sj3` (`gcloud` no puede leer/editar OAuth Clients — limitación oficial; verificado manualmente). **Docs entregados sin código de runtime modificado:** nuevo [`docs/team/runbooks/google-oauth-troubleshooting.md`](./runbooks/google-oauth-troubleshooting.md) (runbook reusable con diagnóstico Playwright + checklist GCP + por-qué del bug + apéndice gcloud limits), [`docs/team/BITACORA-MATIAS.md`](./BITACORA-MATIAS.md) creado (la convención de la memoria estaba documentada pero el archivo no existía aún), entrada cronológica del incidente con verificación. **Cleanup:** [`.env.example:262`](../../.env.example) ya no contiene el client viejo `-6rkar09l6902jog9dvnal6e6m3a44p76` hardcodeado — reemplazado por placeholder simbólico `your-google-client-id.apps.googleusercontent.com` + comentario que apunta al runbook (evita que `cp .env.example .env` revive este bug en local). **Memoria persistente añadida:** `~/.claude/projects/-Users-matias/memory/feedback-gis-redirect-uri-mismatch.md` indexa el patrón para futuras sesiones. **Pendiente menor (no bloqueante):** Privacy Policy + Terms of Service URLs en OAuth Consent Screen (Google lo notificó); estética, no funcional.

**2026-05-21 PM-2 (Tareas Stabilization Session 1 — 4/5 subtasks shipped, 1 blocked):** `hecho confirmado`. Cerrado el goal `goal-prompt-tareas-stabilization-session-1.md` (5 subtasks: token refresh, initial-sync backfill, ErrorBoundary copy, Anthropic key, RLS). **Subtask 1 + 2** (`38b8171 fix(tasks): OAuth token refresh + initial-sync backfill`): nuevo helper exportado `refreshAccessToken(pool, userId)` en [`server/lib/googleTasksClient.js`](../../server/lib/googleTasksClient.js) — desencripta refresh_token via SQL, POST a `https://oauth2.googleapis.com/token`, persiste el nuevo access_token cifrado y extiende `expires_at`; `call()` interno wraps cada fetch en `AbortController(30s)` → `google_timeout 504` antes del Cloud Run worker timeout y reintenta una vez tras refresh exitoso en 401 (con timer fresco por intento, sin leak). [`server/routes/tasksOAuth.js`](../../server/routes/tasksOAuth.js) ahora usa `COALESCE` en `ON CONFLICT UPDATE` para preservar el refresh_token cuando Google omite uno en reconnects (Google solo emite refresh_token en primera consent o con `prompt=consent`). [`server/routes/tasksSync.js`](../../server/routes/tasksSync.js) cierra el círculo: el sync handler SELECT `synced_at` de `task_lists` ANTES del upsert (no después — sino siempre veía `now()`), usa ese valor para decidir entre `updatedMin=EPOCH_ISO` (primer ciclo, ingesta histórica completa) vs `updatedMin=priorSyncedAt` (incremental); closure `fetchWithRefresh` reutiliza el mismo retry-on-401 que el cliente outbound. **Subtask 3** (`595a4cb fix(error-boundary): route-aware copy + pathname context`): [`src/main.jsx`](../../src/main.jsx) — helper `currentPathLabel()` lee `window.location.pathname` (BrowserRouter context aún no montado a nivel de ErrorBoundary), muestra "Error en la Calculadora" solo para `/` y `/calculadora`, "Error en la app (`${path}`)" para el resto; aplica también al fallback `showError()` del catch de `import('./App.jsx')`. **Subtask 5** (`4be393c feat(security): enable RLS on tasks.* tables`, migration `20260522000002_tasks_rls.sql` aplicada via Supabase MCP): `ENABLE ROW LEVEL SECURITY` sobre `task_lists`, `tasks`, `oauth_tokens`, `oauth_state`, `sync_log`, `sync_conflicts` + 6 policies `tasks_service_role_all FOR ALL TO service_role USING (true) WITH CHECK (true)`. Verified post-deploy: `list_tables schemas=["tasks"]` retorna `rls_enabled:true` para los 6 + `sync_log` con 63 rows visible (prueba que el pg pool del backend BMC sigue leyendo — postgres role BYPASSRLS funciona). **Subtask 4 — bloqueado:** rotación de `ANTHROPIC_API_KEY` en Secret Manager (`chatbot-bmc-live`) requiere nueva key del usuario; pausado per spec line 318. Continúa el manual `gh workflow run deploy-calc-api.yml --ref main` workaround para deploys de Cloud Run hasta que se rote. **Rebase merge resolution:** durante el push surgieron 4 commits paralelos en origin (`503e26c`, `d98ef71`, `b36e264`, `7dbb985` — error mapping, 30s timeout, PATCH note clears, ON CONFLICT single-column index). Conflicto en `googleTasksClient.js` resuelto fusionando AMBAS mejoras: timeout protection (de ellos) + 401-refresh retry (mío) coexisten en el mismo `call()`. **Closing artifact:** [`docs/team/HANDOFF-2026-05-22.md`](./HANDOFF-2026-05-22.md) commit `4398e25`. **Verified post-deploy:** Cloud Run run `26209470755` success en rebased HEAD `4398e25`; `/auth/tasks/init` y `/api/tasks/sync` retornan 401 (rutas montadas, JWT requerido — shape esperado). **Deferred verifications (per HANDOFF):** matias debe revoke + reconnect una vez (su refresh_token actual descifra a empty string por un legacy reconnect previo al COALESCE fix; el fix evita la regresión pero no restaura retroactivamente); luego un ciclo de sync probará el backfill de 3 tareas pre-2019. Subtask 3 Playwright `breakme=1` no ejecutado en sesión. **Gate state:** lint tiene 1 error nuevo pre-existing en [`ActivityTracker.jsx:30`](../../src/components/activity/ActivityTracker.jsx) (`useRef(Date.now())` viola react-hooks/purity, introducido por commit `05e51e3 feat(activity-log)`); 2 sheetsCsvGuard failures siguen siendo baseline preexistente per goal prompt.

**2026-05-21 PM (Tareas module — Phase C E2E live + verified):** `hecho confirmado`. La cadena completa OAuth → token storage → Cloud Scheduler → sync está corriendo en producción. Token persistido para `user_id=c66f0acc-1a97-4e05-9c08-525997b4b248` a las `04:50:44 UTC` (scope `tasks`, `refresh_token` presente, encrypted columns 644+340 bytes). Cloud Scheduler `tasks-sync-60s` lleva 15 ciclos sin fallar desde `04:53` (latest `05:03:06`, cadencia 60s). 1 lista sincronizada — `Lista de Matías`; 0 tasks porque la lista está vacía en Google Tasks (no es bug — `lists_touched=1` en cada `details JSONB` confirma el pipe). **Bugs encontrados + fix-loop durante activación:** (1) `vercel.json` faltaba rewrite para `/auth/*` y `/sync/*` → fetch del frontend caía al wildcard SPA → fix `87ce2d1`; (2) `requireUser` es factory — código usaba bare `requireUser` en `tasks.js:35` + `tasksOAuth.js:36,248` → 504 timeout 60s → fix `6e97b5e`; (3) `REDIRECT_URI` ahora apunta a Vercel domain (`${config.frontendBaseUrl}/auth/tasks/callback`) — OPERATOR-CHECKLIST.md actualizado para registrar la URL Vercel en el OAuth client GCP (`92769c6`); (4) housekeeping: auto-cleanup de `oauth_state` expirado dentro del sync handler. **Self-healing observado:** los primeros 2 ciclos de cron post-OAuth dieron `lists_http_403` (propagación de scope de Google ~2 min); el 3er ciclo se recuperó solo sin intervención humana. **Verification cheat-sheet:** `SELECT COUNT(*) FROM tasks.oauth_tokens WHERE revoked_at IS NULL` → 1; `SELECT COUNT(*) FROM tasks.sync_log WHERE event_type='sync_completed' AND created_at > now() - interval '10 min'` → >0 si Scheduler activo.

**2026-05-21 AM (User Activity Log + History System — 5-phase ship):** Sistema unificado de log de actividad con per-user history + admin analytics, ejecutado en un solo run autónomo siguiendo `goal-prompt-user-activity-log-and-history.md`. **Phase 0 research** (`d440ed8`, [docs/team/activity-log-research.md](./activity-log-research.md), ~290 líneas): auditoría de los 7 patrones existentes (identity.audit_log, identity.quote_events, tk_audit_log, wa_audit_log, tasks.sync_log, toolStats ring buffer, last_active_at heartbeat) + survey industria con 18 fuentes (Postgres partitioning, event-sourcing vs audit-log, OpenTelemetry conventions, Linear/Notion UX, RBAC vs ABAC). 9 decisiones lockeadas — nueva tabla aditiva (no extender audit_log), action taxonomy plana de strings dotted, sin partitioning hasta ~50GB, RBAC sin cambios. **Phase 1 schema** (`d440ed8`, [migration aplicada via Supabase MCP](../../supabase/migrations/20260522000001_user_activity_log.sql)): `identity.user_activity_log` con 14 columnas + 5 índices, `event_id BIGSERIAL` (matches convention de otras tablas audit, 50% menos bytes que uuid a ~10M filas/año proyectados). [`server/lib/userActivityLog.js`](../../server/lib/userActivityLog.js): `ACTION_TAXONOMY` (35 acciones), `CLIENT_EMITTABLE` subset (4 acciones safe-from-client incl. `auth.session.end`), `scrubPayload()` PII filter, `logActivity()` never-throws. Tres concerns del advisor corregidos antes de aplicar (RLS inert en arquitectura BMC con privileged pg pool → enforcement en handler, uuid → BIGSERIAL, allowlist client-emit). **Phase 2 instrumentation** (`d44ac02`): nuevos endpoints `POST /api/me/activity` (rate 600/15min, valida CLIENT_EMITTABLE) + `GET /api/me/activity` (keyset cursor, filtros from/to/module/action/outcome/q); dual-write retrofit elegante — modificados los helpers `_audit()` en identityAuth.js y `audit()` en identityAdmin.js para escribir a AMBAS tablas, cubriendo los 12+ call sites existentes con 2 edits. Failure-isolated try/catch en cada insert. **Phase 3 Historial tab** (`1b27580`, [HistorialTab.jsx](../../src/components/me/HistorialTab.jsx) ~250 LOC): timeline agrupado por Hoy/Ayer/Esta semana/Anterior, filter chips módulo+outcome, búsqueda debounced 300ms, keyset pagination. MySpacePage rebuild — 6 → 7 tabs (insertado Historial entre Mensajes y Tareas), KPI strip gana 5ta tile "Eventos hoy". **Phase 4 session-end flush** (`05e51e3`): [`ActivityTracker.jsx`](../../src/components/activity/ActivityTracker.jsx) montado en App.jsx — emite `nav.route.change` debounced 1s via useLocation, `pagehide`+`beforeunload` listener envía `auth.session.end` con `fetch(keepalive:true)` (Authorization Bearer survives unload, sendBeacon no soporta auth header); [`server/jobs/closeOrphanSessions.js`](../../server/jobs/closeOrphanSessions.js) inserta synthetic `auth.session.end outcome='orphan'` para usuarios idle > `ACTIVITY_LOG_ORPHAN_TTL_HOURS` (default 24h, configurable), scheduler horario via setInterval en startup (idempotente). **Phase 5 admin analytics** (`80fdf7a`): [`server/routes/identityAnalytics.js`](../../server/routes/identityAnalytics.js) con 5 endpoints (active-users DAU/WAU/MAU, module-usage, error-rate, timeline date_trunc bucketed, top-actions); [`AnalyticsModule.jsx`](../../src/components/admin/analytics/AnalyticsModule.jsx) en `/hub/admin/analytics` (gated role=admin) — 4 KPI tiles (Error Rate roja >5%), bar chart inline-SVG (sin chart lib), 2 tablas (module-usage, top-actions con columna failures); selector de rango 1/7/30/90d; link "Analytics" en BmcModuleNav junto a "Usuarios". **Verified:** Cloud Run rev `panelin-calc-00401-zcj` Ready; Vercel deployment `calculadora-36here53u` Ready (26s build); smokes — `/health`→200, `/api/me/activity` (no auth)→401, `/api/admin/analytics/*` (no auth)→401; schema acepta INSERT con 14 columnas verificado via INSERT/DELETE test con `event_id=1` returned correctly y deleted cleanly; Cloud Run logs grep error|warn|activity-log|orphan → zero matches (orphan-close scheduler arrancó sin errores). **Tabla en 0 filas pos-deploy by design** — dual-write solo fire en NUEVAS escrituras post-deploy, no backfill. Primera escritura orgánica vendrá del siguiente refresh de token de matias o cualquier admin action. **Total LOC ship:** ~2,200 entre 8 archivos nuevos + edits a 7 existentes.

**2026-05-20 PM (User platform — 4-track ship: Tareas nav, Admin Users, TraKtiMe picker, Mensajes):** Cuatro workstreams cerrados en un solo run autónomo siguiendo `goal-prompt-user-platform-4-tracks.md`. **Track C** (`0ab15fc`): visibilidad de Tareas en hub — entrada en [`src/components/BmcModuleNav.jsx`](../../src/components/BmcModuleNav.jsx) (entre TraKtiMe y Market Intel) + card verde en [`src/components/BmcWolfboardHub.jsx`](../../src/components/BmcWolfboardHub.jsx); la ruta `/hub/tareas`, OAuth PKCE, Cloud Scheduler cron y secrets ya estaban en producción (Cloud Run rev 00396-q88), solo faltaba navegación. **Track A backend** (`49f320f`, +394 LOC): nuevo [`server/routes/identityAdmin.js`](../../server/routes/identityAdmin.js) con 8 endpoints audit-loggeados (`GET /api/admin/users` con keyset cursor + filtros, `GET /:id` con roles/grants/sessions/audit, `POST/DELETE /role-grants`, `PATCH /module-grants/:m`, `POST /suspend|reactivate|revoke-sessions`); guards anti-self-modify + anti-demote-superadmin; rate-limited (60/15min writes, 120/min reads). **Track A frontend** (`0939769`, +974 LOC): nueva página `/hub/admin/users` (gated `RequireGrant role=admin`) mirror del patrón [`admin-cotizaciones/`](../../src/components/admin-cotizaciones/) — entrada [`UserAdminModule.jsx`](../../src/components/admin/users/UserAdminModule.jsx) + sub-componentes Topbar/StatStrip/Toolbar/UsersTable/UserDetailDrawer/UserCombobox, hook [`useUserAdmin.js`](../../src/hooks/useUserAdmin.js) (debounced search, keyset pagination, AbortController fetch); reusa `.adminCot` namespace + tokens `--ac-*` + `SkinProvider` (ahorra ~835 LOC de CSS duplicado); link "Usuarios" en BmcModuleNav visible solo a admin/superadmin. **Track D** (`af133a6`): `GET /api/traktime/projects/:id/members` ahora LEFT JOIN a `identity.users` retornando email+name; UI en [`ProjectsPanel.jsx:256-284`](../../src/components/traktime/Projects/ProjectsPanel.jsx) reemplaza input UUID-paste por `<UserCombobox>` consumiendo `/api/admin/users?search=`; member list renderiza email + name en lugar de `<code>{uuid}</code>`. **Track B** (`1e8cc8b` + gap-closer): nuevo schema `identity.message_threads` / `message_thread_members` / `messages` ([migration aplicada via Supabase MCP](../../supabase/migrations/20260521000001_identity_messages.sql)); 6 endpoints en [`identityMe.js`](../../server/routes/identityMe.js) (list threads / detail / create / reply / mark-read + endpoint lightweight `/api/me/users/search` para que cualquier `comprador` busque destinatarios sin role=admin); MySpacePage rebuild con KPI strip clicable (4 contadores) + 2 tabs nuevos (Mensajes con thread list, ThreadView, NewThreadButton + UserPicker autocomplete; Tareas summary con link a `/hub/tareas`); 4 → 6 tabs totales. **Hotfix previo** (`e702c73`): wrap `App.jsx` con `QueryClientProvider` — sin ese provider TasksModule rompía con "No QueryClient set" (latent bug desde 2026-05-18 Tareas Phase C, expuesto al rutear `/hub/tareas`). **Verified:** Cloud Run rev `panelin-calc-00399-n26` Ready; Vercel deployment `calculadora-o80e6p6a9` Ready; smokes — `/health`→200, `/api/admin/users` (no auth)→401, `/api/me/threads` (no auth)→401, `/api/traktime/projects/:id/members` (no auth)→401. **CI gotcha:** smoke gate falla por `ANTHROPIC_API_KEY` inválido (issue separado), cada track requirió `gh workflow run deploy-calc-api.yml --ref main` para el deploy de Cloud Run. **Total LOC añadidos:** ~3,100 entre backend + frontend + migration + docs.

**2026-05-20 PM (Tareas module — Phase C.1+C.3 code-complete, blocked on operator infra):** Implementados los dos stubs backend restantes para el módulo Tareas. **C.1 — OAuth PKCE flow ([`server/routes/tasksOAuth.js`](../../server/routes/tasksOAuth.js), commit `d6526b5`):** `GET /init` (requireUser) genera verifier+challenge via `crypto.randomBytes(32).toString('base64url')` + SHA-256, persiste el verifier en `tasks.oauth_state.challenge`, retorna `{ ok, url }` para que el SPA navegue (evita exposición del JWT en URL del 302); `GET /callback` hace `DELETE...RETURNING` atómico de `oauth_state` (anti-replay), intercambia código con Google, cifra access+refresh tokens via SQL `pgp_sym_encrypt($1::text, $2)` (la key no toca JS) y upserts a `oauth_tokens ON CONFLICT (user_id)`; `POST /revoke` (requireUser) desencripta via SQL, llama a Google revoke (best-effort) y marca `revoked_at`. Todos degradan con `503` estructurado cuando `googleTasksClientId`/`supabasePgpEncryptKey`/`databaseUrl` faltan. **C.3 — Cloud Scheduler sync handler ([`server/routes/tasksSync.js`](../../server/routes/tasksSync.js), commit `4ee410d`):** Reescrito el `verifyHmacSignature` roto (computaba HMAC de string vacío + `===`) a `crypto.timingSafeEqual` contra `config.syncHmacSecret` (Cloud Scheduler manda el secret crudo como header `X-Sync-Signature` per [OPERATOR-CHECKLIST.md](../hub-tasks-module/OPERATOR-CHECKLIST.md) §3.3). Sync loop: itera `oauth_tokens WHERE revoked_at IS NULL`, desencripta access_token via SQL, GET `users/@me/lists` + `/lists/{id}/tasks?updatedMin=lastSyncedAt&showDeleted=true` paginado, upserts `task_lists ON CONFLICT (google_id)` + `tasks ON CONFLICT (list_id, google_id) WHERE NOT is_deleted`, detecta conflicts (`is_deleted=true local + Google activo` → `sync_conflicts.conflict_type='soft_delete_mismatch'`), maneja 401 (marca revocado + `event_type='token_revoked'`) y 429 (`'rate_limit_hit'`, skip user this cycle). Cada user en try/catch — una falla no mata el ciclo. Tokens **nunca** se loguean en plaintext; errores enmascaran user_id como `***` + last 4. **Config ([`server/config.js`](../../server/config.js) + `.env.example`, commit `494ae62`):** 4 keys expuestas — `googleTasksClientId`, `googleTasksClientSecret`, `supabasePgpEncryptKey`, `syncHmacSecret`. **Frontend wiring ([`src/components/hub/tasks/TasksModule.jsx`](../../src/components/hub/tasks/TasksModule.jsx), incluido en C.1):** `EmptyConnectCTA` ya no usa `<a href>` (no podía mandar Bearer); ahora botón con `fetch('/auth/tasks/init', {Authorization: Bearer accessToken})` → `window.location.href = data.url`. **Estado:** `hecho confirmado` — lint 0 errors + 4 warnings (baseline), `grep not_implemented server/routes/tasks*.js` vacío, `import('./tasksOAuth.js')` + `import('./tasksSync.js')` exit 0. **`bloqueado: operador`** — E2E activation depende de `GOOGLE_TASKS_CLIENT_ID/SECRET`, `SUPABASE_PGP_ENCRYPT_KEY`, Cloud Scheduler API + job según [OPERATOR-CHECKLIST.md](../hub-tasks-module/OPERATOR-CHECKLIST.md) (procedimiento ~18 min). Pre-existing `sheetsCsvGuard.test.js` failures (tab/CR prefix) son baseline preexistente y no relacionado — documentado en commit auth/registration `9cfd9eb`.

**2026-05-20 (Open Google registration — auth system wired into production):** Cualquier usuario de Google ahora puede registrarse y usar `calculadora-bmc.vercel.app`. Auditoría confirmó que todos los secretos GCP estaban ya provisionados (`IDENTITY_JWT_SECRET`, `MFA_KEK_HEX`, `GOOGLE_OAUTH_CLIENT_ID=642127786762-hbkkonaqp9vvfk2qa9sv5go4bd8u4sj3.apps.googleusercontent.com`, `IDENTITY_COOKIE_DOMAIN=.calculadora-bmc.vercel.app`), montados en Cloud Run `panelin-calc`, `VITE_GOOGLE_CLIENT_ID` ya en Vercel desde hace 30 días, y la migración `identity.*` aplicada en Supabase con 14 tablas + 9 módulos sembrados. **Gap único cerrado:** `verifyGoogleAndUpsert` en [`server/lib/identityAuth.js`](../../server/lib/identityAuth.js) (entre `role_grants` y la rama MFA) ahora inserta `calc:write` + `tareas:read` por defecto en `identity.module_grants` para cada login (idempotente vía `ON CONFLICT DO NOTHING`), así todo `comprador` puede usar la calculadora y abrir el módulo Tareas sin esperar grants manuales. **Frontend gating:** [`src/App.jsx`](../../src/App.jsx) ahora envuelve rutas Hub con `<RequireGrant>` — `/hub/ml`, `/hub/wa`, `/hub/canales`, `/hub/plan-import` (`module/minLevel=read`), `/hub/admin`, `/hub/cotizaciones`, `/hub/agent-admin`, `/hub/marketing` (`role=admin`), `/hub` y `/mi-espacio` y `/hub/traktime/*` (solo auth) — y registra nueva ruta `/hub/tareas` con `<RequireGrant module="tareas" minLevel="read">`. La calculadora (`/` y `/calculadora`) queda **pública** sin login. **Verified:** `curl POST /api/auth/google -d '{"idToken":"bad"}'` → 401 (no 500), `curl /health` → 200, `npm run build` → exit 0 (chunk `TasksModule-CqWKCMxx.js` 19.13 kB code-split). **No-blockers conocidos:** `tests/sheetsCsvGuard.test.js` falla 2 casos (`tab prefixed` + `CR prefixed`) pero esto es preexistente en `main` HEAD `85c7a58` (verificado via `git stash` + re-run), no relacionado con este cambio.

**2026-05-18 (Tareas module — Phase C implementation: CRUD + hooks + UI):** Three atomic commits shipped the code-only parts of Phase 1 that don't depend on operator-provisioned secrets. `4480bd3` — server/routes/tasks.js rewritten with 4 working READ endpoints (`GET /lists`, `GET /lists/:id`, `GET /lists/:id/tasks` with keyset pagination, `GET /lists/:id/tasks/:taskId`) querying Supabase `tasks.*` schema; write endpoints return 503 `sync_not_configured`. Added `server/lib/tasksDb.js` (lazy `pg.Pool` to DATABASE_URL, mirrors traktimeDb.js pattern). `b0bd565` — `src/components/hub/tasks/hooks/useTasks.js` rewritten with 9 TanStack Query v5 hooks (4 reads + 5 mutations) that hit the real endpoints, pull `accessToken` from `BmcAuthProvider`, parse 503 specifically via `err.unavailable=true`. `6ea9808` — `src/components/hub/tasks/TasksModule.jsx` rewritten with sidebar `TaskListPicker` + main `TaskListDetail`, empty-state `EmptyConnectCTA` linking to `/auth/tasks/init`, graceful `ErrorPanel` for unavailable state. C1 (OAuth PKCE backend) and C3 (sync handler) remain blocked on operator action — see `docs/hub-tasks-module/PHASE-1-INFRASTRUCTURE.md`. Gate:local exit 0 across all 3 commits (24 lint warnings baseline, 397 tests pass, 2 pre-existing failures in sheetsCsvGuard).

**2026-05-18 (Tareas module — Phase A cleanup + Phase B infra unblock):** Unified residue from two prior sessions on Tareas module. Phase A (commit `f76109f`): mounted 3 orphaned routes in `server/index.js` (`/api/tasks`, `/auth/tasks`, `/sync`), added `"tareas"` to `server/lib/identityAuth.js` ALL_MODULES, removed 5 misplaced files from `docs/hub-tasks-module/`, moved goal-prompt to `PHASE-1-MASTER-PROMPT.md`, expanded README. Phase B (this commit): applied `tasks_init` migration to Supabase production via MCP after fixing 2 bugs in the migration file (MySQL-style inline `COMMENT` clauses → standard Postgres `COMMENT ON COLUMN`; INSERT into `identity.modules` used wrong columns `slug/label/enabled` → corrected to `module/display_name/category`). 6 tables created in `tasks` schema, `tareas` row seeded into `identity.modules`. Tracking 3 remaining operator blockers for Phase C/D in `docs/hub-tasks-module/PHASE-1-INFRASTRUCTURE.md`: Google Tasks OAuth client, `SUPABASE_PGP_ENCRYPT_KEY` secret, Cloud Scheduler API enablement + cron job. **Verified:** `npm run gate:local` exit 0 after Phase A; 6 tables visible at `SELECT * FROM information_schema.tables WHERE table_schema='tasks'`; routes return 401 (auth required) not 404. Phase C (real implementation) blocked on operator action.

**2026-05-18 (BmcAuthProvider bootstrap fix — accessToken refresh on page reload):** Fixed authentication flow where users refreshing the page would encounter "missing_credentials" errors in modules like TraKtiMe. Root cause: `GET /auth/me` endpoint returns user data via httpOnly session cookie but does not return `accessToken`, leaving React context without the JWT needed for API calls. Solution: Added bootstrap logic in [`src/contexts/BmcAuthProvider.jsx`](../../src/contexts/BmcAuthProvider.jsx) (lines 106–109) to detect null accessToken after successful user fetch and immediately call `refreshAccess()` to acquire a fresh token. This ensures the token is available in React context for all downstream consumers (TraKtiMe, agent tools, etc.) without requiring page reload or manual refresh. Commit: `6f1dd9b`. **Verified:** page reload flow now preserves authentication state + token availability.

**2026-05-18 (Global Quote Counter — atomic Postgres + confirmation modal):** Feature complete across backend (migration, pool, routes), utilities, and React component. Counter resets annually (seq 0001-9999). PDF filename format: `NNNNBMC-DDMMYY-RazonSocial-City.pdf`. Header badge displays real-time count ("Cotizaciones: NNNN"). "Cotización lista" button opens confirmation modal with customer details (nombre, RUT, teléfono, dirección), totals, and filename preview. Fixed customer field-name bug in PDF template (nombreRefCliente). End-to-end UI testing verified: counter increments atomically (0007→0008), modal displays and closes correctly, PDF generates with correct naming format. Database pool singleton follows `waDb.js` pattern. All tests pass: **399 passed, 0 failed** (lint 24 warnings pre-existing). Ready for production.

**2026-05-18 (Dependabot npm audit fix — 2 of 3 vulnerabilities resolved):** Ejecutado `npm audit fix --audit-level=moderate` resolviendo 2 de 3 vulnerabilidades: **brace-expansion** (5.0.5→5.0.6, DoS protection bypass GHSA), **ws** (8.20.0→8.20.1, uninitialized memory disclosure). Tercera vulnerability: **xlsx** (HIGH, in `scripts/training/ingestDropboxQuotes.js` batch-only, no fix disponible — fixed versions 0.19.3+ aún no liberadas en npm registry). **Risk:** xlsx limitado a scripts de training, no producción HTTP. Documentada como pending upstream release. **Tests:** gate:local 312+ passed con exit code 0. **Commit:** `620c174` (`fix: npm audit fix for brace-expansion + ws`); revertir `.env` si cambios autogenerados no deseados.

**2026-05-18 (PDF layout optimizer — BMC-2026-0056 compression):** Nuevo script ReportLab [`scripts/pdf/optimize_bmc_pdf_layout.py`](../../scripts/pdf/optimize_bmc_pdf_layout.py) comprime PDFs de cotización BMC de 4 → 2 páginas (398 KB → 5.8 KB). Aplica dark-blue headers (#0B1220) consistente con PDFs de referencia 0057/0060/0061, márgenes compactos 12mm, padding table optimizado 2mm, sección spacing 4–6mm. Ejemplo de output: [`public/quotations/BMC-2026-0056_optimized.pdf`](../../public/quotations/BMC-2026-0056_optimized.pdf). Documentación: [`docs/quotations/BMC-2026-0056_LAYOUT_IMPROVEMENTS.md`](../../docs/quotations/BMC-2026-0056_LAYOUT_IMPROVEMENTS.md).

**2026-05-18 PM (TraKtiMe — missing_credentials fix):** El módulo TraKtiMe no cargaba por error de arquitectura: BmcAuthProvider almacena el JWT `accessToken` en React state + cookies httpOnly, pero el cliente API de TraKtiMe intentaba leer desde `window.localStorage` (vacío en contexto React). **Solución:** Patrón de inyección de token en [`server/lib/api.js`](../../src/components/traktime/shared/api.js) — variable módulo `let injectedToken = null`, nueva función `setApiToken(token)` (exportada), y función `authHeader()` que prioriza `injectedToken` sobre localStorage. **Integración en TraKtiMeModule.jsx:** nuevo `useEffect` que llama `setApiToken(auth.accessToken)` cuando el token cambia (líneas 28–32), y gating de `reload()` a `auth.status === "authenticated"` (líneas 45–49). **Resultado:** Timer carga sin "missing_credentials", autenticación fluye correctamente desde BmcAuthContext → TraKtiMe. Cambios mínimos, sin breaking changes; fallback localStorage mantiene compatibilidad hacia atrás. **Pendiente operador:** end-to-end test en navegador para confirmar que el timer funciona post-login (GCS auth flota).

**2026-05-18 (Market Intelligence v1 — PR #244 mergeado):** Módulo completo implementado y mergeado a `main`. Código extraído de sesión Managed Agent (`sesn_01TPkGGTQ4GW3RRGYcqYFTU5`) via extractor Python sobre events JSON. 38 archivos nuevos/modificados: 8 migraciones SQL idempotentes (schema `bmc_market_intel`, aislado de `bmc_price_monitor`), ETL pipeline con scraping responsable (robots.txt, rate limiting 1req/site/día, delta detection, deduplicación por dominio normalizado), mystery shopping queue (never-auto-approve), sistema de alertas 3 niveles (info/warning/critical, umbrales configurables via env, dedup por competitor+sku+level+UTC date), Express router `/api/marketing/*` (6 endpoints, `requireAuth` per-ruta), dashboard React `/hub/marketing` con 5 subcomponentes, 26 tests node --test (26/26 ✅). CI: Lint ✅, Validate Calculations ✅, Env drift ✅ (se agregaron 5 vars a `.env.example`), Vercel + Cloud Run deployan ✅. **Pendiente operador:** correr `DATABASE_URL=<prod_url> npm run migrate:market-intel` — `DATABASE_URL` sigue sin estar en Cloud Run (mismo gap que traktime y WA Cockpit). Para todos los módulos que dependen de Postgres: provision la DB y setear `DATABASE_URL` en Cloud Run via `gcloud run services update panelin-calc --set-env-vars DATABASE_URL=<url>`.

**2026-05-16 (TraKtiMe — módulo completo Sprint 1→2→3, PR #242):** Implementación end-to-end del módulo de gestión de tiempo y facturación TraKtiMe para METALOG SAS / BMC Uruguay. **Sprint 1 MVP:** 10 migraciones SQL (`traktime-package/migrations/000–010`), runner `scripts/run-traktime-migrations.mjs`, pool singleton `server/lib/traktimeDb.js`, router factory `server/routes/traktime.js` (~35 endpoints), `"traktime"` agregado a `ALL_MODULES` en `identityAuth.js`, ruta React lazy `/hub/traktime/*` en `App.jsx`, link en `BmcModuleNav.jsx`, test de contrato offline `tests/traktime-contract.test.js` (13 asserts). **Sprint 2 team mode + mirror:** `server/lib/traktimeMirrorWorker.js` (cron 03:00 UY, 5 tabs Sheets con scratch+rename + append-only audit watermark), `server/lib/traktimeAudit.js` (never-throws audit helper), endpoints admin `/api/traktime/admin/mirror-now` + `/api/traktime/projects/:id/members`, member scoping en entries/reports, `server/config.js` con vars `TRAKTIME_*`. **Sprint 3 invoicing + PDF:** migraciones `007–009` (invoices, lines, seq), numeración `TRK-YYYY-NNNN` vía `tk_invoice_seq` UPSERT, `server/lib/traktimeInvoicePdf.js` (HTML→Chromium→GCS, max 1 concurrente), template `src/pdf-templates/traktimeInvoice.html` en español, `InvoicesPanel.jsx` con Builder + filtro de estado. Todos los CI checks verdes (Lint ✅, Validate Calculations ✅, Env drift ✅). **Pendiente por el operador:** correr `npm run traktime:migrate` contra DB real, configurar `TRAKTIME_SHEET_ID` y `TRAKTIME_INVOICE_ISSUER_ADDRESS`.

**2026-05-14 PM (F1 v2 completion — 6 PRs landed cierran el bloqueo):** Tras la sesión de mañana (5 PRs F1 mergeados), 6 PRs más entraron a `main` esta tarde y destrabaron los 2 hold (#228 y #229). Secuencia: #231 (3 Playwright smokes para los features F1 — Sugerir IA, Suggested badge, WA timeline — con npm aliases `test:playwright:admin-cot-*`); #232 (fix del noise filter de esos smokes para tolerar 401/403/503 esperados en anon session); #233 (squashed rebase de #223 — `feat(admin-cot): Hybrid RBAC + Phase 3 wiring + outcome model` — supersede del original, +706/-81 LOC, 21 files, incluye `server/lib/wolfboardOutcome.js`, `eslint-rules/bmc-help.js`, `src/components/help/anchors.js`, 15 anchor placements distribuidos entre Topbar/StatStrip/Toolbar/DetailDrawer, `tests/help-anchors.test.js` + `tests/wolfboard-derive-outcome.test.js` con 64 asserts nuevos; conflict resolution preservó tanto el AbortController de #224 como el `X-Panelin-Role` header de #233 en `useAdminCotizaciones.apiFetch`); #228 (rebaseado contra el nuevo main — botón "+ Nueva consulta" + endpoint `POST /api/wolfboard/row-create` para canales no-auto CL/LL/LO/FB/IG); #229 (rebaseado — surface `/api/crm/cockpit/ml-queue` en la tabla del admin con badge "CRM" en filas `source=crm-ml`, select Responsable en drawer wireado a `PATCH /api/cotizaciones/<id>`); #230 ya estaba en main desde la mañana. **Open question pre-uso real:** confirmar valores del dropdown `Responsable` en CRM_Operativo Parametros (códigos vs nombres) — ship actual usa códigos por convención `Admin 2.0` col A. **Estado runtime:** `dev:full` reiniciado con flag `VITE_FEATURE_ADMIN_COT_V2=true` en `.env.local`; v2 module monta limpio en `/hub/cotizaciones` y los 3 smokes Playwright pasan en anon mode (early-exit branch). Para full drawer assertions hay que setear `API_AUTH_TOKEN` en `.env` del server. **Total intra-day:** 10 PRs míos mergeados (#224-#233) más #230 docs, ~2400 LOC entre features + tests + docs.

**2026-05-14 (F1 Admin Cot enrichment — 7 steps, 7 PRs, 4 merged):** Plan F1 sobre el módulo v2 `/hub/cotizaciones` cerrado tras data-analysis (n=353 cotizaciones · WA 58% · ML 10.5% · 84% input telegráfico · 3.9% multi-canal). **Mergeados:** #224 botón ✦ Sugerir IA per-row en [`DetailDrawer`](../../src/components/admin-cotizaciones/DetailDrawer.jsx) + AbortController 45/60s en [`useAdminCotizaciones.apiFetch`](../../src/hooks/useAdminCotizaciones.js) wireado a `/api/crm/suggest-response` (4-LLM fallback); #225 badge 💡 Sugerido en QuoteCard/DetailDrawer + nuevo [`cotizacionAssignment.js`](../../src/utils/cotizacionAssignment.js) rule engine (37 tests) — códigos MA/RA/TIN/SA con regex cobranzas→SA / soporte→MA / WA<80c→TIN / ML+EM→RA / CL+LO→TIN; #226 timeline WA embebido en accordion del drawer (nuevo [`WaTimelineInline.jsx`](../../src/components/admin-cotizaciones/WaTimelineInline.jsx) + [`waPhoneNormalize.js`](../../src/utils/waPhoneNormalize.js) — 20 tests — vía `/api/wa/conversations` + `/api/wa/messages`); #227 workflow scheduled `email-ingest-scheduled.yml` (manual + cron commented out hasta wirear `BMC_EMAIL_SNAPSHOT_URL`) + runbook [`docs/team/runbooks/email-ingest-cron.md`](./runbooks/email-ingest-cron.md). **Open, hold por #223 (RBAC+Phase 3, CONFLICTING):** #228 botón "+ Nueva consulta manual" en Toolbar + nuevo endpoint `POST /api/wolfboard/row-create` (CL/LL/LO/FB/IG); #229 surface CRM_Operativo ML rows en admin table con badge "CRM" + acciones disabled (`/api/crm/cockpit/ml-queue` paralelo a `/api/wolfboard/pendientes`) + Responsable select en drawer wireado a `PATCH /api/cotizaciones/<id>` (best-effort). **Open question pre-#229 merge:** confirmar valores del dropdown `Responsable` en CRM_Operativo Parametros (códigos vs nombres). **Decisiones del run:** plan completo en [`~/.claude/plans/eventual-soaring-spring.md`](file:///Users/matias/.claude/plans/eventual-soaring-spring.md), data-analysis en `~/.claude/team/runs/2026-05-13-2158-bmc-superchat-arch/artifacts/admin-analysis.md`. **Bloqueador remanente:** PR #223 (701 LOC) necesita rebase contra main para destrabar #228 + #229.

**2026-05-13 (Go-live — auditoría del GO-LIVE-DASHBOARD-CHECKLIST y runbook de items manuales):** auditadas las 24 filas del checklist contra producción. **Cerradas con evidencia hoy:** 5.1 (Cloud Run `panelin-calc-00371-j97` live), 6.0 (smoke:prod 8/8 verde incl. `suggest-response` con Claude — key rotator 2026-05-05 efectivo), 7.2 (guía administrativos extendida en [`docs/GUIA-RAPIDA-DASHBOARD-BMC.md`](../GUIA-RAPIDA-DASHBOARD-BMC.md) con AUDIT_LOG / KPI Report / escalación entregas / triggers Apps Script / troubleshooting), 4.2 (Vite ejercitado en runs autónomas top-10/20/30 2026-05-11/12). Anotadas como skipped: 5.2 (VPS), 5.3 (ngrok) — Cloud Run es la superficie elegida. **Items manuales documentados:** nuevo [`GO-LIVE-MANUAL-RUNBOOK-2026-05-13.md`](../bmc-dashboard-modernization/GO-LIVE-MANUAL-RUNBOOK-2026-05-13.md) con secciones click-by-click para 1.4 (share workbook · SA email `bmc-dashboard-sheets@chatbot-bmc-live.iam.gserviceaccount.com`), 2.x (verify-tabs post-1.4), 3.1-3.4 (Apps Script + triggers per [`AUTOMATIONS-BY-WORKBOOK.md`](../google-sheets-module/AUTOMATIONS-BY-WORKBOOK.md)), 6.1-6.7 (UAT del dashboard). Cada fila ☐ remanente del checklist ahora linkea al runbook §correspondiente. **Pendientes específicos:** ejecución del runbook por Matías → al confirmar, tildo las filas en un follow-up commit.

**2026-05-13 (Observability — toolStats → Cloud Logging structured):** cada llamada a `executeTool` ahora emite una línea pino estructurada `event: "agent_tool_call"` con `tool`, `ok`, `latency_ms`, `error_class` vía `req.log` (pino-http), capturada por Cloud Logging vía stdout. El ring buffer en [`server/lib/toolStats.js`](../../server/lib/toolStats.js) se mantiene para el panel dev (`GET /api/agent/tool-stats`); ambos sinks coexisten. Plumbing: 2 call sites en [`agentChat.js`](../../server/routes/agentChat.js) (`/api/agent/exec-tool` + chat tool-loop) pasan `{ logger: req.log }` a `executeTool`; 6 calls internos recursivos en [`agentTools.js`](../../server/lib/agentTools.js) propagan `opts`. Cierra el pendiente de PR #110 release notes (2026-05-06). Tests: nuevo grupo en [`tests/toolStats.test.js`](../../tests/toolStats.test.js) (5 asserts + backwards-compat sin logger).

**2026-05-13 (Infra unblock — Cloud Run deploy roto desde 2026-05-09 reabierto):** Dos bugs encadenados rompieron 11 deploys consecutivos a `panelin-calc`. (1) `canvas@3.2.3` agregado en `a9fb531` como dep huérfana (0 imports en repo) — forzaba node-gyp en `node:24-alpine` sin python/make/g++. Removido en [`9faa3f8`](https://github.com/matiasportugau-ui/Calculadora-BMC/commit/9faa3f8) (26 paquetes eliminados). (2) Typo `requireDevModeAuth` (sin `Middleware`) en [`server/routes/agentTraining.js`](../../server/routes/agentTraining.js):429 introducido por `8d27055` (kbAnalytics) — ReferenceError crasheaba startup. Corregido en [`3ce36db`](https://github.com/matiasportugau-ui/Calculadora-BMC/commit/3ce36db). Revision actual: `panelin-calc-00371-j97`. Libera 51 commits server-side acumulados (RAG v1, dual-write CRM, ML cache SHA-256, security B2/B4/B6). Workflow `deploy-calc-api.yml` path filter ampliado para incluir `package.json|package-lock.json` (evita futuros `workflow_dispatch` manuales).

**2026-05-12 (Branch housekeeping — Sesiones 1–7):** Limpieza de 199 → 52 branches remotas (−147), 26 archive tags preservando código rescatable (clientes-360 prompts, KB experiments, security followups). Sesión 7 ejecutó secuencia de merge del system map: 3 PRs mergeados (#176 BOM dup, #177 Drive PDF, #197 dev mode token); 5 PRs `CONFLICTING` documentados pendientes (#146/#187/#190/#142/#126). Skill `bmc-branch-cleanup` capturada en `~/.claude/skills/`. CONTRIBUTING.md amplía sección "Branch naming" y "Ritual housekeeping mensual". Log auditable: [`docs/team/housekeeping/cleanup-2026-05-12.md`](./housekeeping/cleanup-2026-05-12.md). Mapa visual: [`docs/team/architecture/system-map-and-pending.md`](./architecture/system-map-and-pending.md).

**2026-05-12 (Server hardening — ML cache key SHA-256 + parseCrmRowAtoAK `_meta`):** [`server/routes/mlSearch.js`](../../server/routes/mlSearch.js) ahora hashea el `cacheKey` con SHA-256 (`crypto.createHash`) para evitar colisiones cuando `q` contiene el separador `|`. [`server/lib/crmRowParse.js`](../../server/lib/crmRowParse.js) — `parseCrmRowAtoAK` retorna `_meta: { rowLength, truncated }` para que consumers críticos puedan detectar filas Sheets con menos columnas de las esperadas (antes los índices altos quedaban "" silenciosamente). Sin breaking changes — el shape público se mantiene, `_meta` es additive.

**2026-05-12 (Frontend run — 13 items UI aplicados):** Cuarta corrida autónoma con foco exclusivo UI (commits `6ca873a` → `0e45956`, 8 commits). Consistency: font-size statusBanner, opacity disabled `0.4→0.5`, copy "Error: …" uniforme en [`PricingEditor`](../../src/components/PricingEditor.jsx). UX wins: padding "Cargando operadores", title disabled "Subir archivo", hover state CTA MySpace, opacity "Probar" AI, placeholder `(cualquiera)` grisado, tooltip cells truncadas, warning export con secrets. Loading: spinner CSS animado en "Cargando configuración…". A11y: `:focus-visible` outline 2px en WA cockpit. Mobile: grid 3-col colapsa a 1fr bajo `@media (max-width: 639px)`. SVG: halo blanco en cota