# Handoff — 2026-06-11 21:02 · Panelin #323 hardening + advisor config fix

**Current branch**: `fix/panelin-323-review` (worktree `.claude/worktrees/panelin-323-fix`) @ `aa17414` — pushed, clean.

> ⚠️ The MAIN checkout `~/calculadora-bmc` is **mid interactive-rebase** of `main` onto `238e510`
> (13 of 17 commits remaining; `git rebase --continue` pending). This is a **concurrent live
> session's** operation, NOT this session's. Do not continue/abort/edit the main checkout.

**Uncommitted changes**:
- Worktree `fix/panelin-323-review`: clean — everything committed & pushed as **PR #330**.
- MAIN checkout (mid-rebase, do NOT touch): staged `docs/team/PROJECT-STATE.md`; modified
  `server/routes/webhooks.js`; untracked `server/lib/facturaExpressClient.js`,
  `server/lib/panelinDb.js`, `.playwright-mcp/`, 2 production screenshots. These belong to the
  live session, not this one.
- **LOST**: the tutorial bottom-left change (default `left` → `EDGE_MARGIN`, `STORAGE_KEY` →
  `bmc_floating_tutorial_position_v2` in `src/components/tutorial/FloatingTutorialButton.jsx`)
  was uncommitted in the main checkout and is **no longer in the working tree, HEAD, or stashes**
  after the rebase reset the tree. Must be re-applied if still wanted.

**Blockers**:
- MAIN checkout interactive rebase in progress (owned by a concurrent live session). Do NOT
  touch it. `BITACORA-MATIAS.md` was intentionally NOT updated this session — editing it on a
  half-rebased tree (or in the bg-isolated main checkout) would conflict. Add the line once the
  rebase settles.

**Next prompt to resume exactly**:
"Continue from here:
 (1) Confirm the main-checkout rebase finished — `git -C ~/calculadora-bmc status` shows no
     'rebase in progress' and BRANCH=main.
 (2) Re-apply the tutorial bottom-left default if still wanted: in
     `src/components/tutorial/FloatingTutorialButton.jsx`, set the default-position `left` to
     `EDGE_MARGIN` (keep `top` at `innerHeight - BUTTON_HEIGHT - 24`) and bump `STORAGE_KEY` to
     `bmc_floating_tutorial_position_v2`. Lint, then commit.
 (3) Shepherd draft **PR #330** (`fix/panelin-323-review` → `feat/panelin-floating-chat-20260611`):
     it fixes PR #323's unauthenticated `/api/panelin/*` router. Merge it INTO #323 before
     #323 → main. Expect a `server/routes/panelin.js` conflict with the in-flight
     `sync/facturaexpress/*` routes; add `writeLimiter` to those routes when reconciling.
 (4) Deferred follow-ups: rename Panelin tables to `panelin_*` BEFORE any prod `panelin:migrate`
     (needs a live Postgres to verify); the voice-bridge per-delta fix is owned by the live voice
     session.
 (5) Append the BITACORA-MATIAS.md line for this session once the rebase is done."

**Key files touched (this session)**:
- `server/routes/panelin.js` — `router.use(requireAuth)` + `writeLimiter` + `withTx` transactional PATCH (in PR #330)
- `tests/panelinApi.test.js` (new) + `package.json` `test:api` wiring (in PR #330)
- `scripts/verify-panelin-fase2.mjs` — now sends the service token (in PR #330)
- `~/.claude/settings.json` — `advisorModel` `"opus"` → `"fable"` (APPLIED; fixes the advisor 400 across model switches)
- `src/components/tutorial/FloatingTutorialButton.jsx` — bottom-left default + `_v2` key (**LOST** in main-checkout rebase; re-apply)

**Current state reference**:
- See `docs/team/PROJECT-STATE.md`
- PR #330 (draft): https://github.com/matiasportugau-ui/Calculadora-BMC/pull/330
- `npm run gate:local` was green for PR #330 (lint 0 errors, test + test:api incl. new suite).
