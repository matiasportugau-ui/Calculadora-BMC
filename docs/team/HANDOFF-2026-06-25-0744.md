# Handoff — 2026-06-25 07:44 BMC Chat → Cloud Run

**Status (2026-06-25 PM):** Cerrado. Default iframe = Cloud Run. Express `/chat` opt-in `VITE_BMC_CHAT_LOCAL=1`. Browser Cloud Run OK. Panelin empty-state fix (signature.png / dev resize). `gate:local` pass 2026-06-25. Ver `PROJECT-STATE.md` → Cambios recientes 2026-06-25.

**Current branch**: `main`

**Uncommitted changes**:
- `server/index.js` — added import + mount for bmcChat router
- `src/App.jsx` — added BmcChatPanel import
- `src/components/BmcChatPanel.jsx` — new: floating chat button + iframe
- `server/routes/bmcChat.js` — new: chat API for Express (now unused)
- `server/lib/bmcChatGemini.js`, `server/lib/bmcChatSheets.js` — new: helpers (unused)
- `server/static/` — new: static assets (unused)
- `web/package.json` — new: deps for Cloud Run deploy

**Blockers**:
- none

**Next prompt to resume exactly**:
"Continue from here: run `npm run start:api` (from calculadora-bmc) and open the app at localhost:5173. Click the 💬 button bottom-right to verify the iframe loads the Cloud Run chat. If Express port 3001 fails to start, check node_modules (corrupted symlinks) or use `doppler run -- npm run start:api`."

**Key files touched**:
- `~/calculadora-bmc/src/components/BmcChatPanel.jsx` — iframe → Cloud Run
- `~/bmc-sheet-quote-pipeline/web/server.mjs` — deployed to Cloud Run
- `~/bmc-sheet-quote-pipeline/web/sheets.mjs` — deployed to Cloud Run
- `~/bmc-sheet-quote-pipeline/web/gemini.mjs` — deployed to Cloud Run
- `~/bmc-sheet-quote-pipeline/web/index.html` — deployed to Cloud Run
- `~/bmc-sheet-quote-pipeline/web/package.json` — dependencies for Cloud Run build
- `~/.claude/skills/bmc-chat-web/SKILL.md` — opencode skill to manage the service

**Cloud Run service**:
- URL: `https://bmc-chat-642127786762.us-central1.run.app`
- Project: `chatbot-bmc-live`, region: `us-central1`
- Secrets: GEMINI_API_KEY, GOOGLE_SHEETS_CREDENTIALS (from Secret Manager)
- Min instances: 1 (always on), 256Mi RAM, 1 CPU
- Verified: GET / returns HTML (200), GET /api/inquiries returns 11 inquiries

**Current state reference**:
- See docs/team/PROJECT-STATE.md
