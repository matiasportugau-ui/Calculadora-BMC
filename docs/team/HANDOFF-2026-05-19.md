# Handoff — 2026-05-19

## Branch
`main` — up to date with `origin/main`

## Uncommitted / Untracked Files

### Staged (needs commit):
- `docs/hub-tasks-module/OPERATOR-CHECKLIST.md` — 310-line provisioning guide for 3 operator secrets

### Modified (unstaged — NOT from this session, pre-existing work from another agent):
- `server/routes/pdf.js`
- `src/App.jsx`
- `src/components/hub/tasks/hooks/useTasksSync.js`
- `src/data/calculatorDataVersion.js`

### Untracked:
- `goal-prompt-tareas-phase-c1-c3.md` — master goal prompt for next Phase 1 continuation
- `src/components/PdfPreview.jsx` — new component, untracked

## Current State of Tareas Module

Phase 0 ✅ Complete (committed + pushed)
Phase 1 — Phase C ✅ Complete (6 commits pushed today):
  - Migration fixed + applied to production Supabase
  - Task CRUD reads live (`GET /api/tasks/lists`, etc.)
  - useTasks.js hooks wired to real endpoints
  - TasksModule UI functional with graceful degradation

**BLOCKED on 3 operator actions before Phase 1 can continue:**
1. Create Google Tasks OAuth 2.0 Client in GCP Console → store as `GOOGLE_TASKS_CLIENT_ID` + `GOOGLE_TASKS_CLIENT_SECRET` in Secret Manager (`chatbot-bmc-live`)
2. Generate `SUPABASE_PGP_ENCRYPT_KEY`: `openssl rand -base64 32` → store in Secret Manager
3. Enable Cloud Scheduler API + create `tasks-sync-60s` cron job (`* * * * *` → `POST /sync/google-tasks/pull`)

Full instructions: `docs/hub-tasks-module/OPERATOR-CHECKLIST.md`

## Blockers
- NO code blockers — all existing code is correct and gate:local passes
- OPERATOR blockers above are the only thing preventing Phase 1 completion

## Next Prompt to Resume
```
Retomar Tareas Phase 1. El OPERATOR-CHECKLIST.md está staged y necesita commit.
Los modificados sin stage son de otra sesión — revisar si van en el mismo commit
o separado. Una vez que el operador provisione los 3 secrets (ver
docs/hub-tasks-module/OPERATOR-CHECKLIST.md), implementar:
1. tasksOAuth.js — PKCE flow completo
2. tasksSync.js — polling handler con updatedMin
3. useTasksSync.js — hooks de sync status + conflict resolution
```
