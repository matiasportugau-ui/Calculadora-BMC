# HANDOFF — 2026-07-04 (programa de 3 sabios: hardening AI + control plane + pricing)

## Contexto
Sesión que ejecutó el consenso de una deliberación de 3 agentes ("sabios": Arquitectura/AI,
Seguridad/Infra, Producto/Negocio) sobre los issues de mayor impacto. Plan completo en
`.claude/plans/genraerreporte-de-ai-conetions-idempotent-barto.md`.

## Shipped a producción (todo con gate verde + disciplina de merge)
- **PR #563** — `fix(deps): move nodemailer to dependencies`. Bug vivo descubierto: nodemailer estaba
  en devDependencies mientras `server/Dockerfile` instala `--omit=dev` → **ausente de la imagen del API**,
  todo envío de email (emailReply/waOperatorAuth/marketIntel) tiraba `ERR_MODULE_NOT_FOUND`. Cierra gh#491.
- **PR #567** — `fix(security): require auth on /crm/suggest-response`. Era la única ruta de generación IA
  llamable **anónimamente** (verificado por curl). Nuevo modo `authOnly` en `requireServiceOrUser` +
  rate-limit 20/min. Follow-up #575 (deploy smoke token).
- **PR #576** — `feat(reliability): agentCore failover hardening`. Timeouts por proveedor (`callWithTimeout`,
  30s, AbortSignal+race), cooldown por proveedor (deprioriza, no elimina), orden capability-aware en
  agentChat (`claude→gemini→grok→openai` — solo claude/gemini corren tools). Expuesto en
  `/api/assistants/status` vía `providerCooldowns`. Gate del flip de panelin.
- **PR #578** — `docs: record panelin flip`. 
- **Flip de panelin en prod** — `ASSISTANTS_ACTIVE` = `canales;ml;panelin` (repo Variable + dispatch de
  `deploy-calc-api.yml`). Verificado: los 3 `live`, `/api/agent/chat` → 200 (antes 503), 0 errores post-flip.
  Rollback: `gh variable set ASSISTANTS_ACTIVE --body "canales;ml"` + redeploy.
- **Ya estaban hechos por runs paralelos:** smoke `reason` fix, WA canonical flip (`OMNI_WA_CANONICAL=1`).

## Pricing gh#358 (gotero lateral cámara ISODEC) — corregido en la Matriz, con interino
- Diagnóstico: la fila **PGLC250** (tab BROMYROS, fila 151, sheet `1oDM…`) era una **copia completa del
  SKU 6844** (lateral 200mm sin cámara) → 250 salía más barato que 200. El código mapea `PGLC{n}` (no
  `GLDCAM`, que es el label cosmético del campo `sku`). Ver [[reference-bmc-matriz-live-access]].
- **Aplicado (con OK del owner):** costo derivado interino `31.08` en G151 (venta 37.30 / web 43.51 por
  fórmula), monotonicidad restaurada, nota "derivado — revisar BROMYROS" en la celda. Documentado en gh#358
  (3 comentarios).
- **Pendiente owner:** costo real de BROMYROS para PGLC250 (reemplazar el interino).

## Pendiente / próximos pasos
1. **Decisión abierta — re-bake `constants.js`:** la Matriz ya tiene PGLC250=31.08 pero el código sigue con
   el valor viejo (26.85/32.22). Re-bake (`bake-matriz-to-constants.mjs`) propaga el interino a las
   cotizaciones (venta 250mm $32.22→$37.30, cambio de precio real) — o esperar a BROMYROS. Recomendación:
   re-bake ya (interino > copy-paste confirmado), pero es cambio de precio a cliente → owner decide.
2. **PR 3 (caching) — NO implementado, diseñado.** Footgun: no cachear el prompt entero (buildSystemPrompt
   embebe calcState → cache write 1.25x sin hit). Rutear seam a `bareSystemPrompt`. ~90% ahorro input.
   Ver [[feedback-agentcore-prompt-caching-footgun]].
3. Quote-loop (estado enviada/vista/aceptada + link público + recordatorios): relevado (40% andamiado —
   persiste, HTML cliente, driverToken existen; falta el ciclo de eventos). **Diferido a próximo tren** por
   decisión del owner.

## Estado git
- Worktree: `.claude/worktrees/ai-connections-report`, rama `main` (esta rama `docs/handoff-2026-07-04` es
  solo para este doc). Sin trabajo sin commitear salvo `goal-prompt-resolve-all-pending-one-run.md` (untracked).

## Prompt para retomar
> Retomá el programa de 3 sabios (plan en `.claude/plans/genraerreporte-*.md`). Pendiente: (1) decidir re-bake
> de constants.js por el fix interino de PGLC250 en la Matriz, o esperar costo real de BROMYROS; (2) implementar
> PR 3 (prompt caching en agentCore vía bareSystemPrompt — ojo el footgun del calcState). Verificá primero el
> estado en prod con `/api/assistants/status?deep=1`.
