# HANDOFF — 2026-07-07 (cierre del programa de 3 sabios + observabilidad + diagnóstico del seam)

Continúa `HANDOFF-2026-07-04.md`. Esta sesión cerró el programa completo y diagnosticó por qué el
seam de AI sirve en Gemini.

## Shipped a producción esta sesión (todos con gate verde + merge disciplinado)
- **PR #605** — `feat(perf): prompt caching en agentCore + fix historial Gemini`. `buildSystemPromptParts()`
  separa el system prompt en prefijo estático cacheable (~20k tokens) + cola dinámica; `cache_control`
  ephemeral sobre el prefijo en la rama Claude; `buildSystemPrompt()` queda byte-idéntico (callers
  intactos). Fix Gemini: `systemInstruction` + `contents[]` (antes descartaba el historial). Tests
  `promptCacheSplit.test.js` 7/7.
- **PR #607** — `chore(obs): loguear cache_read/write + input tokens en agent_core_call`. Observabilidad
  del hit rate del cache.
- **PR #609** — `chore(obs): loguear fallas por-proveedor en el seam`. Nuevo evento `provider_error`
  (provider/model/channel/status/error_type/detail) + `provider_empty`. Log-only.
- **Antes en la sesión** (ya en handoff #583 o mergeados en paralelo): #563 nodemailer, #567 auth
  suggest-response, #576 failover hardening, #578 docs panelin flip, **panelin flipeado ON en prod**.

## Hallazgo principal — por qué el seam sirve Gemini (issue #610)
El logging `provider_error` reveló la causa raíz (NO es código):
- **Claude:** `400 invalid_request_error` — *"Your credit balance is too low… purchase credits."* →
  la cuenta Anthropic está **sin créditos** (la key es válida).
- **Grok:** `400 "Incorrect API key provided"` → el `GROK_API_KEY` en prod está **inválido**.
- La cadena `claude→grok→gemini→openai` cae hasta Gemini, que responde. El failover (PR #576) hace
  su trabajo; el caching (PR #605) es correcto pero su payoff está **latente** hasta que Claude sirva.
- Probablemente explica el smoke `channels_pipeline` fallando (#571).

## Pricing gh#358 (gotero cámara ISODEC) — verificado + corregido en la Matriz
- Confirmado contra la **Matriz en vivo** (tab BROMYROS): el mapeo estaba BIEN (código = `PGLC{n}`,
  no `GLDCAM`); la inversión 200>250 era real y ya flageada "REVISAR - inabilitado en ML".
- Causa: **copy-paste de fila completa** — PGLC250 tenía los valores del SKU 6844 (lateral 200 s/cámara).
- **Aplicado (con OK del owner):** costo derivado interino `31.08` en la celda G151 de la Matriz +
  nota "derivado — revisar BROMYROS". Documentado en gh#358 (3 comentarios). Ver
  [[reference-bmc-matriz-live-access]].

## Estado del programa de 3 sabios: **COMPLETO**
Los 6 PRs (auth, failover, WA flip, smoke-alert, panelin flip, caching) + observabilidad + diagnóstico.

## Pendiente — todo owner-gated / externo
1. **#610 (bloquea el payoff del caching + confiabilidad):** cargar créditos Anthropic (billing) +
   rotar `GROK_API_KEY` en GCP Secret Manager (`chatbot-bmc-live`) + redeploy. **Prompt de fix listo
   para la extensión de Claude en `goal-prompt-fix-seam-credentials.md`** (prepara la compra pero para
   el click de pago al owner). Al resolverse, Claude vuelve a servir el seam y el cache empieza a pegar.
2. **#358:** costo real de BROMYROS para PGLC250 → reemplazar el interino en la Matriz + re-bake de
   `constants.js` (`bake-matriz-to-constants.mjs`) + lint de monotonicidad.

## Estado git
- Worktree `.claude/worktrees/ai-connections-report`, rama `main` (esta rama `docs/handoff-2026-07-07`
  es solo para este doc). Sin trabajo sin commitear salvo dos `goal-prompt-*.md` untracked (el de fix
  de credenciales y el batch original).

## Prompt para retomar
> Verificá #610 en prod: `/api/assistants/status?deep=1` → si `providerCooldowns` de claude/grok está
> limpio y `agent_core_call` muestra `provider="claude"` con `cache_read_tokens>0`, el fix de
> credenciales funcionó (cerrá #610). Si no, revisá los `provider_error` logs. Aparte: si el owner ya
> tiene el costo real de BROMYROS para PGLC250, corré el re-bake de constants.js + el lint de
> monotonicidad (gh#358).
