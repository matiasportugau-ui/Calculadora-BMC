# Handoff — 2026-06-13 Grok MCP Configuration + Project Scope Additions (BMC workspace)

**Current branch**: `fix/review-5ae44e21-facturaexpress-platform`

**Uncommitted changes**:
- docs/team/PROJECT-STATE.md (updated with recent Fase 6 realtime, product centralization, platform v1 status)
- package.json
- scripts/collect-catalog-to-panelin.mjs
- server/config.js
- server/index.js
- server/routes/panelin.js
- src/App.jsx
- src/components/BmcModuleNav.jsx
- src/components/ConfigPanel.jsx
- src/components/PanelinCalculadoraV3_backup.jsx
- src/components/PanelinCalculadoraV3_legacy_inline.jsx
- src/components/PanelinChatPanel.jsx
- src/data/calculatorDataVersion.js
- src/utils/projectFile.js
- .grok/config.toml (NEW: project-scoped MCP servers added - bmc, task-master-ai with startup_timeout_sec=60, memory; also pre-existing playwright + chrome-devtools)
- .cursor/skills/bmc-propagation-automate/ (untracked)
- scripts/publish-panelin-to-shopify.mjs (untracked)
- src/components/hub/panelin/ (untracked dir)

**Blockers**:
- Ongoing from PROJECT-STATE.md: Collector implementation for product centralization (Shopify-first seeding to Panelin) pending (Phase 1 of intelligent plan); operator actions needed for GCP keys/quotas (Anthropic credit, OpenAI, Grok invalid key, Gemini API enablement); Google Calendar API + scope for Tasks Phase D (users need re-consent); various Gate-0 infra (revoke fine-grained tokens, provision webhook secrets); no new MCP-specific blockers from this session.
- External: some operator manual steps (Docker Desktop for repro scripts, billing/rotations) that require human action outside agent.

**Next prompt to resume exactly**:
"Continue from here: Verify the new project-scoped MCPs in calculadora-bmc (run `cd /Users/matias/calculadora-bmc && grok mcp list` and `grok mcp doctor`); the .grok/config.toml now includes bmc (custom control), task-master-ai (fixed with 60s timeout), memory per the importance ranking from the Grok MCP session. Also see the updated audit at ~/grok-mcp-audit-2026-06-12.json. Resume the Panelin/FacturaExpress platform review on branch fix/review-5ae44e21-facturaexpress-platform (Fase 6 realtime complete, collector pending, etc.). Check docs/team/PROJECT-STATE.md for latest."

**Key files touched**:
- /Users/matias/calculadora-bmc/.grok/config.toml (MCP additions for bmc #1, task-master-ai, memory #6)
- /Users/matias/grok-mcp-audit-2026-06-12.json (updated with importance ranking + latest doctor + project additions)
- Many src/server/docs in the ongoing facturaexpress/platform review (per git status and PROJECT-STATE)

**Current state reference**:
- See docs/team/PROJECT-STATE.md (heavy on Fase 6 realtime SSE for Panelin/FacturaExpress, product central PIM plan, platform v1 dashboard/ship, review 5ae44e21 fixes closed)
- Latest handoff: this file + prior ones like HANDOFF-TAREAS-STABILIZATION-SESSION-2.md
- Grok MCP work summary: User invoked /grok-mcp-add, reviewed configs/doctors, ranked servers by importance (bmc #1 for custom control, grok_com_github #2, grok_com_vercel #3, playwright #4 etc.), chose to add top ones to project scope (option 3). Native Grok config minimal; heavy reliance on compat + managed grok_com_* (many healthy with high tool counts).