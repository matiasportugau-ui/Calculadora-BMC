# Handoff — 2026-08-07 capitalizar Envíos git + security harden chain

**Session scope**: Envíos capitalizar + harden only (not mobile program, not truck visual, not WA pool WIP).

**Session closed at**: `main` @ `ab3ba43d` (Merge #929 concurrent Entregado delete wired).

**Note for resume agents**: `origin/main` may have moved after this session (e.g. #935 techo irregular). Workspace may be on a feature branch with unrelated dirty files — **never** `git add -A` this handoff with WA/truck/mobile WIP. Live product history: `docs/team/PROJECT-STATE.md`.

**Uncommitted at session end (Envíos)**:
- none for the Envíos code path (all shipped via PRs below)

**Blockers**:
- ninguno
- Residual: full sale-state **UI** (EntregadoConfirmModal, SaleStateEditor, #893 stack) still not productized — server has `logistica-estado` / `logistica-entregado` + `saleState` helpers. UI wire is optional follow-up.

**Next prompt to resume exactly**:
"Continue from here: either (1) smoke/prod UAT of Envíos wizard + Free-Drag long-press + list PDF/Mapa XSS on https://calculadora-bmc.vercel.app/logistica after deploys settle, or (2) productize sale-state UI from closed #893. Do not re-open #901/#910/#892/#907/#905/#902/#896 — use superseding PRs already merged. Ignore unrelated dirty work (mobile program / truck visual / WA pg pool) unless that is the explicit task."

**Session delivered (capitalized + merged to main)**:

| Track | Merged PRs | Tip / notes |
|-------|------------|-------------|
| Phase A lock | commits on feature branches + draft PRs bmc-control #1, Panelin-Workspace #5 | no longer disk-only |
| Wizard + DnD + SDD | #909, #906, #908 | Setup Wizard + trackpad DnD |
| Security / Bug AP | #915 SSRF, #916 wizard XSS/pickup/cloud draft, #918 Btn href XSS | |
| Free-Drag Bug AO | #920 | long-press after pointerup |
| Goal #905/#902/#896 | #921 re-confirm, #922 blank CSV rows, #924 helpers + **#929 wire concurrent delete** | #929 is the production path skeptic required |

**Main tip (session end)**: `ab3ba43d` Merge #929  
**smoke:prod (session end)**: OK (health, MATRIZ, WA, finanzas)

**Key files touched**:
- `src/components/logistica/wizard/*`, `LogisticaCargoScene3d.jsx`, `BmcLogisticaApp.jsx`
- `src/utils/logistica/{safeExternalUrl,freeDragLongPress,wizardState,pickupCatalog,ventasSheetMap,saleState,routeExport}.js`
- `server/routes/{envios,repartos,bmcDashboard}.js`, `server/lib/enviosAdjuntoFetch.js`
- `tests/{enviosAdjuntoFetch,safeExternalUrl,freeDragLongPress,reparto*,ventasSheetMap,saleState,enviosEntregadoConcurrentDelete}.test.js`

**Current state reference**:
- See docs/team/PROJECT-STATE.md (cambios recientes 2026-08-07)
- Superseded drafts (do not re-land): #901→#915, #910→#916, #892→#918, #907→#920, #905→#921, #902→#922, #896→#924+#929
