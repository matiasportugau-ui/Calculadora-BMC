# Commit 3d8529b

- Fecha: 2026-04-25
- Hora: 03:01:07
- Autor: matiasportugau-ui
- Email: matias.portugau@gmail.com
- Branch: main
- Tipo: feat
- Scope: pdf
- Commit: feat(pdf): server-side PDF via Playwright — vectorial quality for Drive uploads

## Resumen
server-side PDF via Playwright — vectorial quality for Drive uploads

## Descripción
Este cambio registra el commit `feat(pdf): server-side PDF via Playwright — vectorial quality for Drive uploads` dentro del sistema de trazabilidad del proyecto. Se modificaron 2 archivos: server/routes/pdf.js, src/utils/pdfGenerator.js.

Contexto del commit:
server/routes/pdf.js:
  POST /api/pdf/generate — receives HTML string, renders with Playwright
  Chromium (executablePath for macOS), returns A4 PDF blob. @page CSS,
  SVG vectors, grid/flex all rendered natively. Falls back with 503 when
  Playwright is unavailable so the client can use the html2pdf fallback.

server/index.js:
  Register /api/pdf route before the broad /api dashboard router.

src/utils/pdfGenerator.js:
  htmlToPdfBlob() now tries the Playwright server endpoint first.
  Only falls back to html2pdf.js (html2canvas) when the server
  returns an error — preserving Drive upload quality without breaking
  offline/fallback scenarios.

src/utils/quotationViews.js:
  Resolve merge conflict markers (trivial comment/formatting only).

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>

## Señales automáticas (QA / release)
- Posible regresión (heurística): **no**
- Tests / validación tocados: **no**
- Breaking mencionado: **no**
- Impacto release sugerido: **med**
- Áreas (prefijos): server, src

## Archivos modificados
- server/routes/pdf.js
- src/utils/pdfGenerator.js

## Diff summary
```text
server/routes/pdf.js      | 9 +++++----
 src/utils/pdfGenerator.js | 4 ++--
 2 files changed, 7 insertions(+), 6 deletions(-)
```

## Riesgo de cambio (tamaño / extensiones)
Verde
