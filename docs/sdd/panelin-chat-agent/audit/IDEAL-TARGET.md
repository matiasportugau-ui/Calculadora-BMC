# Ideal 100% — Panelin Chat Agent

For **this** system, 100% means a new engineer can:

1. Deploy API + SPA with all agent env names filled from OPS.
2. Hit `POST /api/agent/chat` and parse every SSE event type.
3. Recreate tool allowlist from manifest with write-gate rules.
4. Choose correct voice path per browser (matrix).
5. Run `test:agent` + goldens and interpret failures.
6. Rotate OpenAI/Anthropic keys without reading SDD fiction.
7. Know open defects (V3 wake loops, Firefox STT gap) with owners.

Ideal artifacts: as-built `SDD.md` ≥90 + `SDD-TARGET.md` (north star) + living `tools-manifest` + golden index.
