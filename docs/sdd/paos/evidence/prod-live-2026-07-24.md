# PAOS production LIVE — 2026-07-24

**Tag:** CONFIRMED · **Service:** Cloud Run `panelin-calc` · **Project:** `chatbot-bmc-live` · **Region:** `us-central1`

## Probe

```http
GET https://panelin-calc-q74zutv7dq-uc.a.run.app/api/paos/health
→ 200
```

```json
{
  "ok": true,
  "paos": {
    "enabled": true,
    "promote": true,
    "canaryPct": 0,
    "ledgerRetentionDays": 90
  },
  "ledger": {
    "memoryCount": 0,
    "retentionDays": 90,
    "enabled": true
  }
}
```

## Traffic (after fix)

| Field | Value |
|-------|--------|
| Revision | `panelin-calc-00886-g5f` |
| Traffic | 100% LATEST |
| Image | `us-central1-docker.pkg.dev/chatbot-bmc-live/cloud-run-repo/panelin-calc:48153e9dc0f57939fecc215759c898baa8ffcfaa` |
| Code | PR #777 squash-merge to `main` |

## Env (Cloud Run)

| Name | Value |
|------|--------|
| `PAOS_ENABLED` | `1` |
| `PAOS_PROMOTE` | `1` |
| `PAOS_CANARY_PCT` | `0` |
| `PAOS_LEDGER_RETENTION_DAYS` | `90` |

## Root cause of earlier 404

1. GHA **Deploy Calculator API** run `30093481885` **built, pushed, and deployed** the PAOS image successfully.
2. Workflow **job failed** only on post-deploy `npm run smoke:prod` — `POST /api/crm/suggest-response` **503** (OpenAI 429 quota / Claude / Grok key issues). Unrelated to PAOS routes.
3. Service traffic remained **pinned 100%** to pre-PAOS revision **`panelin-calc-00877-4b9`** while newer revisions `00881–00886` existed with Ready=True but **0% traffic** → `GET /api/paos/health` **404**.

## Fix applied

```bash
gcloud run services update-traffic panelin-calc \
  --to-latest \
  --region=us-central1 \
  --project=chatbot-bmc-live
```

Result: traffic → **100% LATEST (`panelin-calc-00886-g5f`)**; health **200** with `enabled:true`.

## Ops runbook (if 404 again after “successful” image build)

1. `gcloud run revisions list --service=panelin-calc --region=us-central1 --limit=5`
2. Confirm latestReady / traffic percent
3. If traffic stuck on old rev: `gcloud run services update-traffic panelin-calc --to-latest --region=us-central1 --project=chatbot-bmc-live`
4. Re-probe `/api/paos/health`

## Related residual (non-PAOS)

- GHA deploy may still go **red** on smoke AI keys; does not mean PAOS image is absent.
- Optional product: IMP-PAOS-06..08 (canary %, USER_OVERRIDE schema, privacy).
