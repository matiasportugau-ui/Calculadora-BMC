# Evidence — restore create CR (N0)

Branch: `feat/workspace-knowledge-create-cr` from origin/main.

- POST create route re-added (was missing from tree after branch switch).
- Approve source_type PDF → Q&A.
- Reject deletes pending `kb-cr-{id}` knowledge_docs.
- Smoke script + npm script restored.
