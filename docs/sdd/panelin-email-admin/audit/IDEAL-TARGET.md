# Ideal 100% — panelin-email-admin

For this system, a perfect SDD + product would include:

1. Recreation-complete SDD (schema 1–12) with CONFIRMED evidence for every external interface.
2. ContextGroup → one SharedWorkspace + **one persisted chat history per groupId**.
3. Full email loop verified in prod: list → read → classify → draft → HITL send (Gmail OAuth green).
4. Omni AI orchestrator classify/suggest on ingest with budget caps and Admin alert routing.
5. A11y audit (axe) on ContextGroupBar + keyboard-only operator path documented.
6. Explicit non-goals frozen: no Gmail DOM, no autonomous send, Chatwoot not required for PMCA.
