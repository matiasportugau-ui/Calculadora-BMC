{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the TraKtiMe always-on-top timer window. The window loads remote content, so it is granted only window show/hide/focus/always-on-top. The global shortcut is registered Rust-side (see lib.rs), so the webview is deliberately NOT given any global-shortcut:* permission.",
  "windows": ["timer"],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus",
    "core:window:allow-set-always-on-top"
  ]
}
